// Code generated by https://github.com/gagliardetto/anchor-go. DO NOT EDIT.
// This file contains instructions and instruction parsers.

package mplcore

import (
	"bytes"
	"fmt"
	binary "github.com/gagliardetto/binary"
	solanago "github.com/gagliardetto/solana-go"
)

// Builds a "create_v1" instruction.
func NewCreateV1Instruction(
	// Params:
	createV1ArgsParam CreateV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createV1ArgsParam`:
		err = enc__.Encode(createV1ArgsParam)
		if err != nil {
			return nil, newFieldError("createV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Signer, Required
		// The address of the new asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, true))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "authority": Read-only, Signer, Optional
		// The authority signing for creation
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 4 "owner": Read-only, Non-signer, Optional
		// The owner of the new asset. Defaults to the authority if not present.
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, false))
		// Account 5 "update_authority": Read-only, Non-signer, Optional
		// The authority on the new asset
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_collection_v1" instruction.
func NewCreateCollectionV1Instruction(
	// Params:
	createCollectionV1ArgsParam CreateCollectionV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateCollectionV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createCollectionV1ArgsParam`:
		err = enc__.Encode(createCollectionV1ArgsParam)
		if err != nil {
			return nil, newFieldError("createCollectionV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Signer, Required
		// The address of the new asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, true))
		// Account 1 "update_authority": Read-only, Non-signer, Optional
		// The authority of the new asset
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_plugin_v1" instruction.
func NewAddPluginV1Instruction(
	// Params:
	addPluginV1ArgsParam AddPluginV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddPluginV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addPluginV1ArgsParam`:
		err = enc__.Encode(addPluginV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addPluginV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_collection_plugin_v1" instruction.
func NewAddCollectionPluginV1Instruction(
	// Params:
	addCollectionPluginV1ArgsParam AddCollectionPluginV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddCollectionPluginV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addCollectionPluginV1ArgsParam`:
		err = enc__.Encode(addCollectionPluginV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addCollectionPluginV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_plugin_v1" instruction.
func NewRemovePluginV1Instruction(
	// Params:
	removePluginV1ArgsParam RemovePluginV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemovePluginV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removePluginV1ArgsParam`:
		err = enc__.Encode(removePluginV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removePluginV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_collection_plugin_v1" instruction.
func NewRemoveCollectionPluginV1Instruction(
	// Params:
	removeCollectionPluginV1ArgsParam RemoveCollectionPluginV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemoveCollectionPluginV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removeCollectionPluginV1ArgsParam`:
		err = enc__.Encode(removeCollectionPluginV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removeCollectionPluginV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_plugin_v1" instruction.
func NewUpdatePluginV1Instruction(
	// Params:
	updatePluginV1ArgsParam UpdatePluginV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdatePluginV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updatePluginV1ArgsParam`:
		err = enc__.Encode(updatePluginV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updatePluginV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_collection_plugin_v1" instruction.
func NewUpdateCollectionPluginV1Instruction(
	// Params:
	updateCollectionPluginV1ArgsParam UpdateCollectionPluginV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateCollectionPluginV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateCollectionPluginV1ArgsParam`:
		err = enc__.Encode(updateCollectionPluginV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateCollectionPluginV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "approve_plugin_authority_v1" instruction.
func NewApprovePluginAuthorityV1Instruction(
	// Params:
	approvePluginAuthorityV1ArgsParam ApprovePluginAuthorityV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ApprovePluginAuthorityV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `approvePluginAuthorityV1ArgsParam`:
		err = enc__.Encode(approvePluginAuthorityV1ArgsParam)
		if err != nil {
			return nil, newFieldError("approvePluginAuthorityV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "approve_collection_plugin_authority_v1" instruction.
func NewApproveCollectionPluginAuthorityV1Instruction(
	// Params:
	approveCollectionPluginAuthorityV1ArgsParam ApproveCollectionPluginAuthorityV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ApproveCollectionPluginAuthorityV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `approveCollectionPluginAuthorityV1ArgsParam`:
		err = enc__.Encode(approveCollectionPluginAuthorityV1ArgsParam)
		if err != nil {
			return nil, newFieldError("approveCollectionPluginAuthorityV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "revoke_plugin_authority_v1" instruction.
func NewRevokePluginAuthorityV1Instruction(
	// Params:
	revokePluginAuthorityV1ArgsParam RevokePluginAuthorityV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RevokePluginAuthorityV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `revokePluginAuthorityV1ArgsParam`:
		err = enc__.Encode(revokePluginAuthorityV1ArgsParam)
		if err != nil {
			return nil, newFieldError("revokePluginAuthorityV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "revoke_collection_plugin_authority_v1" instruction.
func NewRevokeCollectionPluginAuthorityV1Instruction(
	// Params:
	revokeCollectionPluginAuthorityV1ArgsParam RevokeCollectionPluginAuthorityV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RevokeCollectionPluginAuthorityV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `revokeCollectionPluginAuthorityV1ArgsParam`:
		err = enc__.Encode(revokeCollectionPluginAuthorityV1ArgsParam)
		if err != nil {
			return nil, newFieldError("revokeCollectionPluginAuthorityV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "burn_v1" instruction.
func NewBurnV1Instruction(
	// Params:
	burnV1ArgsParam BurnV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_BurnV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `burnV1ArgsParam`:
		err = enc__.Encode(burnV1ArgsParam)
		if err != nil {
			return nil, newFieldError("burnV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Optional
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "burn_collection_v1" instruction.
func NewBurnCollectionV1Instruction(
	// Params:
	burnCollectionV1ArgsParam BurnCollectionV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_BurnCollectionV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `burnCollectionV1ArgsParam`:
		err = enc__.Encode(burnCollectionV1ArgsParam)
		if err != nil {
			return nil, newFieldError("burnCollectionV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Writable, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 3 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "transfer_v1" instruction.
func NewTransferV1Instruction(
	// Params:
	transferV1ArgsParam TransferV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	newOwnerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_TransferV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `transferV1ArgsParam`:
		err = enc__.Encode(transferV1ArgsParam)
		if err != nil {
			return nil, newFieldError("transferV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Read-only, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "new_owner": Read-only, Non-signer, Required
		// The new owner to which to transfer the asset
		accounts__.Append(solanago.NewAccountMeta(newOwnerAccount, false, false))
		// Account 5 "system_program": Read-only, Non-signer, Optional
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_v1" instruction.
func NewUpdateV1Instruction(
	// Params:
	updateV1ArgsParam UpdateV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateV1ArgsParam`:
		err = enc__.Encode(updateV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Read-only, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The update authority or update authority delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_collection_v1" instruction.
func NewUpdateCollectionV1Instruction(
	// Params:
	updateCollectionV1ArgsParam UpdateCollectionV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	newUpdateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateCollectionV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateCollectionV1ArgsParam`:
		err = enc__.Encode(updateCollectionV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateCollectionV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The update authority or update authority delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "new_update_authority": Read-only, Non-signer, Optional
		// The new update authority of the asset
		accounts__.Append(solanago.NewAccountMeta(newUpdateAuthorityAccount, false, false))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "compress_v1" instruction.
func NewCompressV1Instruction(
	// Params:
	compressV1ArgsParam CompressV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CompressV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `compressV1ArgsParam`:
		err = enc__.Encode(compressV1ArgsParam)
		if err != nil {
			return nil, newFieldError("compressV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Read-only, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account receiving the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "decompress_v1" instruction.
func NewDecompressV1Instruction(
	// Params:
	decompressV1ArgsParam DecompressV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_DecompressV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `decompressV1ArgsParam`:
		err = enc__.Encode(decompressV1ArgsParam)
		if err != nil {
			return nil, newFieldError("decompressV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Read-only, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "collect" instruction.
func NewCollectInstruction(
	recipient1Account solanago.PublicKey,
	recipient2Account solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "recipient1": Writable, Non-signer, Required
		// The address of the recipient 1
		accounts__.Append(solanago.NewAccountMeta(recipient1Account, true, false))
		// Account 1 "recipient2": Writable, Non-signer, Required
		// The address of the recipient 2
		accounts__.Append(solanago.NewAccountMeta(recipient2Account, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		Instruction_Collect[:],
	), nil
}

// Builds a "create_v2" instruction.
func NewCreateV2Instruction(
	// Params:
	createV2ArgsParam CreateV2Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateV2[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createV2ArgsParam`:
		err = enc__.Encode(createV2ArgsParam)
		if err != nil {
			return nil, newFieldError("createV2ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Signer, Required
		// The address of the new asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, true))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "authority": Read-only, Signer, Optional
		// The authority signing for creation
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 4 "owner": Read-only, Non-signer, Optional
		// The owner of the new asset. Defaults to the authority if not present.
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, false))
		// Account 5 "update_authority": Read-only, Non-signer, Optional
		// The authority on the new asset
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_collection_v2" instruction.
func NewCreateCollectionV2Instruction(
	// Params:
	createCollectionV2ArgsParam CreateCollectionV2Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateCollectionV2[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createCollectionV2ArgsParam`:
		err = enc__.Encode(createCollectionV2ArgsParam)
		if err != nil {
			return nil, newFieldError("createCollectionV2ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Signer, Required
		// The address of the new asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, true))
		// Account 1 "update_authority": Read-only, Non-signer, Optional
		// The authority of the new asset
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_external_plugin_adapter_v1" instruction.
func NewAddExternalPluginAdapterV1Instruction(
	// Params:
	addExternalPluginAdapterV1ArgsParam AddExternalPluginAdapterV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddExternalPluginAdapterV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addExternalPluginAdapterV1ArgsParam`:
		err = enc__.Encode(addExternalPluginAdapterV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addExternalPluginAdapterV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_collection_external_plugin_adapter_v1" instruction.
func NewAddCollectionExternalPluginAdapterV1Instruction(
	// Params:
	addCollectionExternalPluginAdapterV1ArgsParam AddCollectionExternalPluginAdapterV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddCollectionExternalPluginAdapterV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addCollectionExternalPluginAdapterV1ArgsParam`:
		err = enc__.Encode(addCollectionExternalPluginAdapterV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addCollectionExternalPluginAdapterV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_external_plugin_adapter_v1" instruction.
func NewRemoveExternalPluginAdapterV1Instruction(
	// Params:
	removeExternalPluginAdapterV1ArgsParam RemoveExternalPluginAdapterV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemoveExternalPluginAdapterV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removeExternalPluginAdapterV1ArgsParam`:
		err = enc__.Encode(removeExternalPluginAdapterV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removeExternalPluginAdapterV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_collection_external_plugin_adapter_v1" instruction.
func NewRemoveCollectionExternalPluginAdapterV1Instruction(
	// Params:
	removeCollectionExternalPluginAdapterV1ArgsParam RemoveCollectionExternalPluginAdapterV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemoveCollectionExternalPluginAdapterV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removeCollectionExternalPluginAdapterV1ArgsParam`:
		err = enc__.Encode(removeCollectionExternalPluginAdapterV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removeCollectionExternalPluginAdapterV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_external_plugin_adapter_v1" instruction.
func NewUpdateExternalPluginAdapterV1Instruction(
	// Params:
	updateExternalPluginAdapterV1ArgsParam UpdateExternalPluginAdapterV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateExternalPluginAdapterV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateExternalPluginAdapterV1ArgsParam`:
		err = enc__.Encode(updateExternalPluginAdapterV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateExternalPluginAdapterV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_collection_external_plugin_adapter_v1" instruction.
func NewUpdateCollectionExternalPluginAdapterV1Instruction(
	// Params:
	updateCollectionExternalPluginAdapterV1ArgsParam UpdateCollectionExternalPluginAdapterV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateCollectionExternalPluginAdapterV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateCollectionExternalPluginAdapterV1ArgsParam`:
		err = enc__.Encode(updateCollectionExternalPluginAdapterV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateCollectionExternalPluginAdapterV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 4 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "write_external_plugin_adapter_data_v1" instruction.
func NewWriteExternalPluginAdapterDataV1Instruction(
	// Params:
	writeExternalPluginAdapterDataV1ArgsParam WriteExternalPluginAdapterDataV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	bufferAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_WriteExternalPluginAdapterDataV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `writeExternalPluginAdapterDataV1ArgsParam`:
		err = enc__.Encode(writeExternalPluginAdapterDataV1ArgsParam)
		if err != nil {
			return nil, newFieldError("writeExternalPluginAdapterDataV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The Data Authority of the External Plugin Adapter
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "buffer": Read-only, Non-signer, Optional
		// The buffer to write to the external plugin
		accounts__.Append(solanago.NewAccountMeta(bufferAccount, false, false))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "write_collection_external_plugin_adapter_data_v1" instruction.
func NewWriteCollectionExternalPluginAdapterDataV1Instruction(
	// Params:
	writeCollectionExternalPluginAdapterDataV1ArgsParam WriteCollectionExternalPluginAdapterDataV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	bufferAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_WriteCollectionExternalPluginAdapterDataV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `writeCollectionExternalPluginAdapterDataV1ArgsParam`:
		err = enc__.Encode(writeCollectionExternalPluginAdapterDataV1ArgsParam)
		if err != nil {
			return nil, newFieldError("writeCollectionExternalPluginAdapterDataV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The Data Authority of the External Plugin Adapter
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "buffer": Read-only, Non-signer, Optional
		// The buffer to write to the external plugin
		accounts__.Append(solanago.NewAccountMeta(bufferAccount, false, false))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 5 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_v2" instruction.
func NewUpdateV2Instruction(
	// Params:
	updateV2ArgsParam UpdateV2Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	newCollectionAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	logWrapperAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateV2[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateV2ArgsParam`:
		err = enc__.Encode(updateV2ArgsParam)
		if err != nil {
			return nil, newFieldError("updateV2ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "authority": Read-only, Signer, Optional
		// The update authority or update authority delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "new_collection": Writable, Non-signer, Optional
		// A new collection to which to move the asset
		accounts__.Append(solanago.NewAccountMeta(newCollectionAccount, true, false))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "log_wrapper": Read-only, Non-signer, Optional
		// The SPL Noop Program
		accounts__.Append(solanago.NewAccountMeta(logWrapperAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "execute_v1" instruction.
func NewExecuteV1Instruction(
	// Params:
	executeV1ArgsParam ExecuteV1Args,

	// Accounts:
	assetAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	assetSignerAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	programIdAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ExecuteV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `executeV1ArgsParam`:
		err = enc__.Encode(executeV1ArgsParam)
		if err != nil {
			return nil, newFieldError("executeV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "asset": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(assetAccount, true, false))
		// Account 1 "collection": Writable, Non-signer, Optional
		// The collection to which the asset belongs
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 2 "asset_signer": Read-only, Non-signer, Required
		// The signing PDA for the asset
		accounts__.Append(solanago.NewAccountMeta(assetSignerAccount, false, false))
		// Account 3 "payer": Writable, Non-signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, false))
		// Account 4 "authority": Read-only, Signer, Optional
		// The owner or delegate of the asset
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "program_id": Read-only, Non-signer, Required
		// The program id of the instruction
		accounts__.Append(solanago.NewAccountMeta(programIdAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_collection_info_v1" instruction.
func NewUpdateCollectionInfoV1Instruction(
	// Params:
	updateCollectionInfoV1ArgsParam UpdateCollectionInfoV1Args,

	// Accounts:
	collectionAccount solanago.PublicKey,
	bubblegumSignerAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateCollectionInfoV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateCollectionInfoV1ArgsParam`:
		err = enc__.Encode(updateCollectionInfoV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateCollectionInfoV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection": Writable, Non-signer, Required
		// The address of the asset
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 1 "bubblegum_signer": Read-only, Signer, Required
		// Bubblegum PDA signer
		accounts__.Append(solanago.NewAccountMeta(bubblegumSignerAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_collections_to_group_v1" instruction.
func NewAddCollectionsToGroupV1Instruction(
	// Params:
	addCollectionsToGroupV1ArgsParam AddCollectionsToGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddCollectionsToGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addCollectionsToGroupV1ArgsParam`:
		err = enc__.Encode(addCollectionsToGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addCollectionsToGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Non-signer, Required
		// The address of the group to modify
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The group update authority and collection update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_collections_from_group_v1" instruction.
func NewRemoveCollectionsFromGroupV1Instruction(
	// Params:
	removeCollectionsFromGroupV1ArgsParam RemoveCollectionsFromGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemoveCollectionsFromGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removeCollectionsFromGroupV1ArgsParam`:
		err = enc__.Encode(removeCollectionsFromGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removeCollectionsFromGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Non-signer, Required
		// The address of the group to modify
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The group update authority and collection update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_assets_to_group_v1" instruction.
func NewAddAssetsToGroupV1Instruction(
	// Params:
	addAssetsToGroupV1ArgsParam AddAssetsToGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddAssetsToGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addAssetsToGroupV1ArgsParam`:
		err = enc__.Encode(addAssetsToGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addAssetsToGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Non-signer, Required
		// The address of the group to modify
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The group update authority and asset update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_assets_from_group_v1" instruction.
func NewRemoveAssetsFromGroupV1Instruction(
	// Params:
	removeAssetsFromGroupV1ArgsParam RemoveAssetsFromGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemoveAssetsFromGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removeAssetsFromGroupV1ArgsParam`:
		err = enc__.Encode(removeAssetsFromGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removeAssetsFromGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Non-signer, Required
		// The address of the group to modify
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The group update authority and asset update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "add_groups_to_group_v1" instruction.
func NewAddGroupsToGroupV1Instruction(
	// Params:
	addGroupsToGroupV1ArgsParam AddGroupsToGroupV1Args,

	// Accounts:
	parentGroupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_AddGroupsToGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `addGroupsToGroupV1ArgsParam`:
		err = enc__.Encode(addGroupsToGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("addGroupsToGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "parent_group": Writable, Non-signer, Required
		// The address of the parent group to modify
		accounts__.Append(solanago.NewAccountMeta(parentGroupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The update authority of the parent and child groups
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "remove_groups_from_group_v1" instruction.
func NewRemoveGroupsFromGroupV1Instruction(
	// Params:
	removeGroupsFromGroupV1ArgsParam RemoveGroupsFromGroupV1Args,

	// Accounts:
	parentGroupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_RemoveGroupsFromGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `removeGroupsFromGroupV1ArgsParam`:
		err = enc__.Encode(removeGroupsFromGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("removeGroupsFromGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "parent_group": Writable, Non-signer, Required
		// The address of the parent group to modify
		accounts__.Append(solanago.NewAccountMeta(parentGroupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The update authority of the parent and child groups
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_group_v1" instruction.
func NewCreateGroupV1Instruction(
	// Params:
	createGroupV1ArgsParam CreateGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createGroupV1ArgsParam`:
		err = enc__.Encode(createGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("createGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Signer, Required
		// The address of the new group
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, true))
		// Account 1 "update_authority": Read-only, Signer, Optional
		// The authority of the new group
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 2 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "close_group_v1" instruction.
func NewCloseGroupV1Instruction(
	// Params:
	closeGroupV1ArgsParam CloseGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CloseGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `closeGroupV1ArgsParam`:
		err = enc__.Encode(closeGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("closeGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Non-signer, Required
		// The address of the group to close
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account receiving reclaimed lamports
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The update authority of the group
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update_group_v1" instruction.
func NewUpdateGroupV1Instruction(
	// Params:
	updateGroupV1ArgsParam UpdateGroupV1Args,

	// Accounts:
	groupAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	newUpdateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateGroupV1[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateGroupV1ArgsParam`:
		err = enc__.Encode(updateGroupV1ArgsParam)
		if err != nil {
			return nil, newFieldError("updateGroupV1ArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "group": Writable, Non-signer, Required
		// The address of the group to update
		accounts__.Append(solanago.NewAccountMeta(groupAccount, true, false))
		// Account 1 "payer": Writable, Signer, Required
		// The account paying for the storage fees
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 2 "authority": Read-only, Signer, Optional
		// The update authority of the group
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 3 "new_update_authority": Read-only, Non-signer, Optional
		// The new update authority of the group
		accounts__.Append(solanago.NewAccountMeta(newUpdateAuthorityAccount, false, false))
		// Account 4 "system_program": Read-only, Non-signer, Required
		// The system program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

type CreateV1Instruction struct {
	CreateV1Args CreateV1Args `json:"create_v1_args"`

	// Accounts:
	Asset                   solanago.PublicKey `json:"asset"`
	AssetWritable           bool               `json:"asset_writable"`
	AssetSigner             bool               `json:"asset_signer"`
	Collection              solanago.PublicKey `json:"collection"`
	CollectionWritable      bool               `json:"collection_writable"`
	CollectionOptional      bool               `json:"collection_optional"`
	Authority               solanago.PublicKey `json:"authority"`
	AuthoritySigner         bool               `json:"authority_signer"`
	AuthorityOptional       bool               `json:"authority_optional"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerWritable           bool               `json:"payer_writable"`
	PayerSigner             bool               `json:"payer_signer"`
	Owner                   solanago.PublicKey `json:"owner"`
	OwnerOptional           bool               `json:"owner_optional"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	UpdateAuthorityOptional bool               `json:"update_authority_optional"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
	LogWrapper              solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional      bool               `json:"log_wrapper_optional"`
}

func (obj *CreateV1Instruction) GetDiscriminator() []byte {
	return Instruction_CreateV1[:]
}

// UnmarshalWithDecoder unmarshals the CreateV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateV1Instruction", Instruction_CreateV1[:], discriminator)
	}
	// Deserialize `CreateV1Args`:
	err = decoder.Decode(&obj.CreateV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the CreateV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateV1Instruction(buf []byte) (*CreateV1Instruction, error) {
	obj := new(CreateV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateCollectionV1Instruction struct {
	CreateCollectionV1Args CreateCollectionV1Args `json:"create_collection_v1_args"`

	// Accounts:
	Collection              solanago.PublicKey `json:"collection"`
	CollectionWritable      bool               `json:"collection_writable"`
	CollectionSigner        bool               `json:"collection_signer"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	UpdateAuthorityOptional bool               `json:"update_authority_optional"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerWritable           bool               `json:"payer_writable"`
	PayerSigner             bool               `json:"payer_signer"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
}

func (obj *CreateCollectionV1Instruction) GetDiscriminator() []byte {
	return Instruction_CreateCollectionV1[:]
}

// UnmarshalWithDecoder unmarshals the CreateCollectionV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateCollectionV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateCollectionV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateCollectionV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateCollectionV1Instruction", Instruction_CreateCollectionV1[:], discriminator)
	}
	// Deserialize `CreateCollectionV1Args`:
	err = decoder.Decode(&obj.CreateCollectionV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateCollectionV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateCollectionV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateCollectionV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the CreateCollectionV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateCollectionV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateCollectionV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateCollectionV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateCollectionV1Instruction(buf []byte) (*CreateCollectionV1Instruction, error) {
	obj := new(CreateCollectionV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddPluginV1Instruction struct {
	AddPluginV1Args AddPluginV1Args `json:"add_plugin_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *AddPluginV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddPluginV1[:]
}

// UnmarshalWithDecoder unmarshals the AddPluginV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddPluginV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddPluginV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddPluginV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddPluginV1Instruction", Instruction_AddPluginV1[:], discriminator)
	}
	// Deserialize `AddPluginV1Args`:
	err = decoder.Decode(&obj.AddPluginV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddPluginV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddPluginV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddPluginV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the AddPluginV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddPluginV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddPluginV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddPluginV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddPluginV1Instruction(buf []byte) (*AddPluginV1Instruction, error) {
	obj := new(AddPluginV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddCollectionPluginV1Instruction struct {
	AddCollectionPluginV1Args AddCollectionPluginV1Args `json:"add_collection_plugin_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *AddCollectionPluginV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddCollectionPluginV1[:]
}

// UnmarshalWithDecoder unmarshals the AddCollectionPluginV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddCollectionPluginV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddCollectionPluginV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddCollectionPluginV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddCollectionPluginV1Instruction", Instruction_AddCollectionPluginV1[:], discriminator)
	}
	// Deserialize `AddCollectionPluginV1Args`:
	err = decoder.Decode(&obj.AddCollectionPluginV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddCollectionPluginV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddCollectionPluginV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddCollectionPluginV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the AddCollectionPluginV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddCollectionPluginV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddCollectionPluginV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddCollectionPluginV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddCollectionPluginV1Instruction(buf []byte) (*AddCollectionPluginV1Instruction, error) {
	obj := new(AddCollectionPluginV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemovePluginV1Instruction struct {
	RemovePluginV1Args RemovePluginV1Args `json:"remove_plugin_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *RemovePluginV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemovePluginV1[:]
}

// UnmarshalWithDecoder unmarshals the RemovePluginV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemovePluginV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemovePluginV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemovePluginV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemovePluginV1Instruction", Instruction_RemovePluginV1[:], discriminator)
	}
	// Deserialize `RemovePluginV1Args`:
	err = decoder.Decode(&obj.RemovePluginV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemovePluginV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemovePluginV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemovePluginV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the RemovePluginV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemovePluginV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemovePluginV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemovePluginV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemovePluginV1Instruction(buf []byte) (*RemovePluginV1Instruction, error) {
	obj := new(RemovePluginV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveCollectionPluginV1Instruction struct {
	RemoveCollectionPluginV1Args RemoveCollectionPluginV1Args `json:"remove_collection_plugin_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *RemoveCollectionPluginV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemoveCollectionPluginV1[:]
}

// UnmarshalWithDecoder unmarshals the RemoveCollectionPluginV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveCollectionPluginV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveCollectionPluginV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveCollectionPluginV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveCollectionPluginV1Instruction", Instruction_RemoveCollectionPluginV1[:], discriminator)
	}
	// Deserialize `RemoveCollectionPluginV1Args`:
	err = decoder.Decode(&obj.RemoveCollectionPluginV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemoveCollectionPluginV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveCollectionPluginV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveCollectionPluginV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the RemoveCollectionPluginV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveCollectionPluginV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveCollectionPluginV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveCollectionPluginV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveCollectionPluginV1Instruction(buf []byte) (*RemoveCollectionPluginV1Instruction, error) {
	obj := new(RemoveCollectionPluginV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdatePluginV1Instruction struct {
	UpdatePluginV1Args UpdatePluginV1Args `json:"update_plugin_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *UpdatePluginV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdatePluginV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdatePluginV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdatePluginV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdatePluginV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdatePluginV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdatePluginV1Instruction", Instruction_UpdatePluginV1[:], discriminator)
	}
	// Deserialize `UpdatePluginV1Args`:
	err = decoder.Decode(&obj.UpdatePluginV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdatePluginV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdatePluginV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdatePluginV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdatePluginV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdatePluginV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdatePluginV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdatePluginV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdatePluginV1Instruction(buf []byte) (*UpdatePluginV1Instruction, error) {
	obj := new(UpdatePluginV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateCollectionPluginV1Instruction struct {
	UpdateCollectionPluginV1Args UpdateCollectionPluginV1Args `json:"update_collection_plugin_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *UpdateCollectionPluginV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateCollectionPluginV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateCollectionPluginV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateCollectionPluginV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateCollectionPluginV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateCollectionPluginV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateCollectionPluginV1Instruction", Instruction_UpdateCollectionPluginV1[:], discriminator)
	}
	// Deserialize `UpdateCollectionPluginV1Args`:
	err = decoder.Decode(&obj.UpdateCollectionPluginV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateCollectionPluginV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateCollectionPluginV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateCollectionPluginV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdateCollectionPluginV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateCollectionPluginV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateCollectionPluginV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateCollectionPluginV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateCollectionPluginV1Instruction(buf []byte) (*UpdateCollectionPluginV1Instruction, error) {
	obj := new(UpdateCollectionPluginV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ApprovePluginAuthorityV1Instruction struct {
	ApprovePluginAuthorityV1Args ApprovePluginAuthorityV1Args `json:"approve_plugin_authority_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *ApprovePluginAuthorityV1Instruction) GetDiscriminator() []byte {
	return Instruction_ApprovePluginAuthorityV1[:]
}

// UnmarshalWithDecoder unmarshals the ApprovePluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ApprovePluginAuthorityV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ApprovePluginAuthorityV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ApprovePluginAuthorityV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ApprovePluginAuthorityV1Instruction", Instruction_ApprovePluginAuthorityV1[:], discriminator)
	}
	// Deserialize `ApprovePluginAuthorityV1Args`:
	err = decoder.Decode(&obj.ApprovePluginAuthorityV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *ApprovePluginAuthorityV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ApprovePluginAuthorityV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ApprovePluginAuthorityV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the ApprovePluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ApprovePluginAuthorityV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ApprovePluginAuthorityV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalApprovePluginAuthorityV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalApprovePluginAuthorityV1Instruction(buf []byte) (*ApprovePluginAuthorityV1Instruction, error) {
	obj := new(ApprovePluginAuthorityV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ApproveCollectionPluginAuthorityV1Instruction struct {
	ApproveCollectionPluginAuthorityV1Args ApproveCollectionPluginAuthorityV1Args `json:"approve_collection_plugin_authority_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *ApproveCollectionPluginAuthorityV1Instruction) GetDiscriminator() []byte {
	return Instruction_ApproveCollectionPluginAuthorityV1[:]
}

// UnmarshalWithDecoder unmarshals the ApproveCollectionPluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ApproveCollectionPluginAuthorityV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ApproveCollectionPluginAuthorityV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ApproveCollectionPluginAuthorityV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ApproveCollectionPluginAuthorityV1Instruction", Instruction_ApproveCollectionPluginAuthorityV1[:], discriminator)
	}
	// Deserialize `ApproveCollectionPluginAuthorityV1Args`:
	err = decoder.Decode(&obj.ApproveCollectionPluginAuthorityV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *ApproveCollectionPluginAuthorityV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ApproveCollectionPluginAuthorityV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ApproveCollectionPluginAuthorityV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the ApproveCollectionPluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ApproveCollectionPluginAuthorityV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ApproveCollectionPluginAuthorityV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalApproveCollectionPluginAuthorityV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalApproveCollectionPluginAuthorityV1Instruction(buf []byte) (*ApproveCollectionPluginAuthorityV1Instruction, error) {
	obj := new(ApproveCollectionPluginAuthorityV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RevokePluginAuthorityV1Instruction struct {
	RevokePluginAuthorityV1Args RevokePluginAuthorityV1Args `json:"revoke_plugin_authority_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *RevokePluginAuthorityV1Instruction) GetDiscriminator() []byte {
	return Instruction_RevokePluginAuthorityV1[:]
}

// UnmarshalWithDecoder unmarshals the RevokePluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RevokePluginAuthorityV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RevokePluginAuthorityV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RevokePluginAuthorityV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RevokePluginAuthorityV1Instruction", Instruction_RevokePluginAuthorityV1[:], discriminator)
	}
	// Deserialize `RevokePluginAuthorityV1Args`:
	err = decoder.Decode(&obj.RevokePluginAuthorityV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RevokePluginAuthorityV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RevokePluginAuthorityV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RevokePluginAuthorityV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the RevokePluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RevokePluginAuthorityV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RevokePluginAuthorityV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRevokePluginAuthorityV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRevokePluginAuthorityV1Instruction(buf []byte) (*RevokePluginAuthorityV1Instruction, error) {
	obj := new(RevokePluginAuthorityV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RevokeCollectionPluginAuthorityV1Instruction struct {
	RevokeCollectionPluginAuthorityV1Args RevokeCollectionPluginAuthorityV1Args `json:"revoke_collection_plugin_authority_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *RevokeCollectionPluginAuthorityV1Instruction) GetDiscriminator() []byte {
	return Instruction_RevokeCollectionPluginAuthorityV1[:]
}

// UnmarshalWithDecoder unmarshals the RevokeCollectionPluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RevokeCollectionPluginAuthorityV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RevokeCollectionPluginAuthorityV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RevokeCollectionPluginAuthorityV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RevokeCollectionPluginAuthorityV1Instruction", Instruction_RevokeCollectionPluginAuthorityV1[:], discriminator)
	}
	// Deserialize `RevokeCollectionPluginAuthorityV1Args`:
	err = decoder.Decode(&obj.RevokeCollectionPluginAuthorityV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RevokeCollectionPluginAuthorityV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RevokeCollectionPluginAuthorityV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RevokeCollectionPluginAuthorityV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the RevokeCollectionPluginAuthorityV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RevokeCollectionPluginAuthorityV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RevokeCollectionPluginAuthorityV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRevokeCollectionPluginAuthorityV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRevokeCollectionPluginAuthorityV1Instruction(buf []byte) (*RevokeCollectionPluginAuthorityV1Instruction, error) {
	obj := new(RevokeCollectionPluginAuthorityV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type BurnV1Instruction struct {
	BurnV1Args BurnV1Args `json:"burn_v1_args"`

	// Accounts:
	Asset                 solanago.PublicKey `json:"asset"`
	AssetWritable         bool               `json:"asset_writable"`
	Collection            solanago.PublicKey `json:"collection"`
	CollectionWritable    bool               `json:"collection_writable"`
	CollectionOptional    bool               `json:"collection_optional"`
	Payer                 solanago.PublicKey `json:"payer"`
	PayerWritable         bool               `json:"payer_writable"`
	PayerSigner           bool               `json:"payer_signer"`
	Authority             solanago.PublicKey `json:"authority"`
	AuthoritySigner       bool               `json:"authority_signer"`
	AuthorityOptional     bool               `json:"authority_optional"`
	SystemProgram         solanago.PublicKey `json:"system_program"`
	SystemProgramOptional bool               `json:"system_program_optional"`
	LogWrapper            solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional    bool               `json:"log_wrapper_optional"`
}

func (obj *BurnV1Instruction) GetDiscriminator() []byte {
	return Instruction_BurnV1[:]
}

// UnmarshalWithDecoder unmarshals the BurnV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *BurnV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "BurnV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_BurnV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "BurnV1Instruction", Instruction_BurnV1[:], discriminator)
	}
	// Deserialize `BurnV1Args`:
	err = decoder.Decode(&obj.BurnV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *BurnV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *BurnV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *BurnV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the BurnV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *BurnV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling BurnV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalBurnV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalBurnV1Instruction(buf []byte) (*BurnV1Instruction, error) {
	obj := new(BurnV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type BurnCollectionV1Instruction struct {
	BurnCollectionV1Args BurnCollectionV1Args `json:"burn_collection_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthorityWritable  bool               `json:"authority_writable"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *BurnCollectionV1Instruction) GetDiscriminator() []byte {
	return Instruction_BurnCollectionV1[:]
}

// UnmarshalWithDecoder unmarshals the BurnCollectionV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *BurnCollectionV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "BurnCollectionV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_BurnCollectionV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "BurnCollectionV1Instruction", Instruction_BurnCollectionV1[:], discriminator)
	}
	// Deserialize `BurnCollectionV1Args`:
	err = decoder.Decode(&obj.BurnCollectionV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *BurnCollectionV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *BurnCollectionV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *BurnCollectionV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the BurnCollectionV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *BurnCollectionV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling BurnCollectionV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalBurnCollectionV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalBurnCollectionV1Instruction(buf []byte) (*BurnCollectionV1Instruction, error) {
	obj := new(BurnCollectionV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type TransferV1Instruction struct {
	TransferV1Args TransferV1Args `json:"transfer_v1_args"`

	// Accounts:
	Asset                 solanago.PublicKey `json:"asset"`
	AssetWritable         bool               `json:"asset_writable"`
	Collection            solanago.PublicKey `json:"collection"`
	CollectionOptional    bool               `json:"collection_optional"`
	Payer                 solanago.PublicKey `json:"payer"`
	PayerWritable         bool               `json:"payer_writable"`
	PayerSigner           bool               `json:"payer_signer"`
	Authority             solanago.PublicKey `json:"authority"`
	AuthoritySigner       bool               `json:"authority_signer"`
	AuthorityOptional     bool               `json:"authority_optional"`
	NewOwner              solanago.PublicKey `json:"new_owner"`
	SystemProgram         solanago.PublicKey `json:"system_program"`
	SystemProgramOptional bool               `json:"system_program_optional"`
	LogWrapper            solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional    bool               `json:"log_wrapper_optional"`
}

func (obj *TransferV1Instruction) GetDiscriminator() []byte {
	return Instruction_TransferV1[:]
}

// UnmarshalWithDecoder unmarshals the TransferV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *TransferV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "TransferV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_TransferV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "TransferV1Instruction", Instruction_TransferV1[:], discriminator)
	}
	// Deserialize `TransferV1Args`:
	err = decoder.Decode(&obj.TransferV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *TransferV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from new_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_owner", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *TransferV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_owner", len(accountKeys)-1)
	}
	obj.NewOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *TransferV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.NewOwner)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the TransferV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *TransferV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling TransferV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalTransferV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalTransferV1Instruction(buf []byte) (*TransferV1Instruction, error) {
	obj := new(TransferV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateV1Instruction struct {
	UpdateV1Args UpdateV1Args `json:"update_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *UpdateV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateV1Instruction", Instruction_UpdateV1[:], discriminator)
	}
	// Deserialize `UpdateV1Args`:
	err = decoder.Decode(&obj.UpdateV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdateV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateV1Instruction(buf []byte) (*UpdateV1Instruction, error) {
	obj := new(UpdateV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateCollectionV1Instruction struct {
	UpdateCollectionV1Args UpdateCollectionV1Args `json:"update_collection_v1_args"`

	// Accounts:
	Collection                 solanago.PublicKey `json:"collection"`
	CollectionWritable         bool               `json:"collection_writable"`
	Payer                      solanago.PublicKey `json:"payer"`
	PayerWritable              bool               `json:"payer_writable"`
	PayerSigner                bool               `json:"payer_signer"`
	Authority                  solanago.PublicKey `json:"authority"`
	AuthoritySigner            bool               `json:"authority_signer"`
	AuthorityOptional          bool               `json:"authority_optional"`
	NewUpdateAuthority         solanago.PublicKey `json:"new_update_authority"`
	NewUpdateAuthorityOptional bool               `json:"new_update_authority_optional"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	LogWrapper                 solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional         bool               `json:"log_wrapper_optional"`
}

func (obj *UpdateCollectionV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateCollectionV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateCollectionV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateCollectionV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateCollectionV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateCollectionV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateCollectionV1Instruction", Instruction_UpdateCollectionV1[:], discriminator)
	}
	// Deserialize `UpdateCollectionV1Args`:
	err = decoder.Decode(&obj.UpdateCollectionV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateCollectionV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from new_update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateCollectionV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_update_authority", len(accountKeys)-1)
	}
	obj.NewUpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateCollectionV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.NewUpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdateCollectionV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateCollectionV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateCollectionV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateCollectionV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateCollectionV1Instruction(buf []byte) (*UpdateCollectionV1Instruction, error) {
	obj := new(UpdateCollectionV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CompressV1Instruction struct {
	CompressV1Args CompressV1Args `json:"compress_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *CompressV1Instruction) GetDiscriminator() []byte {
	return Instruction_CompressV1[:]
}

// UnmarshalWithDecoder unmarshals the CompressV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CompressV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CompressV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CompressV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CompressV1Instruction", Instruction_CompressV1[:], discriminator)
	}
	// Deserialize `CompressV1Args`:
	err = decoder.Decode(&obj.CompressV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CompressV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CompressV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CompressV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the CompressV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CompressV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CompressV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalCompressV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCompressV1Instruction(buf []byte) (*CompressV1Instruction, error) {
	obj := new(CompressV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DecompressV1Instruction struct {
	DecompressV1Args DecompressV1Args `json:"decompress_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *DecompressV1Instruction) GetDiscriminator() []byte {
	return Instruction_DecompressV1[:]
}

// UnmarshalWithDecoder unmarshals the DecompressV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DecompressV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DecompressV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DecompressV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DecompressV1Instruction", Instruction_DecompressV1[:], discriminator)
	}
	// Deserialize `DecompressV1Args`:
	err = decoder.Decode(&obj.DecompressV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *DecompressV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DecompressV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DecompressV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the DecompressV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DecompressV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DecompressV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalDecompressV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDecompressV1Instruction(buf []byte) (*DecompressV1Instruction, error) {
	obj := new(DecompressV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CollectInstruction struct {

	// Accounts:
	Recipient1         solanago.PublicKey `json:"recipient1"`
	Recipient1Writable bool               `json:"recipient1_writable"`
	Recipient2         solanago.PublicKey `json:"recipient2"`
	Recipient2Writable bool               `json:"recipient2_writable"`
}

func (obj *CollectInstruction) GetDiscriminator() []byte {
	return Instruction_Collect[:]
}

// UnmarshalWithDecoder unmarshals the CollectInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CollectInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CollectInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Collect[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CollectInstruction", Instruction_Collect[:], discriminator)
	}
	return nil
}

func (obj *CollectInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from recipient1 account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "recipient1", err)
	}
	indices = append(indices, index)
	// Decode from recipient2 account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "recipient2", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CollectInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set recipient1 account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "recipient1", len(accountKeys)-1)
	}
	obj.Recipient1 = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set recipient2 account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "recipient2", len(accountKeys)-1)
	}
	obj.Recipient2 = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CollectInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Recipient1)
	keys = append(keys, obj.Recipient2)
	return keys
}

// Unmarshal unmarshals the CollectInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CollectInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CollectInstruction: %w", err)
	}
	return nil
}

// UnmarshalCollectInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCollectInstruction(buf []byte) (*CollectInstruction, error) {
	obj := new(CollectInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateV2Instruction struct {
	CreateV2Args CreateV2Args `json:"create_v2_args"`

	// Accounts:
	Asset                   solanago.PublicKey `json:"asset"`
	AssetWritable           bool               `json:"asset_writable"`
	AssetSigner             bool               `json:"asset_signer"`
	Collection              solanago.PublicKey `json:"collection"`
	CollectionWritable      bool               `json:"collection_writable"`
	CollectionOptional      bool               `json:"collection_optional"`
	Authority               solanago.PublicKey `json:"authority"`
	AuthoritySigner         bool               `json:"authority_signer"`
	AuthorityOptional       bool               `json:"authority_optional"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerWritable           bool               `json:"payer_writable"`
	PayerSigner             bool               `json:"payer_signer"`
	Owner                   solanago.PublicKey `json:"owner"`
	OwnerOptional           bool               `json:"owner_optional"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	UpdateAuthorityOptional bool               `json:"update_authority_optional"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
	LogWrapper              solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional      bool               `json:"log_wrapper_optional"`
}

func (obj *CreateV2Instruction) GetDiscriminator() []byte {
	return Instruction_CreateV2[:]
}

// UnmarshalWithDecoder unmarshals the CreateV2Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateV2Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateV2Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateV2[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateV2Instruction", Instruction_CreateV2[:], discriminator)
	}
	// Deserialize `CreateV2Args`:
	err = decoder.Decode(&obj.CreateV2Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateV2Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateV2Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateV2Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the CreateV2Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateV2Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateV2Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateV2Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateV2Instruction(buf []byte) (*CreateV2Instruction, error) {
	obj := new(CreateV2Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateCollectionV2Instruction struct {
	CreateCollectionV2Args CreateCollectionV2Args `json:"create_collection_v2_args"`

	// Accounts:
	Collection              solanago.PublicKey `json:"collection"`
	CollectionWritable      bool               `json:"collection_writable"`
	CollectionSigner        bool               `json:"collection_signer"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	UpdateAuthorityOptional bool               `json:"update_authority_optional"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerWritable           bool               `json:"payer_writable"`
	PayerSigner             bool               `json:"payer_signer"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
}

func (obj *CreateCollectionV2Instruction) GetDiscriminator() []byte {
	return Instruction_CreateCollectionV2[:]
}

// UnmarshalWithDecoder unmarshals the CreateCollectionV2Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateCollectionV2Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateCollectionV2Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateCollectionV2[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateCollectionV2Instruction", Instruction_CreateCollectionV2[:], discriminator)
	}
	// Deserialize `CreateCollectionV2Args`:
	err = decoder.Decode(&obj.CreateCollectionV2Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateCollectionV2Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateCollectionV2Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateCollectionV2Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the CreateCollectionV2Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateCollectionV2Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateCollectionV2Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateCollectionV2Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateCollectionV2Instruction(buf []byte) (*CreateCollectionV2Instruction, error) {
	obj := new(CreateCollectionV2Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddExternalPluginAdapterV1Instruction struct {
	AddExternalPluginAdapterV1Args AddExternalPluginAdapterV1Args `json:"add_external_plugin_adapter_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *AddExternalPluginAdapterV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddExternalPluginAdapterV1[:]
}

// UnmarshalWithDecoder unmarshals the AddExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddExternalPluginAdapterV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddExternalPluginAdapterV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddExternalPluginAdapterV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddExternalPluginAdapterV1Instruction", Instruction_AddExternalPluginAdapterV1[:], discriminator)
	}
	// Deserialize `AddExternalPluginAdapterV1Args`:
	err = decoder.Decode(&obj.AddExternalPluginAdapterV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddExternalPluginAdapterV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddExternalPluginAdapterV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddExternalPluginAdapterV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the AddExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddExternalPluginAdapterV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddExternalPluginAdapterV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddExternalPluginAdapterV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddExternalPluginAdapterV1Instruction(buf []byte) (*AddExternalPluginAdapterV1Instruction, error) {
	obj := new(AddExternalPluginAdapterV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddCollectionExternalPluginAdapterV1Instruction struct {
	AddCollectionExternalPluginAdapterV1Args AddCollectionExternalPluginAdapterV1Args `json:"add_collection_external_plugin_adapter_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *AddCollectionExternalPluginAdapterV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddCollectionExternalPluginAdapterV1[:]
}

// UnmarshalWithDecoder unmarshals the AddCollectionExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddCollectionExternalPluginAdapterV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddCollectionExternalPluginAdapterV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddCollectionExternalPluginAdapterV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddCollectionExternalPluginAdapterV1Instruction", Instruction_AddCollectionExternalPluginAdapterV1[:], discriminator)
	}
	// Deserialize `AddCollectionExternalPluginAdapterV1Args`:
	err = decoder.Decode(&obj.AddCollectionExternalPluginAdapterV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddCollectionExternalPluginAdapterV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddCollectionExternalPluginAdapterV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddCollectionExternalPluginAdapterV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the AddCollectionExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddCollectionExternalPluginAdapterV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddCollectionExternalPluginAdapterV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddCollectionExternalPluginAdapterV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddCollectionExternalPluginAdapterV1Instruction(buf []byte) (*AddCollectionExternalPluginAdapterV1Instruction, error) {
	obj := new(AddCollectionExternalPluginAdapterV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveExternalPluginAdapterV1Instruction struct {
	RemoveExternalPluginAdapterV1Args RemoveExternalPluginAdapterV1Args `json:"remove_external_plugin_adapter_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *RemoveExternalPluginAdapterV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemoveExternalPluginAdapterV1[:]
}

// UnmarshalWithDecoder unmarshals the RemoveExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveExternalPluginAdapterV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveExternalPluginAdapterV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveExternalPluginAdapterV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveExternalPluginAdapterV1Instruction", Instruction_RemoveExternalPluginAdapterV1[:], discriminator)
	}
	// Deserialize `RemoveExternalPluginAdapterV1Args`:
	err = decoder.Decode(&obj.RemoveExternalPluginAdapterV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemoveExternalPluginAdapterV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveExternalPluginAdapterV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveExternalPluginAdapterV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the RemoveExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveExternalPluginAdapterV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveExternalPluginAdapterV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveExternalPluginAdapterV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveExternalPluginAdapterV1Instruction(buf []byte) (*RemoveExternalPluginAdapterV1Instruction, error) {
	obj := new(RemoveExternalPluginAdapterV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveCollectionExternalPluginAdapterV1Instruction struct {
	RemoveCollectionExternalPluginAdapterV1Args RemoveCollectionExternalPluginAdapterV1Args `json:"remove_collection_external_plugin_adapter_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *RemoveCollectionExternalPluginAdapterV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemoveCollectionExternalPluginAdapterV1[:]
}

// UnmarshalWithDecoder unmarshals the RemoveCollectionExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveCollectionExternalPluginAdapterV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveCollectionExternalPluginAdapterV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveCollectionExternalPluginAdapterV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveCollectionExternalPluginAdapterV1Instruction", Instruction_RemoveCollectionExternalPluginAdapterV1[:], discriminator)
	}
	// Deserialize `RemoveCollectionExternalPluginAdapterV1Args`:
	err = decoder.Decode(&obj.RemoveCollectionExternalPluginAdapterV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemoveCollectionExternalPluginAdapterV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveCollectionExternalPluginAdapterV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveCollectionExternalPluginAdapterV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the RemoveCollectionExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveCollectionExternalPluginAdapterV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveCollectionExternalPluginAdapterV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveCollectionExternalPluginAdapterV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveCollectionExternalPluginAdapterV1Instruction(buf []byte) (*RemoveCollectionExternalPluginAdapterV1Instruction, error) {
	obj := new(RemoveCollectionExternalPluginAdapterV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateExternalPluginAdapterV1Instruction struct {
	UpdateExternalPluginAdapterV1Args UpdateExternalPluginAdapterV1Args `json:"update_external_plugin_adapter_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *UpdateExternalPluginAdapterV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateExternalPluginAdapterV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateExternalPluginAdapterV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateExternalPluginAdapterV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateExternalPluginAdapterV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateExternalPluginAdapterV1Instruction", Instruction_UpdateExternalPluginAdapterV1[:], discriminator)
	}
	// Deserialize `UpdateExternalPluginAdapterV1Args`:
	err = decoder.Decode(&obj.UpdateExternalPluginAdapterV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateExternalPluginAdapterV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateExternalPluginAdapterV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateExternalPluginAdapterV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdateExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateExternalPluginAdapterV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateExternalPluginAdapterV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateExternalPluginAdapterV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateExternalPluginAdapterV1Instruction(buf []byte) (*UpdateExternalPluginAdapterV1Instruction, error) {
	obj := new(UpdateExternalPluginAdapterV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateCollectionExternalPluginAdapterV1Instruction struct {
	UpdateCollectionExternalPluginAdapterV1Args UpdateCollectionExternalPluginAdapterV1Args `json:"update_collection_external_plugin_adapter_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *UpdateCollectionExternalPluginAdapterV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateCollectionExternalPluginAdapterV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateCollectionExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateCollectionExternalPluginAdapterV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateCollectionExternalPluginAdapterV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateCollectionExternalPluginAdapterV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateCollectionExternalPluginAdapterV1Instruction", Instruction_UpdateCollectionExternalPluginAdapterV1[:], discriminator)
	}
	// Deserialize `UpdateCollectionExternalPluginAdapterV1Args`:
	err = decoder.Decode(&obj.UpdateCollectionExternalPluginAdapterV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateCollectionExternalPluginAdapterV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateCollectionExternalPluginAdapterV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateCollectionExternalPluginAdapterV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdateCollectionExternalPluginAdapterV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateCollectionExternalPluginAdapterV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateCollectionExternalPluginAdapterV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateCollectionExternalPluginAdapterV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateCollectionExternalPluginAdapterV1Instruction(buf []byte) (*UpdateCollectionExternalPluginAdapterV1Instruction, error) {
	obj := new(UpdateCollectionExternalPluginAdapterV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type WriteExternalPluginAdapterDataV1Instruction struct {
	WriteExternalPluginAdapterDataV1Args WriteExternalPluginAdapterDataV1Args `json:"write_external_plugin_adapter_data_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	Buffer             solanago.PublicKey `json:"buffer"`
	BufferOptional     bool               `json:"buffer_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *WriteExternalPluginAdapterDataV1Instruction) GetDiscriminator() []byte {
	return Instruction_WriteExternalPluginAdapterDataV1[:]
}

// UnmarshalWithDecoder unmarshals the WriteExternalPluginAdapterDataV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *WriteExternalPluginAdapterDataV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "WriteExternalPluginAdapterDataV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_WriteExternalPluginAdapterDataV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "WriteExternalPluginAdapterDataV1Instruction", Instruction_WriteExternalPluginAdapterDataV1[:], discriminator)
	}
	// Deserialize `WriteExternalPluginAdapterDataV1Args`:
	err = decoder.Decode(&obj.WriteExternalPluginAdapterDataV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *WriteExternalPluginAdapterDataV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from buffer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "buffer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *WriteExternalPluginAdapterDataV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set buffer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "buffer", len(accountKeys)-1)
	}
	obj.Buffer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *WriteExternalPluginAdapterDataV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Buffer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the WriteExternalPluginAdapterDataV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *WriteExternalPluginAdapterDataV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling WriteExternalPluginAdapterDataV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalWriteExternalPluginAdapterDataV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalWriteExternalPluginAdapterDataV1Instruction(buf []byte) (*WriteExternalPluginAdapterDataV1Instruction, error) {
	obj := new(WriteExternalPluginAdapterDataV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type WriteCollectionExternalPluginAdapterDataV1Instruction struct {
	WriteCollectionExternalPluginAdapterDataV1Args WriteCollectionExternalPluginAdapterDataV1Args `json:"write_collection_external_plugin_adapter_data_v1_args"`

	// Accounts:
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	Buffer             solanago.PublicKey `json:"buffer"`
	BufferOptional     bool               `json:"buffer_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	LogWrapper         solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional bool               `json:"log_wrapper_optional"`
}

func (obj *WriteCollectionExternalPluginAdapterDataV1Instruction) GetDiscriminator() []byte {
	return Instruction_WriteCollectionExternalPluginAdapterDataV1[:]
}

// UnmarshalWithDecoder unmarshals the WriteCollectionExternalPluginAdapterDataV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *WriteCollectionExternalPluginAdapterDataV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "WriteCollectionExternalPluginAdapterDataV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_WriteCollectionExternalPluginAdapterDataV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "WriteCollectionExternalPluginAdapterDataV1Instruction", Instruction_WriteCollectionExternalPluginAdapterDataV1[:], discriminator)
	}
	// Deserialize `WriteCollectionExternalPluginAdapterDataV1Args`:
	err = decoder.Decode(&obj.WriteCollectionExternalPluginAdapterDataV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *WriteCollectionExternalPluginAdapterDataV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from buffer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "buffer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *WriteCollectionExternalPluginAdapterDataV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set buffer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "buffer", len(accountKeys)-1)
	}
	obj.Buffer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *WriteCollectionExternalPluginAdapterDataV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Buffer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the WriteCollectionExternalPluginAdapterDataV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *WriteCollectionExternalPluginAdapterDataV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling WriteCollectionExternalPluginAdapterDataV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalWriteCollectionExternalPluginAdapterDataV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalWriteCollectionExternalPluginAdapterDataV1Instruction(buf []byte) (*WriteCollectionExternalPluginAdapterDataV1Instruction, error) {
	obj := new(WriteCollectionExternalPluginAdapterDataV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateV2Instruction struct {
	UpdateV2Args UpdateV2Args `json:"update_v2_args"`

	// Accounts:
	Asset                 solanago.PublicKey `json:"asset"`
	AssetWritable         bool               `json:"asset_writable"`
	Collection            solanago.PublicKey `json:"collection"`
	CollectionWritable    bool               `json:"collection_writable"`
	CollectionOptional    bool               `json:"collection_optional"`
	Payer                 solanago.PublicKey `json:"payer"`
	PayerWritable         bool               `json:"payer_writable"`
	PayerSigner           bool               `json:"payer_signer"`
	Authority             solanago.PublicKey `json:"authority"`
	AuthoritySigner       bool               `json:"authority_signer"`
	AuthorityOptional     bool               `json:"authority_optional"`
	NewCollection         solanago.PublicKey `json:"new_collection"`
	NewCollectionWritable bool               `json:"new_collection_writable"`
	NewCollectionOptional bool               `json:"new_collection_optional"`
	SystemProgram         solanago.PublicKey `json:"system_program"`
	LogWrapper            solanago.PublicKey `json:"log_wrapper"`
	LogWrapperOptional    bool               `json:"log_wrapper_optional"`
}

func (obj *UpdateV2Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateV2[:]
}

// UnmarshalWithDecoder unmarshals the UpdateV2Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateV2Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateV2Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateV2[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateV2Instruction", Instruction_UpdateV2[:], discriminator)
	}
	// Deserialize `UpdateV2Args`:
	err = decoder.Decode(&obj.UpdateV2Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateV2Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from new_collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_collection", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from log_wrapper account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "log_wrapper", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateV2Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_collection", len(accountKeys)-1)
	}
	obj.NewCollection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set log_wrapper account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "log_wrapper", len(accountKeys)-1)
	}
	obj.LogWrapper = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateV2Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.NewCollection)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.LogWrapper)
	return keys
}

// Unmarshal unmarshals the UpdateV2Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateV2Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateV2Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateV2Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateV2Instruction(buf []byte) (*UpdateV2Instruction, error) {
	obj := new(UpdateV2Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ExecuteV1Instruction struct {
	ExecuteV1Args ExecuteV1Args `json:"execute_v1_args"`

	// Accounts:
	Asset              solanago.PublicKey `json:"asset"`
	AssetWritable      bool               `json:"asset_writable"`
	Collection         solanago.PublicKey `json:"collection"`
	CollectionWritable bool               `json:"collection_writable"`
	CollectionOptional bool               `json:"collection_optional"`
	AssetSigner        solanago.PublicKey `json:"asset_signer"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	ProgramId          solanago.PublicKey `json:"program_id"`
}

func (obj *ExecuteV1Instruction) GetDiscriminator() []byte {
	return Instruction_ExecuteV1[:]
}

// UnmarshalWithDecoder unmarshals the ExecuteV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ExecuteV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ExecuteV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ExecuteV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ExecuteV1Instruction", Instruction_ExecuteV1[:], discriminator)
	}
	// Deserialize `ExecuteV1Args`:
	err = decoder.Decode(&obj.ExecuteV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *ExecuteV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from asset account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from asset_signer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "asset_signer", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from program_id account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "program_id", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ExecuteV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set asset account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset", len(accountKeys)-1)
	}
	obj.Asset = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set asset_signer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "asset_signer", len(accountKeys)-1)
	}
	obj.AssetSigner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set program_id account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "program_id", len(accountKeys)-1)
	}
	obj.ProgramId = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ExecuteV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Asset)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.AssetSigner)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.ProgramId)
	return keys
}

// Unmarshal unmarshals the ExecuteV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ExecuteV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ExecuteV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalExecuteV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalExecuteV1Instruction(buf []byte) (*ExecuteV1Instruction, error) {
	obj := new(ExecuteV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateCollectionInfoV1Instruction struct {
	UpdateCollectionInfoV1Args UpdateCollectionInfoV1Args `json:"update_collection_info_v1_args"`

	// Accounts:
	Collection            solanago.PublicKey `json:"collection"`
	CollectionWritable    bool               `json:"collection_writable"`
	BubblegumSigner       solanago.PublicKey `json:"bubblegum_signer"`
	BubblegumSignerSigner bool               `json:"bubblegum_signer_signer"`
}

func (obj *UpdateCollectionInfoV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateCollectionInfoV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateCollectionInfoV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateCollectionInfoV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateCollectionInfoV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateCollectionInfoV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateCollectionInfoV1Instruction", Instruction_UpdateCollectionInfoV1[:], discriminator)
	}
	// Deserialize `UpdateCollectionInfoV1Args`:
	err = decoder.Decode(&obj.UpdateCollectionInfoV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateCollectionInfoV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from bubblegum_signer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "bubblegum_signer", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateCollectionInfoV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set bubblegum_signer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "bubblegum_signer", len(accountKeys)-1)
	}
	obj.BubblegumSigner = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateCollectionInfoV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.BubblegumSigner)
	return keys
}

// Unmarshal unmarshals the UpdateCollectionInfoV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateCollectionInfoV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateCollectionInfoV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateCollectionInfoV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateCollectionInfoV1Instruction(buf []byte) (*UpdateCollectionInfoV1Instruction, error) {
	obj := new(UpdateCollectionInfoV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddCollectionsToGroupV1Instruction struct {
	AddCollectionsToGroupV1Args AddCollectionsToGroupV1Args `json:"add_collections_to_group_v1_args"`

	// Accounts:
	Group             solanago.PublicKey `json:"group"`
	GroupWritable     bool               `json:"group_writable"`
	Payer             solanago.PublicKey `json:"payer"`
	PayerWritable     bool               `json:"payer_writable"`
	PayerSigner       bool               `json:"payer_signer"`
	Authority         solanago.PublicKey `json:"authority"`
	AuthoritySigner   bool               `json:"authority_signer"`
	AuthorityOptional bool               `json:"authority_optional"`
	SystemProgram     solanago.PublicKey `json:"system_program"`
}

func (obj *AddCollectionsToGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddCollectionsToGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the AddCollectionsToGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddCollectionsToGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddCollectionsToGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddCollectionsToGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddCollectionsToGroupV1Instruction", Instruction_AddCollectionsToGroupV1[:], discriminator)
	}
	// Deserialize `AddCollectionsToGroupV1Args`:
	err = decoder.Decode(&obj.AddCollectionsToGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddCollectionsToGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddCollectionsToGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddCollectionsToGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the AddCollectionsToGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddCollectionsToGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddCollectionsToGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddCollectionsToGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddCollectionsToGroupV1Instruction(buf []byte) (*AddCollectionsToGroupV1Instruction, error) {
	obj := new(AddCollectionsToGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveCollectionsFromGroupV1Instruction struct {
	RemoveCollectionsFromGroupV1Args RemoveCollectionsFromGroupV1Args `json:"remove_collections_from_group_v1_args"`

	// Accounts:
	Group             solanago.PublicKey `json:"group"`
	GroupWritable     bool               `json:"group_writable"`
	Payer             solanago.PublicKey `json:"payer"`
	PayerWritable     bool               `json:"payer_writable"`
	PayerSigner       bool               `json:"payer_signer"`
	Authority         solanago.PublicKey `json:"authority"`
	AuthoritySigner   bool               `json:"authority_signer"`
	AuthorityOptional bool               `json:"authority_optional"`
	SystemProgram     solanago.PublicKey `json:"system_program"`
}

func (obj *RemoveCollectionsFromGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemoveCollectionsFromGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the RemoveCollectionsFromGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveCollectionsFromGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveCollectionsFromGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveCollectionsFromGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveCollectionsFromGroupV1Instruction", Instruction_RemoveCollectionsFromGroupV1[:], discriminator)
	}
	// Deserialize `RemoveCollectionsFromGroupV1Args`:
	err = decoder.Decode(&obj.RemoveCollectionsFromGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemoveCollectionsFromGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveCollectionsFromGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveCollectionsFromGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the RemoveCollectionsFromGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveCollectionsFromGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveCollectionsFromGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveCollectionsFromGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveCollectionsFromGroupV1Instruction(buf []byte) (*RemoveCollectionsFromGroupV1Instruction, error) {
	obj := new(RemoveCollectionsFromGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddAssetsToGroupV1Instruction struct {
	AddAssetsToGroupV1Args AddAssetsToGroupV1Args `json:"add_assets_to_group_v1_args"`

	// Accounts:
	Group             solanago.PublicKey `json:"group"`
	GroupWritable     bool               `json:"group_writable"`
	Payer             solanago.PublicKey `json:"payer"`
	PayerWritable     bool               `json:"payer_writable"`
	PayerSigner       bool               `json:"payer_signer"`
	Authority         solanago.PublicKey `json:"authority"`
	AuthoritySigner   bool               `json:"authority_signer"`
	AuthorityOptional bool               `json:"authority_optional"`
	SystemProgram     solanago.PublicKey `json:"system_program"`
}

func (obj *AddAssetsToGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddAssetsToGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the AddAssetsToGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddAssetsToGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddAssetsToGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddAssetsToGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddAssetsToGroupV1Instruction", Instruction_AddAssetsToGroupV1[:], discriminator)
	}
	// Deserialize `AddAssetsToGroupV1Args`:
	err = decoder.Decode(&obj.AddAssetsToGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddAssetsToGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddAssetsToGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddAssetsToGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the AddAssetsToGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddAssetsToGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddAssetsToGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddAssetsToGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddAssetsToGroupV1Instruction(buf []byte) (*AddAssetsToGroupV1Instruction, error) {
	obj := new(AddAssetsToGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveAssetsFromGroupV1Instruction struct {
	RemoveAssetsFromGroupV1Args RemoveAssetsFromGroupV1Args `json:"remove_assets_from_group_v1_args"`

	// Accounts:
	Group             solanago.PublicKey `json:"group"`
	GroupWritable     bool               `json:"group_writable"`
	Payer             solanago.PublicKey `json:"payer"`
	PayerWritable     bool               `json:"payer_writable"`
	PayerSigner       bool               `json:"payer_signer"`
	Authority         solanago.PublicKey `json:"authority"`
	AuthoritySigner   bool               `json:"authority_signer"`
	AuthorityOptional bool               `json:"authority_optional"`
	SystemProgram     solanago.PublicKey `json:"system_program"`
}

func (obj *RemoveAssetsFromGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemoveAssetsFromGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the RemoveAssetsFromGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveAssetsFromGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveAssetsFromGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveAssetsFromGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveAssetsFromGroupV1Instruction", Instruction_RemoveAssetsFromGroupV1[:], discriminator)
	}
	// Deserialize `RemoveAssetsFromGroupV1Args`:
	err = decoder.Decode(&obj.RemoveAssetsFromGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemoveAssetsFromGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveAssetsFromGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveAssetsFromGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the RemoveAssetsFromGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveAssetsFromGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveAssetsFromGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveAssetsFromGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveAssetsFromGroupV1Instruction(buf []byte) (*RemoveAssetsFromGroupV1Instruction, error) {
	obj := new(RemoveAssetsFromGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type AddGroupsToGroupV1Instruction struct {
	AddGroupsToGroupV1Args AddGroupsToGroupV1Args `json:"add_groups_to_group_v1_args"`

	// Accounts:
	ParentGroup         solanago.PublicKey `json:"parent_group"`
	ParentGroupWritable bool               `json:"parent_group_writable"`
	Payer               solanago.PublicKey `json:"payer"`
	PayerWritable       bool               `json:"payer_writable"`
	PayerSigner         bool               `json:"payer_signer"`
	Authority           solanago.PublicKey `json:"authority"`
	AuthoritySigner     bool               `json:"authority_signer"`
	AuthorityOptional   bool               `json:"authority_optional"`
	SystemProgram       solanago.PublicKey `json:"system_program"`
}

func (obj *AddGroupsToGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_AddGroupsToGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the AddGroupsToGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *AddGroupsToGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "AddGroupsToGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_AddGroupsToGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "AddGroupsToGroupV1Instruction", Instruction_AddGroupsToGroupV1[:], discriminator)
	}
	// Deserialize `AddGroupsToGroupV1Args`:
	err = decoder.Decode(&obj.AddGroupsToGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *AddGroupsToGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from parent_group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "parent_group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *AddGroupsToGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set parent_group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "parent_group", len(accountKeys)-1)
	}
	obj.ParentGroup = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *AddGroupsToGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.ParentGroup)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the AddGroupsToGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *AddGroupsToGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling AddGroupsToGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalAddGroupsToGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalAddGroupsToGroupV1Instruction(buf []byte) (*AddGroupsToGroupV1Instruction, error) {
	obj := new(AddGroupsToGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveGroupsFromGroupV1Instruction struct {
	RemoveGroupsFromGroupV1Args RemoveGroupsFromGroupV1Args `json:"remove_groups_from_group_v1_args"`

	// Accounts:
	ParentGroup         solanago.PublicKey `json:"parent_group"`
	ParentGroupWritable bool               `json:"parent_group_writable"`
	Payer               solanago.PublicKey `json:"payer"`
	PayerWritable       bool               `json:"payer_writable"`
	PayerSigner         bool               `json:"payer_signer"`
	Authority           solanago.PublicKey `json:"authority"`
	AuthoritySigner     bool               `json:"authority_signer"`
	AuthorityOptional   bool               `json:"authority_optional"`
	SystemProgram       solanago.PublicKey `json:"system_program"`
}

func (obj *RemoveGroupsFromGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_RemoveGroupsFromGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the RemoveGroupsFromGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveGroupsFromGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveGroupsFromGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveGroupsFromGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveGroupsFromGroupV1Instruction", Instruction_RemoveGroupsFromGroupV1[:], discriminator)
	}
	// Deserialize `RemoveGroupsFromGroupV1Args`:
	err = decoder.Decode(&obj.RemoveGroupsFromGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RemoveGroupsFromGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from parent_group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "parent_group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveGroupsFromGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set parent_group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "parent_group", len(accountKeys)-1)
	}
	obj.ParentGroup = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveGroupsFromGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.ParentGroup)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the RemoveGroupsFromGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveGroupsFromGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveGroupsFromGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveGroupsFromGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveGroupsFromGroupV1Instruction(buf []byte) (*RemoveGroupsFromGroupV1Instruction, error) {
	obj := new(RemoveGroupsFromGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateGroupV1Instruction struct {
	CreateGroupV1Args CreateGroupV1Args `json:"create_group_v1_args"`

	// Accounts:
	Group                   solanago.PublicKey `json:"group"`
	GroupWritable           bool               `json:"group_writable"`
	GroupSigner             bool               `json:"group_signer"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner   bool               `json:"update_authority_signer"`
	UpdateAuthorityOptional bool               `json:"update_authority_optional"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerWritable           bool               `json:"payer_writable"`
	PayerSigner             bool               `json:"payer_signer"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
}

func (obj *CreateGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_CreateGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the CreateGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateGroupV1Instruction", Instruction_CreateGroupV1[:], discriminator)
	}
	// Deserialize `CreateGroupV1Args`:
	err = decoder.Decode(&obj.CreateGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the CreateGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateGroupV1Instruction(buf []byte) (*CreateGroupV1Instruction, error) {
	obj := new(CreateGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CloseGroupV1Instruction struct {
	CloseGroupV1Args CloseGroupV1Args `json:"close_group_v1_args"`

	// Accounts:
	Group             solanago.PublicKey `json:"group"`
	GroupWritable     bool               `json:"group_writable"`
	Payer             solanago.PublicKey `json:"payer"`
	PayerWritable     bool               `json:"payer_writable"`
	PayerSigner       bool               `json:"payer_signer"`
	Authority         solanago.PublicKey `json:"authority"`
	AuthoritySigner   bool               `json:"authority_signer"`
	AuthorityOptional bool               `json:"authority_optional"`
}

func (obj *CloseGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_CloseGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the CloseGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CloseGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CloseGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CloseGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CloseGroupV1Instruction", Instruction_CloseGroupV1[:], discriminator)
	}
	// Deserialize `CloseGroupV1Args`:
	err = decoder.Decode(&obj.CloseGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CloseGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CloseGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 3 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 3, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CloseGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	return keys
}

// Unmarshal unmarshals the CloseGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CloseGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CloseGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalCloseGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCloseGroupV1Instruction(buf []byte) (*CloseGroupV1Instruction, error) {
	obj := new(CloseGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateGroupV1Instruction struct {
	UpdateGroupV1Args UpdateGroupV1Args `json:"update_group_v1_args"`

	// Accounts:
	Group                      solanago.PublicKey `json:"group"`
	GroupWritable              bool               `json:"group_writable"`
	Payer                      solanago.PublicKey `json:"payer"`
	PayerWritable              bool               `json:"payer_writable"`
	PayerSigner                bool               `json:"payer_signer"`
	Authority                  solanago.PublicKey `json:"authority"`
	AuthoritySigner            bool               `json:"authority_signer"`
	AuthorityOptional          bool               `json:"authority_optional"`
	NewUpdateAuthority         solanago.PublicKey `json:"new_update_authority"`
	NewUpdateAuthorityOptional bool               `json:"new_update_authority_optional"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
}

func (obj *UpdateGroupV1Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateGroupV1[:]
}

// UnmarshalWithDecoder unmarshals the UpdateGroupV1Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateGroupV1Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateGroupV1Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateGroupV1[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateGroupV1Instruction", Instruction_UpdateGroupV1[:], discriminator)
	}
	// Deserialize `UpdateGroupV1Args`:
	err = decoder.Decode(&obj.UpdateGroupV1Args)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateGroupV1Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from group account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "group", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from new_update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateGroupV1Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set group account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "group", len(accountKeys)-1)
	}
	obj.Group = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_update_authority", len(accountKeys)-1)
	}
	obj.NewUpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateGroupV1Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Group)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.NewUpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the UpdateGroupV1Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateGroupV1Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateGroupV1Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateGroupV1Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateGroupV1Instruction(buf []byte) (*UpdateGroupV1Instruction, error) {
	obj := new(UpdateGroupV1Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

// Instruction interface defines common methods for all instruction types
type Instruction interface {
	GetDiscriminator() []byte

	UnmarshalWithDecoder(decoder *binary.Decoder) error

	UnmarshalAccountIndices(buf []byte) ([]uint8, error)

	PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error

	GetAccountKeys() []solanago.PublicKey
}

// ParseInstruction parses instruction data and optionally populates accounts
// If accountIndicesData is nil or empty, accounts will not be populated
func ParseInstruction(instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (Instruction, error) {
	// Validate inputs
	if len(instructionData) < 1 {
		return nil, fmt.Errorf("instruction data too short: expected at least 1 bytes, got %d", len(instructionData))
	}
	// Extract discriminator
	discriminator := [1]byte{}
	copy(discriminator[:], instructionData[0:1])
	// Parse based on discriminator
	switch discriminator {
	case Instruction_CreateV1:
		instruction := new(CreateV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateCollectionV1:
		instruction := new(CreateCollectionV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateCollectionV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddPluginV1:
		instruction := new(AddPluginV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddPluginV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddCollectionPluginV1:
		instruction := new(AddCollectionPluginV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddCollectionPluginV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemovePluginV1:
		instruction := new(RemovePluginV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemovePluginV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveCollectionPluginV1:
		instruction := new(RemoveCollectionPluginV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveCollectionPluginV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdatePluginV1:
		instruction := new(UpdatePluginV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdatePluginV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateCollectionPluginV1:
		instruction := new(UpdateCollectionPluginV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateCollectionPluginV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ApprovePluginAuthorityV1:
		instruction := new(ApprovePluginAuthorityV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ApprovePluginAuthorityV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ApproveCollectionPluginAuthorityV1:
		instruction := new(ApproveCollectionPluginAuthorityV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ApproveCollectionPluginAuthorityV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RevokePluginAuthorityV1:
		instruction := new(RevokePluginAuthorityV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RevokePluginAuthorityV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RevokeCollectionPluginAuthorityV1:
		instruction := new(RevokeCollectionPluginAuthorityV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RevokeCollectionPluginAuthorityV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_BurnV1:
		instruction := new(BurnV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as BurnV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_BurnCollectionV1:
		instruction := new(BurnCollectionV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as BurnCollectionV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_TransferV1:
		instruction := new(TransferV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as TransferV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateV1:
		instruction := new(UpdateV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateCollectionV1:
		instruction := new(UpdateCollectionV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateCollectionV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CompressV1:
		instruction := new(CompressV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CompressV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DecompressV1:
		instruction := new(DecompressV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DecompressV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Collect:
		instruction := new(CollectInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CollectInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateV2:
		instruction := new(CreateV2Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateV2Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateCollectionV2:
		instruction := new(CreateCollectionV2Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateCollectionV2Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddExternalPluginAdapterV1:
		instruction := new(AddExternalPluginAdapterV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddExternalPluginAdapterV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddCollectionExternalPluginAdapterV1:
		instruction := new(AddCollectionExternalPluginAdapterV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddCollectionExternalPluginAdapterV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveExternalPluginAdapterV1:
		instruction := new(RemoveExternalPluginAdapterV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveExternalPluginAdapterV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveCollectionExternalPluginAdapterV1:
		instruction := new(RemoveCollectionExternalPluginAdapterV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveCollectionExternalPluginAdapterV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateExternalPluginAdapterV1:
		instruction := new(UpdateExternalPluginAdapterV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateExternalPluginAdapterV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateCollectionExternalPluginAdapterV1:
		instruction := new(UpdateCollectionExternalPluginAdapterV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateCollectionExternalPluginAdapterV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_WriteExternalPluginAdapterDataV1:
		instruction := new(WriteExternalPluginAdapterDataV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as WriteExternalPluginAdapterDataV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_WriteCollectionExternalPluginAdapterDataV1:
		instruction := new(WriteCollectionExternalPluginAdapterDataV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as WriteCollectionExternalPluginAdapterDataV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateV2:
		instruction := new(UpdateV2Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateV2Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ExecuteV1:
		instruction := new(ExecuteV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ExecuteV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateCollectionInfoV1:
		instruction := new(UpdateCollectionInfoV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateCollectionInfoV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddCollectionsToGroupV1:
		instruction := new(AddCollectionsToGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddCollectionsToGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveCollectionsFromGroupV1:
		instruction := new(RemoveCollectionsFromGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveCollectionsFromGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddAssetsToGroupV1:
		instruction := new(AddAssetsToGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddAssetsToGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveAssetsFromGroupV1:
		instruction := new(RemoveAssetsFromGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveAssetsFromGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_AddGroupsToGroupV1:
		instruction := new(AddGroupsToGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as AddGroupsToGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveGroupsFromGroupV1:
		instruction := new(RemoveGroupsFromGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveGroupsFromGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateGroupV1:
		instruction := new(CreateGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CloseGroupV1:
		instruction := new(CloseGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CloseGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateGroupV1:
		instruction := new(UpdateGroupV1Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateGroupV1Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	default:
		return nil, fmt.Errorf("unknown instruction discriminator: %v", discriminator[:])
	}
}

// ParseInstructionTyped parses instruction data and returns a specific instruction type // T must implement the Instruction interface
func ParseInstructionTyped[T Instruction](instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (T, error) {
	instruction, err := ParseInstruction(instructionData, accountIndicesData, accountKeys)
	if err != nil {
		return *new(T), err
	}
	typed, ok := instruction.(T)
	if !ok {
		return *new(T), fmt.Errorf("instruction is not of expected type")
	}
	return typed, nil
}

// ParseInstructionWithoutAccounts parses instruction data without account information
func ParseInstructionWithoutAccounts(instructionData []byte) (Instruction, error) {
	return ParseInstruction(instructionData, nil, []solanago.PublicKey{})
}

// ParseInstructionWithAccounts parses instruction data with account information
func ParseInstructionWithAccounts(instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (Instruction, error) {
	return ParseInstruction(instructionData, accountIndicesData, accountKeys)
}
