// Code generated by https://github.com/gagliardetto/anchor-go. DO NOT EDIT.
// This file contains instructions and instruction parsers.

package mpltokenmetadata

import (
	"bytes"
	"fmt"
	binary "github.com/gagliardetto/binary"
	solanago "github.com/gagliardetto/solana-go"
)

// Builds a "create_metadata_account" instruction.
func NewCreateMetadataAccountInstruction(
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "mint": Read-only, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 2 "mint_authority": Read-only, Signer, Required
		// Mint authority
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 3 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 4 "update_authority": Read-only, Non-signer, Required
		// update authority info
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "rent": Read-only, Non-signer, Required
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "update_metadata_account" instruction.
func NewUpdateMetadataAccountInstruction(
	metadataAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "update_authority": Read-only, Signer, Required
		// Update authority key
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "deprecated_create_master_edition" instruction.
func NewDeprecatedCreateMasterEditionInstruction(
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	printingMintAccount solanago.PublicKey,
	oneTimePrintingAuthorizationMintAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	printingMintAuthorityAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
	oneTimePrintingAuthorizationMintAuthorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "edition": Writable, Non-signer, Required
		// Unallocated edition V1 account with address as pda of ['metadata', program id, mint, 'edition']
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 1 "mint": Writable, Non-signer, Required
		// Metadata mint
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 2 "printing_mint": Writable, Non-signer, Required
		// Printing mint - A mint you control that can mint tokens that can be exchanged for limited editions of your master edition via the MintNewEditionFromMasterEditionViaToken endpoint
		accounts__.Append(solanago.NewAccountMeta(printingMintAccount, true, false))
		// Account 3 "one_time_printing_authorization_mint": Writable, Non-signer, Required
		// One time authorization printing mint - A mint you control that prints tokens that gives the bearer permission to mint any number of tokens from the printing mint one time via an endpoint with the token-metadata program for your metadata. Also burns the token.
		accounts__.Append(solanago.NewAccountMeta(oneTimePrintingAuthorizationMintAccount, true, false))
		// Account 4 "update_authority": Read-only, Signer, Required
		// Current Update authority key
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 5 "printing_mint_authority": Read-only, Signer, Required
		// Printing mint authority - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY.
		accounts__.Append(solanago.NewAccountMeta(printingMintAuthorityAccount, false, true))
		// Account 6 "mint_authority": Read-only, Signer, Required
		// Mint authority on the metadata's mint - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 7 "metadata": Read-only, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 8 "payer": Read-only, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, false, true))
		// Account 9 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 10 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 11 "rent": Read-only, Non-signer, Required
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
		// Account 12 "one_time_printing_authorization_mint_authority": Read-only, Signer, Required
		// One time authorization printing mint authority - must be provided if using max supply. THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY.
		accounts__.Append(solanago.NewAccountMeta(oneTimePrintingAuthorizationMintAuthorityAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "deprecated_mint_new_edition_from_master_edition_via_printing_token" instruction.
func NewDeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction(
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	printingMintAccount solanago.PublicKey,
	masterTokenAccountAccount solanago.PublicKey,
	editionMarkerAccount solanago.PublicKey,
	burnAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	masterUpdateAuthorityAccount solanago.PublicKey,
	masterMetadataAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
	reservationListAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// New Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "edition": Writable, Non-signer, Required
		// New Edition V1 (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 2 "master_edition": Writable, Non-signer, Required
		// Master Record Edition V1 (pda of ['metadata', program id, master metadata mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 3 "mint": Writable, Non-signer, Required
		// Mint of new token - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 4 "mint_authority": Read-only, Signer, Required
		// Mint authority of new mint
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 5 "printing_mint": Writable, Non-signer, Required
		// Printing Mint of master record edition
		accounts__.Append(solanago.NewAccountMeta(printingMintAccount, true, false))
		// Account 6 "master_token_account": Writable, Non-signer, Required
		// Token account containing Printing mint token to be transferred
		accounts__.Append(solanago.NewAccountMeta(masterTokenAccountAccount, true, false))
		// Account 7 "edition_marker": Writable, Non-signer, Required
		// Edition pda to mark creation - will be checked for pre-existence. (pda of ['metadata', program id, master mint id, edition_number])
		accounts__.Append(solanago.NewAccountMeta(editionMarkerAccount, true, false))
		// Account 8 "burn_authority": Read-only, Signer, Required
		// Burn authority for this token
		accounts__.Append(solanago.NewAccountMeta(burnAuthorityAccount, false, true))
		// Account 9 "payer": Read-only, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, false, true))
		// Account 10 "master_update_authority": Read-only, Non-signer, Required
		// update authority info for new metadata account
		accounts__.Append(solanago.NewAccountMeta(masterUpdateAuthorityAccount, false, false))
		// Account 11 "master_metadata": Read-only, Non-signer, Required
		// Master record metadata account
		accounts__.Append(solanago.NewAccountMeta(masterMetadataAccount, false, false))
		// Account 12 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 13 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 14 "rent": Read-only, Non-signer, Required
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
		// Account 15 "reservation_list": Writable, Non-signer, Optional
		// Reservation List - If present, and you are on this list, you can get an edition number given by your position on the list.
		accounts__.Append(solanago.NewAccountMeta(reservationListAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "update_primary_sale_happened_via_token" instruction.
func NewUpdatePrimarySaleHappenedViaTokenInstruction(
	metadataAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "owner": Read-only, Signer, Required
		// Owner on the token account
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, true))
		// Account 2 "token": Read-only, Non-signer, Required
		// Account containing tokens from the metadata's mint
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "deprecated_set_reservation_list" instruction.
func NewDeprecatedSetReservationListInstruction(
	masterEditionAccount solanago.PublicKey,
	reservationListAccount solanago.PublicKey,
	resourceAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "master_edition": Writable, Non-signer, Required
		// Master Edition V1 key (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 1 "reservation_list": Writable, Non-signer, Required
		// PDA for ReservationList of ['metadata', program id, master edition key, 'reservation', resource-key]
		accounts__.Append(solanago.NewAccountMeta(reservationListAccount, true, false))
		// Account 2 "resource": Read-only, Signer, Required
		// The resource you tied the reservation list too
		accounts__.Append(solanago.NewAccountMeta(resourceAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "deprecated_create_reservation_list" instruction.
func NewDeprecatedCreateReservationListInstruction(
	reservationListAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	resourceAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "reservation_list": Writable, Non-signer, Required
		// PDA for ReservationList of ['metadata', program id, master edition key, 'reservation', resource-key]
		accounts__.Append(solanago.NewAccountMeta(reservationListAccount, true, false))
		// Account 1 "payer": Read-only, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, false, true))
		// Account 2 "update_authority": Read-only, Signer, Required
		// Update authority
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 3 "master_edition": Read-only, Non-signer, Required
		//
		//	Master Edition V1 key (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, false, false))
		// Account 4 "resource": Read-only, Non-signer, Required
		// A resource you wish to tie the reservation list to. This is so your later visitors who come to redeem can derive your reservation list PDA with something they can easily get at. You choose what this should be.
		accounts__.Append(solanago.NewAccountMeta(resourceAccount, false, false))
		// Account 5 "metadata": Read-only, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "rent": Read-only, Non-signer, Required
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "sign_metadata" instruction.
func NewSignMetadataInstruction(
	metadataAccount solanago.PublicKey,
	creatorAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "creator": Read-only, Signer, Required
		// Creator
		accounts__.Append(solanago.NewAccountMeta(creatorAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "deprecated_mint_printing_tokens_via_token" instruction.
func NewDeprecatedMintPrintingTokensViaTokenInstruction(
	destinationAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	oneTimePrintingAuthorizationMintAccount solanago.PublicKey,
	printingMintAccount solanago.PublicKey,
	burnAuthorityAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "destination": Writable, Non-signer, Required
		// Destination account
		accounts__.Append(solanago.NewAccountMeta(destinationAccount, true, false))
		// Account 1 "token": Writable, Non-signer, Required
		// Token account containing one time authorization token
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 2 "one_time_printing_authorization_mint": Writable, Non-signer, Required
		// One time authorization mint
		accounts__.Append(solanago.NewAccountMeta(oneTimePrintingAuthorizationMintAccount, true, false))
		// Account 3 "printing_mint": Writable, Non-signer, Required
		// Printing mint
		accounts__.Append(solanago.NewAccountMeta(printingMintAccount, true, false))
		// Account 4 "burn_authority": Read-only, Signer, Required
		// Burn authority
		accounts__.Append(solanago.NewAccountMeta(burnAuthorityAccount, false, true))
		// Account 5 "metadata": Read-only, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 6 "master_edition": Read-only, Non-signer, Required
		// Master Edition V1 key (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, false, false))
		// Account 7 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 8 "rent": Read-only, Non-signer, Required
		// Rent
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "deprecated_mint_printing_tokens" instruction.
func NewDeprecatedMintPrintingTokensInstruction(
	destinationAccount solanago.PublicKey,
	printingMintAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "destination": Writable, Non-signer, Required
		// Destination account
		accounts__.Append(solanago.NewAccountMeta(destinationAccount, true, false))
		// Account 1 "printing_mint": Writable, Non-signer, Required
		// Printing mint
		accounts__.Append(solanago.NewAccountMeta(printingMintAccount, true, false))
		// Account 2 "update_authority": Read-only, Signer, Required
		// Update authority
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 3 "metadata": Read-only, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 4 "master_edition": Read-only, Non-signer, Required
		// Master Edition V1 key (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, false, false))
		// Account 5 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 6 "rent": Read-only, Non-signer, Required
		// Rent
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "create_master_edition" instruction.
func NewCreateMasterEditionInstruction(
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "edition": Writable, Non-signer, Required
		// Unallocated edition V2 account with address as pda of ['metadata', program id, mint, 'edition']
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 1 "mint": Writable, Non-signer, Required
		// Metadata mint
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 2 "update_authority": Read-only, Signer, Required
		// Update authority
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 3 "mint_authority": Read-only, Signer, Required
		// Mint authority on the metadata's mint - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 4 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 5 "metadata": Read-only, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 6 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 7 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 8 "rent": Read-only, Non-signer, Required
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "mint_new_edition_from_master_edition_via_token" instruction.
func NewMintNewEditionFromMasterEditionViaTokenInstruction(
	// Params:
	mintNewEditionFromMasterEditionViaTokenArgsParam MintNewEditionFromMasterEditionViaTokenArgs,

	// Accounts:
	newMetadataAccount solanago.PublicKey,
	newEditionAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	newMintAccount solanago.PublicKey,
	editionMarkPdaAccount solanago.PublicKey,
	newMintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	tokenAccountOwnerAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	newMetadataUpdateAuthorityAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_MintNewEditionFromMasterEditionViaToken[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `mintNewEditionFromMasterEditionViaTokenArgsParam`:
		err = enc__.Encode(mintNewEditionFromMasterEditionViaTokenArgsParam)
		if err != nil {
			return nil, newFieldError("mintNewEditionFromMasterEditionViaTokenArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "new_metadata": Writable, Non-signer, Required
		// New Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(newMetadataAccount, true, false))
		// Account 1 "new_edition": Writable, Non-signer, Required
		// New Edition (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(newEditionAccount, true, false))
		// Account 2 "master_edition": Writable, Non-signer, Required
		// Master Record Edition V2 (pda of ['metadata', program id, master metadata mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 3 "new_mint": Writable, Non-signer, Required
		// Mint of new token - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(newMintAccount, true, false))
		// Account 4 "edition_mark_pda": Writable, Non-signer, Required
		// Edition pda to mark creation - will be checked for pre-existence. (pda of ['metadata', program id, master metadata mint id, 'edition', edition_number]) where edition_number is NOT the edition number you pass in args but actually edition_number = floor(edition/EDITION_MARKER_BIT_SIZE).
		accounts__.Append(solanago.NewAccountMeta(editionMarkPdaAccount, true, false))
		// Account 5 "new_mint_authority": Read-only, Signer, Required
		// Mint authority of new mint
		accounts__.Append(solanago.NewAccountMeta(newMintAuthorityAccount, false, true))
		// Account 6 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 7 "token_account_owner": Read-only, Signer, Required
		// owner of token account containing master token (#8)
		accounts__.Append(solanago.NewAccountMeta(tokenAccountOwnerAccount, false, true))
		// Account 8 "token_account": Read-only, Non-signer, Required
		// token account containing token from master metadata mint
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, false, false))
		// Account 9 "new_metadata_update_authority": Read-only, Non-signer, Required
		// Update authority info for new metadata
		accounts__.Append(solanago.NewAccountMeta(newMetadataUpdateAuthorityAccount, false, false))
		// Account 10 "metadata": Read-only, Non-signer, Required
		// Master record metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 11 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 12 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 13 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "convert_master_edition_v1_to_v2" instruction.
func NewConvertMasterEditionV1ToV2Instruction(
	masterEditionAccount solanago.PublicKey,
	oneTimeAuthAccount solanago.PublicKey,
	printingMintAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "master_edition": Writable, Non-signer, Required
		// Master Record Edition V1 (pda of ['metadata', program id, master metadata mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 1 "one_time_auth": Writable, Non-signer, Required
		// One time authorization mint
		accounts__.Append(solanago.NewAccountMeta(oneTimeAuthAccount, true, false))
		// Account 2 "printing_mint": Writable, Non-signer, Required
		// Printing mint
		accounts__.Append(solanago.NewAccountMeta(printingMintAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "mint_new_edition_from_master_edition_via_vault_proxy" instruction.
func NewMintNewEditionFromMasterEditionViaVaultProxyInstruction(
	// Params:
	mintNewEditionFromMasterEditionViaTokenArgsParam MintNewEditionFromMasterEditionViaTokenArgs,

	// Accounts:
	newMetadataAccount solanago.PublicKey,
	newEditionAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	newMintAccount solanago.PublicKey,
	editionMarkPdaAccount solanago.PublicKey,
	newMintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	vaultAuthorityAccount solanago.PublicKey,
	safetyDepositStoreAccount solanago.PublicKey,
	safetyDepositBoxAccount solanago.PublicKey,
	vaultAccount solanago.PublicKey,
	newMetadataUpdateAuthorityAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	tokenVaultProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_MintNewEditionFromMasterEditionViaVaultProxy[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `mintNewEditionFromMasterEditionViaTokenArgsParam`:
		err = enc__.Encode(mintNewEditionFromMasterEditionViaTokenArgsParam)
		if err != nil {
			return nil, newFieldError("mintNewEditionFromMasterEditionViaTokenArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "new_metadata": Writable, Non-signer, Required
		// New Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(newMetadataAccount, true, false))
		// Account 1 "new_edition": Writable, Non-signer, Required
		// New Edition (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(newEditionAccount, true, false))
		// Account 2 "master_edition": Writable, Non-signer, Required
		// Master Record Edition V2 (pda of ['metadata', program id, master metadata mint id, 'edition']
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 3 "new_mint": Writable, Non-signer, Required
		// Mint of new token - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(newMintAccount, true, false))
		// Account 4 "edition_mark_pda": Writable, Non-signer, Required
		// Edition pda to mark creation - will be checked for pre-existence. (pda of ['metadata', program id, master metadata mint id, 'edition', edition_number]) where edition_number is NOT the edition number you pass in args but actually edition_number = floor(edition/EDITION_MARKER_BIT_SIZE).
		accounts__.Append(solanago.NewAccountMeta(editionMarkPdaAccount, true, false))
		// Account 5 "new_mint_authority": Read-only, Signer, Required
		// Mint authority of new mint
		accounts__.Append(solanago.NewAccountMeta(newMintAuthorityAccount, false, true))
		// Account 6 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 7 "vault_authority": Read-only, Signer, Required
		// Vault authority
		accounts__.Append(solanago.NewAccountMeta(vaultAuthorityAccount, false, true))
		// Account 8 "safety_deposit_store": Read-only, Non-signer, Required
		// Safety deposit token store account
		accounts__.Append(solanago.NewAccountMeta(safetyDepositStoreAccount, false, false))
		// Account 9 "safety_deposit_box": Read-only, Non-signer, Required
		// Safety deposit box
		accounts__.Append(solanago.NewAccountMeta(safetyDepositBoxAccount, false, false))
		// Account 10 "vault": Read-only, Non-signer, Required
		// Vault
		accounts__.Append(solanago.NewAccountMeta(vaultAccount, false, false))
		// Account 11 "new_metadata_update_authority": Read-only, Non-signer, Required
		// Update authority info for new metadata
		accounts__.Append(solanago.NewAccountMeta(newMetadataUpdateAuthorityAccount, false, false))
		// Account 12 "metadata": Read-only, Non-signer, Required
		// Master record metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 13 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 14 "token_vault_program": Read-only, Non-signer, Required
		// Token vault program
		accounts__.Append(solanago.NewAccountMeta(tokenVaultProgramAccount, false, false))
		// Account 15 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 16 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "puff_metadata" instruction.
func NewPuffMetadataInstruction(
	metadataAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "update_metadata_account_v2" instruction.
func NewUpdateMetadataAccountV2Instruction(
	// Params:
	updateMetadataAccountArgsV2Param UpdateMetadataAccountArgsV2,

	// Accounts:
	metadataAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_UpdateMetadataAccountV2[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateMetadataAccountArgsV2Param`:
		err = enc__.Encode(updateMetadataAccountArgsV2Param)
		if err != nil {
			return nil, newFieldError("updateMetadataAccountArgsV2Param", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "update_authority": Read-only, Signer, Required
		// Update authority key
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create_metadata_account_v2" instruction.
func NewCreateMetadataAccountV2Instruction(
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "mint": Read-only, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 2 "mint_authority": Read-only, Signer, Required
		// Mint authority
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 3 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 4 "update_authority": Read-only, Non-signer, Required
		// update authority info
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "create_master_edition_v3" instruction.
func NewCreateMasterEditionV3Instruction(
	// Params:
	createMasterEditionArgsParam CreateMasterEditionArgs,

	// Accounts:
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateMasterEditionV3[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createMasterEditionArgsParam`:
		err = enc__.Encode(createMasterEditionArgsParam)
		if err != nil {
			return nil, newFieldError("createMasterEditionArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "edition": Writable, Non-signer, Required
		// Unallocated edition V2 account with address as pda of ['metadata', program id, mint, 'edition']
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 1 "mint": Writable, Non-signer, Required
		// Metadata mint
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 2 "update_authority": Read-only, Signer, Required
		// Update authority
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 3 "mint_authority": Read-only, Signer, Required
		// Mint authority on the metadata's mint - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 4 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 5 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 6 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 7 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 8 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "verify_collection" instruction.
func NewVerifyCollectionInstruction(
	metadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	collectionMasterEditionAccountAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "collection_authority": Writable, Signer, Required
		// Collection Update authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, true, true))
		// Account 2 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 4 "collection": Read-only, Non-signer, Required
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 5 "collection_master_edition_account": Read-only, Non-signer, Required
		// MasterEdition2 Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccountAccount, false, false))
		// Account 6 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "utilize" instruction.
func NewUtilizeInstruction(
	// Params:
	utilizeArgsParam UtilizeArgs,

	// Accounts:
	metadataAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	useAuthorityAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	ataProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
	useAuthorityRecordAccount solanago.PublicKey,
	burnerAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Utilize[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `utilizeArgsParam`:
		err = enc__.Encode(utilizeArgsParam)
		if err != nil {
			return nil, newFieldError("utilizeArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "token_account": Writable, Non-signer, Required
		// Token Account Of NFT
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, true, false))
		// Account 2 "mint": Writable, Non-signer, Required
		// Mint of the Metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 3 "use_authority": Writable, Signer, Required
		// A Use Authority / Can be the current Owner of the NFT
		accounts__.Append(solanago.NewAccountMeta(useAuthorityAccount, true, true))
		// Account 4 "owner": Read-only, Non-signer, Required
		// Owner
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, false, false))
		// Account 5 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 6 "ata_program": Read-only, Non-signer, Required
		// Associated Token program
		accounts__.Append(solanago.NewAccountMeta(ataProgramAccount, false, false))
		// Account 7 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 8 "rent": Read-only, Non-signer, Required
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
		// Account 9 "use_authority_record": Writable, Non-signer, Optional
		// Use Authority Record PDA If present the program Assumes a delegated use authority
		accounts__.Append(solanago.NewAccountMeta(useAuthorityRecordAccount, true, false))
		// Account 10 "burner": Read-only, Non-signer, Optional
		// Program As Signer (Burner)
		accounts__.Append(solanago.NewAccountMeta(burnerAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "approve_use_authority" instruction.
func NewApproveUseAuthorityInstruction(
	// Params:
	approveUseAuthorityArgsParam ApproveUseAuthorityArgs,

	// Accounts:
	useAuthorityRecordAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	userAccount solanago.PublicKey,
	ownerTokenAccountAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	burnerAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_ApproveUseAuthority[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `approveUseAuthorityArgsParam`:
		err = enc__.Encode(approveUseAuthorityArgsParam)
		if err != nil {
			return nil, newFieldError("approveUseAuthorityArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "use_authority_record": Writable, Non-signer, Required
		// Use Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(useAuthorityRecordAccount, true, false))
		// Account 1 "owner": Writable, Signer, Required
		// Owner
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, true, true))
		// Account 2 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "user": Read-only, Non-signer, Required
		// A Use Authority
		accounts__.Append(solanago.NewAccountMeta(userAccount, false, false))
		// Account 4 "owner_token_account": Writable, Non-signer, Required
		// Owned Token Account Of Mint
		accounts__.Append(solanago.NewAccountMeta(ownerTokenAccountAccount, true, false))
		// Account 5 "metadata": Read-only, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 6 "mint": Read-only, Non-signer, Required
		// Mint of Metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 7 "burner": Read-only, Non-signer, Required
		// Program As Signer (Burner)
		accounts__.Append(solanago.NewAccountMeta(burnerAccount, false, false))
		// Account 8 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 9 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 10 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "revoke_use_authority" instruction.
func NewRevokeUseAuthorityInstruction(
	useAuthorityRecordAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	userAccount solanago.PublicKey,
	ownerTokenAccountAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "use_authority_record": Writable, Non-signer, Required
		// Use Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(useAuthorityRecordAccount, true, false))
		// Account 1 "owner": Writable, Signer, Required
		// Owner
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, true, true))
		// Account 2 "user": Read-only, Non-signer, Required
		// A Use Authority
		accounts__.Append(solanago.NewAccountMeta(userAccount, false, false))
		// Account 3 "owner_token_account": Writable, Non-signer, Required
		// Owned Token Account Of Mint
		accounts__.Append(solanago.NewAccountMeta(ownerTokenAccountAccount, true, false))
		// Account 4 "mint": Read-only, Non-signer, Required
		// Mint of Metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 5 "metadata": Read-only, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 6 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 7 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 8 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "unverify_collection" instruction.
func NewUnverifyCollectionInstruction(
	metadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	collectionMasterEditionAccountAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "collection_authority": Writable, Signer, Required
		// Collection Authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, true, true))
		// Account 2 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 3 "collection": Read-only, Non-signer, Required
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 4 "collection_master_edition_account": Read-only, Non-signer, Required
		// MasterEdition2 Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccountAccount, false, false))
		// Account 5 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "approve_collection_authority" instruction.
func NewApproveCollectionAuthorityInstruction(
	collectionAuthorityRecordAccount solanago.PublicKey,
	newCollectionAuthorityAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection_authority_record": Writable, Non-signer, Required
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, true, false))
		// Account 1 "new_collection_authority": Read-only, Non-signer, Required
		// A Collection Authority
		accounts__.Append(solanago.NewAccountMeta(newCollectionAuthorityAccount, false, false))
		// Account 2 "update_authority": Writable, Signer, Required
		// Update Authority of Collection NFT
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, true, true))
		// Account 3 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 4 "metadata": Read-only, Non-signer, Required
		// Collection Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 5 "mint": Read-only, Non-signer, Required
		// Mint of Collection Metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "revoke_collection_authority" instruction.
func NewRevokeCollectionAuthorityInstruction(
	collectionAuthorityRecordAccount solanago.PublicKey,
	delegateAuthorityAccount solanago.PublicKey,
	revokeAuthorityAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection_authority_record": Writable, Non-signer, Required
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, true, false))
		// Account 1 "delegate_authority": Writable, Non-signer, Required
		// Delegated Collection Authority
		accounts__.Append(solanago.NewAccountMeta(delegateAuthorityAccount, true, false))
		// Account 2 "revoke_authority": Writable, Signer, Required
		// Update Authority, or Delegated Authority, of Collection NFT
		accounts__.Append(solanago.NewAccountMeta(revokeAuthorityAccount, true, true))
		// Account 3 "metadata": Read-only, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 4 "mint": Read-only, Non-signer, Required
		// Mint of Metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "set_and_verify_collection" instruction.
func NewSetAndVerifyCollectionInstruction(
	metadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	collectionMasterEditionAccountAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "collection_authority": Writable, Signer, Required
		// Collection Update authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, true, true))
		// Account 2 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "update_authority": Read-only, Non-signer, Required
		// Update Authority of Collection NFT and NFT
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 4 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 5 "collection": Read-only, Non-signer, Required
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, false, false))
		// Account 6 "collection_master_edition_account": Read-only, Non-signer, Required
		// MasterEdition2 Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccountAccount, false, false))
		// Account 7 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "freeze_delegated_account" instruction.
func NewFreezeDelegatedAccountInstruction(
	delegateAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "delegate": Writable, Signer, Required
		// Delegate
		accounts__.Append(solanago.NewAccountMeta(delegateAccount, true, true))
		// Account 1 "token_account": Writable, Non-signer, Required
		// Token account to freeze
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, true, false))
		// Account 2 "edition": Read-only, Non-signer, Required
		// Edition
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 3 "mint": Read-only, Non-signer, Required
		// Token mint
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 4 "token_program": Read-only, Non-signer, Required
		// Token Program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "thaw_delegated_account" instruction.
func NewThawDelegatedAccountInstruction(
	delegateAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "delegate": Writable, Signer, Required
		// Delegate
		accounts__.Append(solanago.NewAccountMeta(delegateAccount, true, true))
		// Account 1 "token_account": Writable, Non-signer, Required
		// Token account to thaw
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, true, false))
		// Account 2 "edition": Read-only, Non-signer, Required
		// Edition
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 3 "mint": Read-only, Non-signer, Required
		// Token mint
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 4 "token_program": Read-only, Non-signer, Required
		// Token Program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "remove_creator_verification" instruction.
func NewRemoveCreatorVerificationInstruction(
	metadataAccount solanago.PublicKey,
	creatorAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "creator": Read-only, Signer, Required
		// Creator
		accounts__.Append(solanago.NewAccountMeta(creatorAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "burn_nft" instruction.
func NewBurnNftInstruction(
	metadataAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	masterEditionAccountAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	collectionMetadataAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "owner": Writable, Signer, Required
		// NFT owner
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, true, true))
		// Account 2 "mint": Writable, Non-signer, Required
		// Mint of the NFT
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 3 "token_account": Writable, Non-signer, Required
		// Token account to close
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, true, false))
		// Account 4 "master_edition_account": Writable, Non-signer, Required
		// MasterEdition2 of the NFT
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccountAccount, true, false))
		// Account 5 "spl_token_program": Read-only, Non-signer, Required
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 6 "collection_metadata": Writable, Non-signer, Optional
		// Metadata of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "verify_sized_collection_item" instruction.
func NewVerifySizedCollectionItemInstruction(
	metadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	collectionMasterEditionAccountAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "collection_authority": Read-only, Signer, Required
		// Collection Update authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, false, true))
		// Account 2 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 4 "collection": Writable, Non-signer, Required
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 5 "collection_master_edition_account": Read-only, Non-signer, Required
		// MasterEdition2 Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccountAccount, false, false))
		// Account 6 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "unverify_sized_collection_item" instruction.
func NewUnverifySizedCollectionItemInstruction(
	metadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	collectionMasterEditionAccountAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "collection_authority": Read-only, Signer, Required
		// Collection Authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, false, true))
		// Account 2 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 4 "collection": Writable, Non-signer, Required
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 5 "collection_master_edition_account": Read-only, Non-signer, Required
		// MasterEdition2 Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccountAccount, false, false))
		// Account 6 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "set_and_verify_sized_collection_item" instruction.
func NewSetAndVerifySizedCollectionItemInstruction(
	metadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAccount solanago.PublicKey,
	collectionMasterEditionAccountAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "collection_authority": Read-only, Signer, Required
		// Collection Update authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, false, true))
		// Account 2 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "update_authority": Read-only, Non-signer, Required
		// Update Authority of Collection NFT and NFT
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 4 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 5 "collection": Writable, Non-signer, Required
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionAccount, true, false))
		// Account 6 "collection_master_edition_account": Read-only, Non-signer, Required
		// MasterEdition2 Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccountAccount, false, false))
		// Account 7 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "create_metadata_account_v3" instruction.
func NewCreateMetadataAccountV3Instruction(
	// Params:
	createMetadataAccountArgsV3Param CreateMetadataAccountArgsV3,

	// Accounts:
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	mintAuthorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	rentAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_CreateMetadataAccountV3[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createMetadataAccountArgsV3Param`:
		err = enc__.Encode(createMetadataAccountArgsV3Param)
		if err != nil {
			return nil, newFieldError("createMetadataAccountArgsV3Param", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "mint": Read-only, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 2 "mint_authority": Read-only, Signer, Required
		// Mint authority
		accounts__.Append(solanago.NewAccountMeta(mintAuthorityAccount, false, true))
		// Account 3 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 4 "update_authority": Read-only, Non-signer, Required
		// update authority info
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "rent": Read-only, Non-signer, Optional
		// Rent info
		accounts__.Append(solanago.NewAccountMeta(rentAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "set_collection_size" instruction.
func NewSetCollectionSizeInstruction(
	// Params:
	setCollectionSizeArgsParam SetCollectionSizeArgs,

	// Accounts:
	collectionMetadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_SetCollectionSize[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `setCollectionSizeArgsParam`:
		err = enc__.Encode(setCollectionSizeArgsParam)
		if err != nil {
			return nil, newFieldError("setCollectionSizeArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection_metadata": Writable, Non-signer, Required
		// Collection Metadata account
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, true, false))
		// Account 1 "collection_authority": Writable, Signer, Required
		// Collection Update authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, true, true))
		// Account 2 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 3 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "set_token_standard" instruction.
func NewSetTokenStandardInstruction(
	metadataAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "update_authority": Read-only, Signer, Required
		// Metadata update authority
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, true))
		// Account 2 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 3 "edition": Read-only, Non-signer, Optional
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "bubblegum_set_collection_size" instruction.
func NewBubblegumSetCollectionSizeInstruction(
	// Params:
	setCollectionSizeArgsParam SetCollectionSizeArgs,

	// Accounts:
	collectionMetadataAccount solanago.PublicKey,
	collectionAuthorityAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	bubblegumSignerAccount solanago.PublicKey,
	collectionAuthorityRecordAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_BubblegumSetCollectionSize[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `setCollectionSizeArgsParam`:
		err = enc__.Encode(setCollectionSizeArgsParam)
		if err != nil {
			return nil, newFieldError("setCollectionSizeArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "collection_metadata": Writable, Non-signer, Required
		// Collection Metadata account
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, true, false))
		// Account 1 "collection_authority": Read-only, Signer, Required
		// Collection Update authority
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityAccount, false, true))
		// Account 2 "collection_mint": Read-only, Non-signer, Required
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 3 "bubblegum_signer": Read-only, Signer, Required
		// Signing PDA of Bubblegum program
		accounts__.Append(solanago.NewAccountMeta(bubblegumSignerAccount, false, true))
		// Account 4 "collection_authority_record": Read-only, Non-signer, Optional
		// Collection Authority Record PDA
		accounts__.Append(solanago.NewAccountMeta(collectionAuthorityRecordAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "burn_edition_nft" instruction.
func NewBurnEditionNftInstruction(
	metadataAccount solanago.PublicKey,
	ownerAccount solanago.PublicKey,
	printEditionMintAccount solanago.PublicKey,
	masterEditionMintAccount solanago.PublicKey,
	printEditionTokenAccountAccount solanago.PublicKey,
	masterEditionTokenAccountAccount solanago.PublicKey,
	masterEditionAccountAccount solanago.PublicKey,
	printEditionAccountAccount solanago.PublicKey,
	editionMarkerAccountAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "owner": Writable, Signer, Required
		// NFT owner
		accounts__.Append(solanago.NewAccountMeta(ownerAccount, true, true))
		// Account 2 "print_edition_mint": Writable, Non-signer, Required
		// Mint of the print edition NFT
		accounts__.Append(solanago.NewAccountMeta(printEditionMintAccount, true, false))
		// Account 3 "master_edition_mint": Read-only, Non-signer, Required
		// Mint of the original/master NFT
		accounts__.Append(solanago.NewAccountMeta(masterEditionMintAccount, false, false))
		// Account 4 "print_edition_token_account": Writable, Non-signer, Required
		// Token account the print edition NFT is in
		accounts__.Append(solanago.NewAccountMeta(printEditionTokenAccountAccount, true, false))
		// Account 5 "master_edition_token_account": Read-only, Non-signer, Required
		// Token account the Master Edition NFT is in
		accounts__.Append(solanago.NewAccountMeta(masterEditionTokenAccountAccount, false, false))
		// Account 6 "master_edition_account": Writable, Non-signer, Required
		// MasterEdition2 of the original NFT
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccountAccount, true, false))
		// Account 7 "print_edition_account": Writable, Non-signer, Required
		// Print Edition account of the NFT
		accounts__.Append(solanago.NewAccountMeta(printEditionAccountAccount, true, false))
		// Account 8 "edition_marker_account": Writable, Non-signer, Required
		// Edition Marker PDA of the NFT
		accounts__.Append(solanago.NewAccountMeta(editionMarkerAccountAccount, true, false))
		// Account 9 "spl_token_program": Read-only, Non-signer, Required
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "create_escrow_account" instruction.
func NewCreateEscrowAccountInstruction(
	escrowAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "escrow": Writable, Non-signer, Required
		// Escrow account
		accounts__.Append(solanago.NewAccountMeta(escrowAccount, true, false))
		// Account 1 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 2 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 3 "token_account": Read-only, Non-signer, Required
		// Token account of the token
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, false, false))
		// Account 4 "edition": Read-only, Non-signer, Required
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 5 "payer": Writable, Signer, Required
		// Wallet paying for the transaction and new account
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 8 "authority": Read-only, Signer, Optional
		// Authority/creator of the escrow account
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "close_escrow_account" instruction.
func NewCloseEscrowAccountInstruction(
	escrowAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenAccountAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "escrow": Writable, Non-signer, Required
		// Escrow account
		accounts__.Append(solanago.NewAccountMeta(escrowAccount, true, false))
		// Account 1 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 2 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 3 "token_account": Read-only, Non-signer, Required
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccountAccount, false, false))
		// Account 4 "edition": Read-only, Non-signer, Required
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 5 "payer": Writable, Signer, Required
		// Wallet paying for the transaction and new account
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "transfer_out_of_escrow" instruction.
func NewTransferOutOfEscrowInstruction(
	// Params:
	transferOutOfEscrowArgsParam TransferOutOfEscrowArgs,

	// Accounts:
	escrowAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	attributeMintAccount solanago.PublicKey,
	attributeSrcAccount solanago.PublicKey,
	attributeDstAccount solanago.PublicKey,
	escrowMintAccount solanago.PublicKey,
	escrowAccountAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	ataProgramAccount solanago.PublicKey,
	tokenProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_TransferOutOfEscrow[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `transferOutOfEscrowArgsParam`:
		err = enc__.Encode(transferOutOfEscrowArgsParam)
		if err != nil {
			return nil, newFieldError("transferOutOfEscrowArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "escrow": Read-only, Non-signer, Required
		// Escrow account
		accounts__.Append(solanago.NewAccountMeta(escrowAccount, false, false))
		// Account 1 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 2 "payer": Writable, Signer, Required
		// Wallet paying for the transaction and new account
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 3 "attribute_mint": Read-only, Non-signer, Required
		// Mint account for the new attribute
		accounts__.Append(solanago.NewAccountMeta(attributeMintAccount, false, false))
		// Account 4 "attribute_src": Writable, Non-signer, Required
		// Token account source for the new attribute
		accounts__.Append(solanago.NewAccountMeta(attributeSrcAccount, true, false))
		// Account 5 "attribute_dst": Writable, Non-signer, Required
		// Token account, owned by TM, destination for the new attribute
		accounts__.Append(solanago.NewAccountMeta(attributeDstAccount, true, false))
		// Account 6 "escrow_mint": Read-only, Non-signer, Required
		// Mint account that the escrow is attached
		accounts__.Append(solanago.NewAccountMeta(escrowMintAccount, false, false))
		// Account 7 "escrow_account": Read-only, Non-signer, Required
		// Token account that holds the token the escrow is attached to
		accounts__.Append(solanago.NewAccountMeta(escrowAccountAccount, false, false))
		// Account 8 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 9 "ata_program": Read-only, Non-signer, Required
		// Associated Token program
		accounts__.Append(solanago.NewAccountMeta(ataProgramAccount, false, false))
		// Account 10 "token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(tokenProgramAccount, false, false))
		// Account 11 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 12 "authority": Read-only, Signer, Optional
		// Authority/creator of the escrow account
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "burn" instruction.
func NewBurnInstruction(
	// Params:
	burnArgsParam BurnArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	collectionMetadataAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	masterEditionMintAccount solanago.PublicKey,
	masterEditionTokenAccount solanago.PublicKey,
	editionMarkerAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Burn[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `burnArgsParam`:
		{
			err := EncodeBurnArgs(enc__, burnArgsParam)
			if err != nil {
				return nil, newFieldError("burnArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Writable, Signer, Required
		// Asset owner or Utility delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, true, true))
		// Account 1 "collection_metadata": Writable, Non-signer, Optional
		// Metadata of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, true, false))
		// Account 2 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 3 "edition": Writable, Non-signer, Optional
		// Edition of the asset
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 4 "mint": Writable, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 5 "token": Writable, Non-signer, Required
		// Token account to close
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 6 "master_edition": Writable, Non-signer, Optional
		// Master edition account
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 7 "master_edition_mint": Read-only, Non-signer, Optional
		// Master edition mint of the asset
		accounts__.Append(solanago.NewAccountMeta(masterEditionMintAccount, false, false))
		// Account 8 "master_edition_token": Read-only, Non-signer, Optional
		// Master edition token account
		accounts__.Append(solanago.NewAccountMeta(masterEditionTokenAccount, false, false))
		// Account 9 "edition_marker": Writable, Non-signer, Optional
		// Edition marker account
		accounts__.Append(solanago.NewAccountMeta(editionMarkerAccount, true, false))
		// Account 10 "token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 11 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 12 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 13 "spl_token_program": Read-only, Non-signer, Required
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "create" instruction.
func NewCreateInstruction(
	// Params:
	createArgsParam CreateArgs,

	// Accounts:
	metadataAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Create[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `createArgsParam`:
		{
			err := EncodeCreateArgs(enc__, createArgsParam)
			if err != nil {
				return nil, newFieldError("createArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Unallocated metadata account with address as pda of ['metadata', program id, mint id]
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "master_edition": Writable, Non-signer, Optional
		// Unallocated edition account with address as pda of ['metadata', program id, mint, 'edition']
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 2 "mint": Writable, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 3 "authority": Read-only, Signer, Required
		// Mint authority
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 5 "update_authority": Read-only, Non-signer, Required
		// Update authority for the metadata account
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 8 "spl_token_program": Read-only, Non-signer, Optional
		// SPL Token program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "mint" instruction.
func NewMintInstruction(
	// Params:
	mintArgsParam MintArgs,

	// Accounts:
	tokenAccount solanago.PublicKey,
	tokenOwnerAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	delegateRecordAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	splAtaProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Mint[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `mintArgsParam`:
		{
			err := EncodeMintArgs(enc__, mintArgsParam)
			if err != nil {
				return nil, newFieldError("mintArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "token": Writable, Non-signer, Required
		// Token or Associated Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 1 "token_owner": Read-only, Non-signer, Optional
		// Owner of the token account
		accounts__.Append(solanago.NewAccountMeta(tokenOwnerAccount, false, false))
		// Account 2 "metadata": Read-only, Non-signer, Required
		// Metadata account (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, false, false))
		// Account 3 "master_edition": Writable, Non-signer, Optional
		// Master Edition account
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 4 "token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 5 "mint": Writable, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 6 "authority": Read-only, Signer, Required
		// (Mint or Update) authority
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 7 "delegate_record": Read-only, Non-signer, Optional
		// Metadata delegate record
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, false, false))
		// Account 8 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 9 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 10 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 11 "spl_token_program": Read-only, Non-signer, Required
		// SPL Token program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 12 "spl_ata_program": Read-only, Non-signer, Required
		// SPL Associated Token Account program
		accounts__.Append(solanago.NewAccountMeta(splAtaProgramAccount, false, false))
		// Account 13 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 14 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "delegate" instruction.
func NewDelegateInstruction(
	// Params:
	delegateArgsParam DelegateArgs,

	// Accounts:
	delegateRecordAccount solanago.PublicKey,
	delegateAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Delegate[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `delegateArgsParam`:
		{
			err := EncodeDelegateArgs(enc__, delegateArgsParam)
			if err != nil {
				return nil, newFieldError("delegateArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "delegate_record": Writable, Non-signer, Optional
		// Delegate record account
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, true, false))
		// Account 1 "delegate": Read-only, Non-signer, Required
		// Owner of the delegated account
		accounts__.Append(solanago.NewAccountMeta(delegateAccount, false, false))
		// Account 2 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 3 "master_edition": Read-only, Non-signer, Optional
		// Master Edition account
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, false, false))
		// Account 4 "token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 5 "mint": Read-only, Non-signer, Required
		// Mint of metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 6 "token": Writable, Non-signer, Optional
		// Token account of mint
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 7 "authority": Read-only, Signer, Required
		// Update authority or token owner
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 8 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 9 "system_program": Read-only, Non-signer, Required
		// System Program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 10 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 11 "spl_token_program": Read-only, Non-signer, Optional
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 12 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 13 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "revoke" instruction.
func NewRevokeInstruction(
	// Params:
	revokeArgsParam RevokeArgs,

	// Accounts:
	delegateRecordAccount solanago.PublicKey,
	delegateAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Revoke[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `revokeArgsParam`:
		err = enc__.Encode(revokeArgsParam)
		if err != nil {
			return nil, newFieldError("revokeArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "delegate_record": Writable, Non-signer, Optional
		// Delegate record account
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, true, false))
		// Account 1 "delegate": Read-only, Non-signer, Required
		// Owner of the delegated account
		accounts__.Append(solanago.NewAccountMeta(delegateAccount, false, false))
		// Account 2 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 3 "master_edition": Read-only, Non-signer, Optional
		// Master Edition account
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, false, false))
		// Account 4 "token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 5 "mint": Read-only, Non-signer, Required
		// Mint of metadata
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 6 "token": Writable, Non-signer, Optional
		// Token account of mint
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 7 "authority": Read-only, Signer, Required
		// Update authority or token owner
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 8 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 9 "system_program": Read-only, Non-signer, Required
		// System Program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 10 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 11 "spl_token_program": Read-only, Non-signer, Optional
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 12 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 13 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "lock" instruction.
func NewLockInstruction(
	// Params:
	lockArgsParam LockArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	tokenOwnerAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Lock[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `lockArgsParam`:
		{
			err := EncodeLockArgs(enc__, lockArgsParam)
			if err != nil {
				return nil, newFieldError("lockArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Delegate or freeze authority
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "token_owner": Read-only, Non-signer, Optional
		// Token owner account
		accounts__.Append(solanago.NewAccountMeta(tokenOwnerAccount, false, false))
		// Account 2 "token": Writable, Non-signer, Required
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 3 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 4 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 5 "edition": Read-only, Non-signer, Optional
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 6 "token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 7 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 8 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 9 "sysvar_instructions": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 10 "spl_token_program": Read-only, Non-signer, Optional
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 11 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 12 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "unlock" instruction.
func NewUnlockInstruction(
	// Params:
	unlockArgsParam UnlockArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	tokenOwnerAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Unlock[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `unlockArgsParam`:
		{
			err := EncodeUnlockArgs(enc__, unlockArgsParam)
			if err != nil {
				return nil, newFieldError("unlockArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Delegate or freeze authority
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "token_owner": Read-only, Non-signer, Optional
		// Token owner account
		accounts__.Append(solanago.NewAccountMeta(tokenOwnerAccount, false, false))
		// Account 2 "token": Writable, Non-signer, Required
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 3 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 4 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 5 "edition": Read-only, Non-signer, Optional
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 6 "token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 7 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 8 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 9 "sysvar_instructions": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 10 "spl_token_program": Read-only, Non-signer, Optional
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 11 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 12 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "migrate" instruction.
func NewMigrateInstruction(
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	tokenOwnerAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	collectionMetadataAccount solanago.PublicKey,
	delegateRecordAccount solanago.PublicKey,
	tokenRecordAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "edition": Writable, Non-signer, Required
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 2 "token": Writable, Non-signer, Required
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 3 "token_owner": Read-only, Non-signer, Required
		// Token account owner
		accounts__.Append(solanago.NewAccountMeta(tokenOwnerAccount, false, false))
		// Account 4 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 5 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 6 "authority": Read-only, Signer, Required
		// Update authority
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 7 "collection_metadata": Read-only, Non-signer, Required
		// Collection metadata account
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, false, false))
		// Account 8 "delegate_record": Read-only, Non-signer, Required
		// Delegate record account
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, false, false))
		// Account 9 "token_record": Writable, Non-signer, Required
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(tokenRecordAccount, true, false))
		// Account 10 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 11 "sysvar_instructions": Read-only, Non-signer, Required
		// Instruction sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 12 "spl_token_program": Read-only, Non-signer, Required
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 13 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 14 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "transfer" instruction.
func NewTransferInstruction(
	// Params:
	transferArgsParam TransferArgs,

	// Accounts:
	tokenAccount solanago.PublicKey,
	tokenOwnerAccount solanago.PublicKey,
	destinationAccount solanago.PublicKey,
	destinationOwnerAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	ownerTokenRecordAccount solanago.PublicKey,
	destinationTokenRecordAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	splAtaProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Transfer[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `transferArgsParam`:
		{
			err := EncodeTransferArgs(enc__, transferArgsParam)
			if err != nil {
				return nil, newFieldError("transferArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "token": Writable, Non-signer, Required
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 1 "token_owner": Read-only, Non-signer, Required
		// Token account owner
		accounts__.Append(solanago.NewAccountMeta(tokenOwnerAccount, false, false))
		// Account 2 "destination": Writable, Non-signer, Required
		// Destination token account
		accounts__.Append(solanago.NewAccountMeta(destinationAccount, true, false))
		// Account 3 "destination_owner": Read-only, Non-signer, Required
		// Destination token account owner
		accounts__.Append(solanago.NewAccountMeta(destinationOwnerAccount, false, false))
		// Account 4 "mint": Read-only, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 5 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 6 "edition": Read-only, Non-signer, Optional
		// Edition of token asset
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 7 "owner_token_record": Writable, Non-signer, Optional
		// Owner token record account
		accounts__.Append(solanago.NewAccountMeta(ownerTokenRecordAccount, true, false))
		// Account 8 "destination_token_record": Writable, Non-signer, Optional
		// Destination token record account
		accounts__.Append(solanago.NewAccountMeta(destinationTokenRecordAccount, true, false))
		// Account 9 "authority": Read-only, Signer, Required
		// Transfer authority (token owner or delegate)
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 10 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 11 "system_program": Read-only, Non-signer, Required
		// System Program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 12 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 13 "spl_token_program": Read-only, Non-signer, Required
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 14 "spl_ata_program": Read-only, Non-signer, Required
		// SPL Associated Token Account program
		accounts__.Append(solanago.NewAccountMeta(splAtaProgramAccount, false, false))
		// Account 15 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 16 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "update" instruction.
func NewUpdateInstruction(
	// Params:
	updateArgsParam UpdateArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	delegateRecordAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Update[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `updateArgsParam`:
		{
			err := EncodeUpdateArgs(enc__, updateArgsParam)
			if err != nil {
				return nil, newFieldError("updateArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "delegate_record": Read-only, Non-signer, Optional
		// Delegate record PDA
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, false, false))
		// Account 2 "token": Read-only, Non-signer, Optional
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, false, false))
		// Account 3 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 4 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 5 "edition": Read-only, Non-signer, Optional
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, false, false))
		// Account 6 "payer": Writable, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 7 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 8 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 9 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 10 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "use" instruction.
func NewUseInstruction(
	// Params:
	useArgsParam UseArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	delegateRecordAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	authorizationRulesProgramAccount solanago.PublicKey,
	authorizationRulesAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Use[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `useArgsParam`:
		{
			err := EncodeUseArgs(enc__, useArgsParam)
			if err != nil {
				return nil, newFieldError("useArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Token owner or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "delegate_record": Writable, Non-signer, Optional
		// Delegate record PDA
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, true, false))
		// Account 2 "token": Writable, Non-signer, Optional
		// Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, true, false))
		// Account 3 "mint": Read-only, Non-signer, Required
		// Mint account
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 4 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 5 "edition": Writable, Non-signer, Optional
		// Edition account
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 6 "payer": Read-only, Signer, Required
		// Payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, false, true))
		// Account 7 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 8 "sysvar_instructions": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 9 "spl_token_program": Read-only, Non-signer, Optional
		// SPL Token Program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 10 "authorization_rules_program": Read-only, Non-signer, Optional
		// Token Authorization Rules Program
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesProgramAccount, false, false))
		// Account 11 "authorization_rules": Read-only, Non-signer, Optional
		// Token Authorization Rules account
		accounts__.Append(solanago.NewAccountMeta(authorizationRulesAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "verify" instruction.
func NewVerifyInstruction(
	// Params:
	verificationArgsParam VerificationArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	delegateRecordAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionMetadataAccount solanago.PublicKey,
	collectionMasterEditionAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Verify[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `verificationArgsParam`:
		err = enc__.Encode(verificationArgsParam)
		if err != nil {
			return nil, newFieldError("verificationArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Creator to verify, collection update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "delegate_record": Read-only, Non-signer, Optional
		// Delegate record PDA
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, false, false))
		// Account 2 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 3 "collection_mint": Read-only, Non-signer, Optional
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 4 "collection_metadata": Writable, Non-signer, Optional
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, true, false))
		// Account 5 "collection_master_edition": Read-only, Non-signer, Optional
		// Master Edition Account of the Collection Token
		accounts__.Append(solanago.NewAccountMeta(collectionMasterEditionAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 7 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "unverify" instruction.
func NewUnverifyInstruction(
	// Params:
	verificationArgsParam VerificationArgs,

	// Accounts:
	authorityAccount solanago.PublicKey,
	delegateRecordAccount solanago.PublicKey,
	metadataAccount solanago.PublicKey,
	collectionMintAccount solanago.PublicKey,
	collectionMetadataAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Unverify[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `verificationArgsParam`:
		err = enc__.Encode(verificationArgsParam)
		if err != nil {
			return nil, newFieldError("verificationArgsParam", err)
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Creator to verify, collection (or metadata if parent burned) update authority or delegate
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "delegate_record": Read-only, Non-signer, Optional
		// Delegate record PDA
		accounts__.Append(solanago.NewAccountMeta(delegateRecordAccount, false, false))
		// Account 2 "metadata": Writable, Non-signer, Required
		// Metadata account
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 3 "collection_mint": Read-only, Non-signer, Optional
		// Mint of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMintAccount, false, false))
		// Account 4 "collection_metadata": Writable, Non-signer, Optional
		// Metadata Account of the Collection
		accounts__.Append(solanago.NewAccountMeta(collectionMetadataAccount, true, false))
		// Account 5 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
		// Account 6 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "collect" instruction.
func NewCollectInstruction(
	authorityAccount solanago.PublicKey,
	recipientAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "authority": Read-only, Signer, Required
		// Authority to collect fees
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 1 "recipient": Read-only, Non-signer, Required
		// The account to transfer collected fees to
		accounts__.Append(solanago.NewAccountMeta(recipientAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "print" instruction.
func NewPrintInstruction(
	// Params:
	printArgsParam PrintArgs,

	// Accounts:
	editionMetadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	editionMintAccount solanago.PublicKey,
	editionTokenAccountOwnerAccount solanago.PublicKey,
	editionTokenAccountAccount solanago.PublicKey,
	editionMintAuthorityAccount solanago.PublicKey,
	editionTokenRecordAccount solanago.PublicKey,
	masterEditionAccount solanago.PublicKey,
	editionMarkerPdaAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	masterTokenAccountOwnerAccount solanago.PublicKey,
	masterTokenAccountAccount solanago.PublicKey,
	masterMetadataAccount solanago.PublicKey,
	updateAuthorityAccount solanago.PublicKey,
	splTokenProgramAccount solanago.PublicKey,
	splAtaProgramAccount solanago.PublicKey,
	sysvarInstructionsAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	buf__ := new(bytes.Buffer)
	enc__ := binary.NewBorshEncoder(buf__)

	// Encode the instruction discriminator.
	err := enc__.WriteBytes(Instruction_Print[:], false)
	if err != nil {
		return nil, fmt.Errorf("failed to write instruction discriminator: %w", err)
	}
	{
		// Serialize `printArgsParam`:
		{
			err := EncodePrintArgs(enc__, printArgsParam)
			if err != nil {
				return nil, newFieldError("printArgsParam", err)
			}
		}
	}
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "edition_metadata": Writable, Non-signer, Required
		// New Metadata key (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(editionMetadataAccount, true, false))
		// Account 1 "edition": Writable, Non-signer, Required
		// New Edition (pda of ['metadata', program id, mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 2 "edition_mint": Writable, Non-signer, Required
		// Mint of new token - THIS WILL TRANSFER AUTHORITY AWAY FROM THIS KEY
		accounts__.Append(solanago.NewAccountMeta(editionMintAccount, true, false))
		// Account 3 "edition_token_account_owner": Read-only, Non-signer, Required
		// Owner of the token account of new token
		accounts__.Append(solanago.NewAccountMeta(editionTokenAccountOwnerAccount, false, false))
		// Account 4 "edition_token_account": Writable, Non-signer, Required
		// Token account of new token
		accounts__.Append(solanago.NewAccountMeta(editionTokenAccountAccount, true, false))
		// Account 5 "edition_mint_authority": Read-only, Signer, Required
		// Mint authority of new mint
		accounts__.Append(solanago.NewAccountMeta(editionMintAuthorityAccount, false, true))
		// Account 6 "edition_token_record": Writable, Non-signer, Optional
		// Token record account
		accounts__.Append(solanago.NewAccountMeta(editionTokenRecordAccount, true, false))
		// Account 7 "master_edition": Writable, Non-signer, Required
		// Master Record Edition V2 (pda of ['metadata', program id, master metadata mint id, 'edition'])
		accounts__.Append(solanago.NewAccountMeta(masterEditionAccount, true, false))
		// Account 8 "edition_marker_pda": Writable, Non-signer, Required
		// Edition pda to mark creation - will be checked for pre-existence. (pda of ['metadata', program id, master metadata mint id, 'edition', edition_number]) where edition_number is NOT the edition number you pass in args but actually edition_number = floor(edition/EDITION_MARKER_BIT_SIZE).
		accounts__.Append(solanago.NewAccountMeta(editionMarkerPdaAccount, true, false))
		// Account 9 "payer": Writable, Signer, Required
		// payer
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, true))
		// Account 10 "master_token_account_owner": Read-only, Non-signer, Required
		// owner of token account containing master token
		accounts__.Append(solanago.NewAccountMeta(masterTokenAccountOwnerAccount, false, false))
		// Account 11 "master_token_account": Read-only, Non-signer, Required
		// token account containing token from master metadata mint
		accounts__.Append(solanago.NewAccountMeta(masterTokenAccountAccount, false, false))
		// Account 12 "master_metadata": Read-only, Non-signer, Required
		// Master record metadata account
		accounts__.Append(solanago.NewAccountMeta(masterMetadataAccount, false, false))
		// Account 13 "update_authority": Read-only, Non-signer, Required
		// The update authority of the master edition.
		accounts__.Append(solanago.NewAccountMeta(updateAuthorityAccount, false, false))
		// Account 14 "spl_token_program": Read-only, Non-signer, Required
		// Token program
		accounts__.Append(solanago.NewAccountMeta(splTokenProgramAccount, false, false))
		// Account 15 "spl_ata_program": Read-only, Non-signer, Required
		// SPL Associated Token Account program
		accounts__.Append(solanago.NewAccountMeta(splAtaProgramAccount, false, false))
		// Account 16 "sysvar_instructions": Read-only, Non-signer, Required
		// Instructions sysvar account
		accounts__.Append(solanago.NewAccountMeta(sysvarInstructionsAccount, false, false))
		// Account 17 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		buf__.Bytes(),
	), nil
}

// Builds a "resize" instruction.
func NewResizeInstruction(
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	payerAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	tokenAccount solanago.PublicKey,
	systemProgramAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// The metadata account of the digital asset
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "edition": Writable, Non-signer, Required
		// The master edition or edition account of the digital asset, an uninitialized account for fungible assets
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 2 "mint": Read-only, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, false, false))
		// Account 3 "payer": Writable, Non-signer, Required
		// The recipient of the excess rent and authority if the authority account is not present
		accounts__.Append(solanago.NewAccountMeta(payerAccount, true, false))
		// Account 4 "authority": Read-only, Signer, Optional
		// Owner of the asset for (p)NFTs, or mint authority for fungible assets, if different from the payer
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 5 "token": Read-only, Non-signer, Optional
		// Token or Associated Token account
		accounts__.Append(solanago.NewAccountMeta(tokenAccount, false, false))
		// Account 6 "system_program": Read-only, Non-signer, Required
		// System program
		accounts__.Append(solanago.NewAccountMeta(systemProgramAccount, false, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

// Builds a "close_accounts" instruction.
func NewCloseAccountsInstruction(
	metadataAccount solanago.PublicKey,
	editionAccount solanago.PublicKey,
	mintAccount solanago.PublicKey,
	authorityAccount solanago.PublicKey,
	destinationAccount solanago.PublicKey,
) (solanago.Instruction, error) {
	accounts__ := solanago.AccountMetaSlice{}

	// Add the accounts to the instruction.
	{
		// Account 0 "metadata": Writable, Non-signer, Required
		// Metadata (pda of ['metadata', program id, mint id])
		accounts__.Append(solanago.NewAccountMeta(metadataAccount, true, false))
		// Account 1 "edition": Writable, Non-signer, Required
		// Edition of the asset
		accounts__.Append(solanago.NewAccountMeta(editionAccount, true, false))
		// Account 2 "mint": Writable, Non-signer, Required
		// Mint of token asset
		accounts__.Append(solanago.NewAccountMeta(mintAccount, true, false))
		// Account 3 "authority": Read-only, Signer, Required
		// Authority to close ownerless accounts
		accounts__.Append(solanago.NewAccountMeta(authorityAccount, false, true))
		// Account 4 "destination": Writable, Non-signer, Required
		// The destination account that will receive the rent.
		accounts__.Append(solanago.NewAccountMeta(destinationAccount, true, false))
	}

	// Create the instruction.
	return solanago.NewInstruction(
		ProgramID,
		accounts__,
		nil,
	), nil
}

type CreateMetadataAccountInstruction struct {

	// Accounts:
	Metadata            solanago.PublicKey `json:"metadata"`
	MetadataWritable    bool               `json:"metadata_writable"`
	Mint                solanago.PublicKey `json:"mint"`
	MintAuthority       solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner bool               `json:"mint_authority_signer"`
	Payer               solanago.PublicKey `json:"payer"`
	PayerWritable       bool               `json:"payer_writable"`
	PayerSigner         bool               `json:"payer_signer"`
	UpdateAuthority     solanago.PublicKey `json:"update_authority"`
	SystemProgram       solanago.PublicKey `json:"system_program"`
	Rent                solanago.PublicKey `json:"rent"`
}

func (obj *CreateMetadataAccountInstruction) GetDiscriminator() []byte {
	return Instruction_CreateMetadataAccount[:]
}

// UnmarshalWithDecoder unmarshals the CreateMetadataAccountInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateMetadataAccountInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateMetadataAccountInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateMetadataAccount[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateMetadataAccountInstruction", Instruction_CreateMetadataAccount[:], discriminator)
	}
	return nil
}

func (obj *CreateMetadataAccountInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateMetadataAccountInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateMetadataAccountInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the CreateMetadataAccountInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateMetadataAccountInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateMetadataAccountInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateMetadataAccountInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateMetadataAccountInstruction(buf []byte) (*CreateMetadataAccountInstruction, error) {
	obj := new(CreateMetadataAccountInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateMetadataAccountInstruction struct {

	// Accounts:
	Metadata              solanago.PublicKey `json:"metadata"`
	MetadataWritable      bool               `json:"metadata_writable"`
	UpdateAuthority       solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner bool               `json:"update_authority_signer"`
}

func (obj *UpdateMetadataAccountInstruction) GetDiscriminator() []byte {
	return Instruction_UpdateMetadataAccount[:]
}

// UnmarshalWithDecoder unmarshals the UpdateMetadataAccountInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateMetadataAccountInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateMetadataAccountInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateMetadataAccount[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateMetadataAccountInstruction", Instruction_UpdateMetadataAccount[:], discriminator)
	}
	return nil
}

func (obj *UpdateMetadataAccountInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateMetadataAccountInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateMetadataAccountInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.UpdateAuthority)
	return keys
}

// Unmarshal unmarshals the UpdateMetadataAccountInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateMetadataAccountInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateMetadataAccountInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateMetadataAccountInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateMetadataAccountInstruction(buf []byte) (*UpdateMetadataAccountInstruction, error) {
	obj := new(UpdateMetadataAccountInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DeprecatedCreateMasterEditionInstruction struct {

	// Accounts:
	Edition                                         solanago.PublicKey `json:"edition"`
	EditionWritable                                 bool               `json:"edition_writable"`
	Mint                                            solanago.PublicKey `json:"mint"`
	MintWritable                                    bool               `json:"mint_writable"`
	PrintingMint                                    solanago.PublicKey `json:"printing_mint"`
	PrintingMintWritable                            bool               `json:"printing_mint_writable"`
	OneTimePrintingAuthorizationMint                solanago.PublicKey `json:"one_time_printing_authorization_mint"`
	OneTimePrintingAuthorizationMintWritable        bool               `json:"one_time_printing_authorization_mint_writable"`
	UpdateAuthority                                 solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner                           bool               `json:"update_authority_signer"`
	PrintingMintAuthority                           solanago.PublicKey `json:"printing_mint_authority"`
	PrintingMintAuthoritySigner                     bool               `json:"printing_mint_authority_signer"`
	MintAuthority                                   solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner                             bool               `json:"mint_authority_signer"`
	Metadata                                        solanago.PublicKey `json:"metadata"`
	Payer                                           solanago.PublicKey `json:"payer"`
	PayerSigner                                     bool               `json:"payer_signer"`
	TokenProgram                                    solanago.PublicKey `json:"token_program"`
	SystemProgram                                   solanago.PublicKey `json:"system_program"`
	Rent                                            solanago.PublicKey `json:"rent"`
	OneTimePrintingAuthorizationMintAuthority       solanago.PublicKey `json:"one_time_printing_authorization_mint_authority"`
	OneTimePrintingAuthorizationMintAuthoritySigner bool               `json:"one_time_printing_authorization_mint_authority_signer"`
}

func (obj *DeprecatedCreateMasterEditionInstruction) GetDiscriminator() []byte {
	return Instruction_DeprecatedCreateMasterEdition[:]
}

// UnmarshalWithDecoder unmarshals the DeprecatedCreateMasterEditionInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DeprecatedCreateMasterEditionInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DeprecatedCreateMasterEditionInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DeprecatedCreateMasterEdition[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DeprecatedCreateMasterEditionInstruction", Instruction_DeprecatedCreateMasterEdition[:], discriminator)
	}
	return nil
}

func (obj *DeprecatedCreateMasterEditionInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from printing_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "printing_mint", err)
	}
	indices = append(indices, index)
	// Decode from one_time_printing_authorization_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "one_time_printing_authorization_mint", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from printing_mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "printing_mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	// Decode from one_time_printing_authorization_mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "one_time_printing_authorization_mint_authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DeprecatedCreateMasterEditionInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 13 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 13, len(indices))
	}
	indexOffset := 0
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set printing_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "printing_mint", len(accountKeys)-1)
	}
	obj.PrintingMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set one_time_printing_authorization_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "one_time_printing_authorization_mint", len(accountKeys)-1)
	}
	obj.OneTimePrintingAuthorizationMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set printing_mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "printing_mint_authority", len(accountKeys)-1)
	}
	obj.PrintingMintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set one_time_printing_authorization_mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "one_time_printing_authorization_mint_authority", len(accountKeys)-1)
	}
	obj.OneTimePrintingAuthorizationMintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DeprecatedCreateMasterEditionInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.PrintingMint)
	keys = append(keys, obj.OneTimePrintingAuthorizationMint)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.PrintingMintAuthority)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	keys = append(keys, obj.OneTimePrintingAuthorizationMintAuthority)
	return keys
}

// Unmarshal unmarshals the DeprecatedCreateMasterEditionInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DeprecatedCreateMasterEditionInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DeprecatedCreateMasterEditionInstruction: %w", err)
	}
	return nil
}

// UnmarshalDeprecatedCreateMasterEditionInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDeprecatedCreateMasterEditionInstruction(buf []byte) (*DeprecatedCreateMasterEditionInstruction, error) {
	obj := new(DeprecatedCreateMasterEditionInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction struct {

	// Accounts:
	Metadata                   solanago.PublicKey `json:"metadata"`
	MetadataWritable           bool               `json:"metadata_writable"`
	Edition                    solanago.PublicKey `json:"edition"`
	EditionWritable            bool               `json:"edition_writable"`
	MasterEdition              solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable      bool               `json:"master_edition_writable"`
	Mint                       solanago.PublicKey `json:"mint"`
	MintWritable               bool               `json:"mint_writable"`
	MintAuthority              solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner        bool               `json:"mint_authority_signer"`
	PrintingMint               solanago.PublicKey `json:"printing_mint"`
	PrintingMintWritable       bool               `json:"printing_mint_writable"`
	MasterTokenAccount         solanago.PublicKey `json:"master_token_account"`
	MasterTokenAccountWritable bool               `json:"master_token_account_writable"`
	EditionMarker              solanago.PublicKey `json:"edition_marker"`
	EditionMarkerWritable      bool               `json:"edition_marker_writable"`
	BurnAuthority              solanago.PublicKey `json:"burn_authority"`
	BurnAuthoritySigner        bool               `json:"burn_authority_signer"`
	Payer                      solanago.PublicKey `json:"payer"`
	PayerSigner                bool               `json:"payer_signer"`
	MasterUpdateAuthority      solanago.PublicKey `json:"master_update_authority"`
	MasterMetadata             solanago.PublicKey `json:"master_metadata"`
	TokenProgram               solanago.PublicKey `json:"token_program"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	Rent                       solanago.PublicKey `json:"rent"`
	ReservationList            solanago.PublicKey `json:"reservation_list"`
	ReservationListWritable    bool               `json:"reservation_list_writable"`
	ReservationListOptional    bool               `json:"reservation_list_optional"`
}

func (obj *DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction) GetDiscriminator() []byte {
	return Instruction_DeprecatedMintNewEditionFromMasterEditionViaPrintingToken[:]
}

// UnmarshalWithDecoder unmarshals the DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DeprecatedMintNewEditionFromMasterEditionViaPrintingToken[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction", Instruction_DeprecatedMintNewEditionFromMasterEditionViaPrintingToken[:], discriminator)
	}
	return nil
}

func (obj *DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from printing_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "printing_mint", err)
	}
	indices = append(indices, index)
	// Decode from master_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_token_account", err)
	}
	indices = append(indices, index)
	// Decode from edition_marker account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_marker", err)
	}
	indices = append(indices, index)
	// Decode from burn_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "burn_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from master_update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_update_authority", err)
	}
	indices = append(indices, index)
	// Decode from master_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	// Decode from reservation_list account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "reservation_list", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 16 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 16, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set printing_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "printing_mint", len(accountKeys)-1)
	}
	obj.PrintingMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_token_account", len(accountKeys)-1)
	}
	obj.MasterTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_marker account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_marker", len(accountKeys)-1)
	}
	obj.EditionMarker = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set burn_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "burn_authority", len(accountKeys)-1)
	}
	obj.BurnAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_update_authority", len(accountKeys)-1)
	}
	obj.MasterUpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_metadata", len(accountKeys)-1)
	}
	obj.MasterMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set reservation_list account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "reservation_list", len(accountKeys)-1)
	}
	obj.ReservationList = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.PrintingMint)
	keys = append(keys, obj.MasterTokenAccount)
	keys = append(keys, obj.EditionMarker)
	keys = append(keys, obj.BurnAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.MasterUpdateAuthority)
	keys = append(keys, obj.MasterMetadata)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	keys = append(keys, obj.ReservationList)
	return keys
}

// Unmarshal unmarshals the DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction: %w", err)
	}
	return nil
}

// UnmarshalDeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction(buf []byte) (*DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction, error) {
	obj := new(DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdatePrimarySaleHappenedViaTokenInstruction struct {

	// Accounts:
	Metadata         solanago.PublicKey `json:"metadata"`
	MetadataWritable bool               `json:"metadata_writable"`
	Owner            solanago.PublicKey `json:"owner"`
	OwnerSigner      bool               `json:"owner_signer"`
	Token            solanago.PublicKey `json:"token"`
}

func (obj *UpdatePrimarySaleHappenedViaTokenInstruction) GetDiscriminator() []byte {
	return Instruction_UpdatePrimarySaleHappenedViaToken[:]
}

// UnmarshalWithDecoder unmarshals the UpdatePrimarySaleHappenedViaTokenInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdatePrimarySaleHappenedViaTokenInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdatePrimarySaleHappenedViaTokenInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdatePrimarySaleHappenedViaToken[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdatePrimarySaleHappenedViaTokenInstruction", Instruction_UpdatePrimarySaleHappenedViaToken[:], discriminator)
	}
	return nil
}

func (obj *UpdatePrimarySaleHappenedViaTokenInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdatePrimarySaleHappenedViaTokenInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 3 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 3, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdatePrimarySaleHappenedViaTokenInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.Token)
	return keys
}

// Unmarshal unmarshals the UpdatePrimarySaleHappenedViaTokenInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdatePrimarySaleHappenedViaTokenInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdatePrimarySaleHappenedViaTokenInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpdatePrimarySaleHappenedViaTokenInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdatePrimarySaleHappenedViaTokenInstruction(buf []byte) (*UpdatePrimarySaleHappenedViaTokenInstruction, error) {
	obj := new(UpdatePrimarySaleHappenedViaTokenInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DeprecatedSetReservationListInstruction struct {

	// Accounts:
	MasterEdition           solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable   bool               `json:"master_edition_writable"`
	ReservationList         solanago.PublicKey `json:"reservation_list"`
	ReservationListWritable bool               `json:"reservation_list_writable"`
	Resource                solanago.PublicKey `json:"resource"`
	ResourceSigner          bool               `json:"resource_signer"`
}

func (obj *DeprecatedSetReservationListInstruction) GetDiscriminator() []byte {
	return Instruction_DeprecatedSetReservationList[:]
}

// UnmarshalWithDecoder unmarshals the DeprecatedSetReservationListInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DeprecatedSetReservationListInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DeprecatedSetReservationListInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DeprecatedSetReservationList[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DeprecatedSetReservationListInstruction", Instruction_DeprecatedSetReservationList[:], discriminator)
	}
	return nil
}

func (obj *DeprecatedSetReservationListInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from reservation_list account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "reservation_list", err)
	}
	indices = append(indices, index)
	// Decode from resource account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "resource", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DeprecatedSetReservationListInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 3 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 3, len(indices))
	}
	indexOffset := 0
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set reservation_list account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "reservation_list", len(accountKeys)-1)
	}
	obj.ReservationList = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set resource account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "resource", len(accountKeys)-1)
	}
	obj.Resource = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DeprecatedSetReservationListInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.ReservationList)
	keys = append(keys, obj.Resource)
	return keys
}

// Unmarshal unmarshals the DeprecatedSetReservationListInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DeprecatedSetReservationListInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DeprecatedSetReservationListInstruction: %w", err)
	}
	return nil
}

// UnmarshalDeprecatedSetReservationListInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDeprecatedSetReservationListInstruction(buf []byte) (*DeprecatedSetReservationListInstruction, error) {
	obj := new(DeprecatedSetReservationListInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DeprecatedCreateReservationListInstruction struct {

	// Accounts:
	ReservationList         solanago.PublicKey `json:"reservation_list"`
	ReservationListWritable bool               `json:"reservation_list_writable"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerSigner             bool               `json:"payer_signer"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner   bool               `json:"update_authority_signer"`
	MasterEdition           solanago.PublicKey `json:"master_edition"`
	Resource                solanago.PublicKey `json:"resource"`
	Metadata                solanago.PublicKey `json:"metadata"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
	Rent                    solanago.PublicKey `json:"rent"`
}

func (obj *DeprecatedCreateReservationListInstruction) GetDiscriminator() []byte {
	return Instruction_DeprecatedCreateReservationList[:]
}

// UnmarshalWithDecoder unmarshals the DeprecatedCreateReservationListInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DeprecatedCreateReservationListInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DeprecatedCreateReservationListInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DeprecatedCreateReservationList[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DeprecatedCreateReservationListInstruction", Instruction_DeprecatedCreateReservationList[:], discriminator)
	}
	return nil
}

func (obj *DeprecatedCreateReservationListInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from reservation_list account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "reservation_list", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from resource account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "resource", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DeprecatedCreateReservationListInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set reservation_list account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "reservation_list", len(accountKeys)-1)
	}
	obj.ReservationList = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set resource account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "resource", len(accountKeys)-1)
	}
	obj.Resource = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DeprecatedCreateReservationListInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.ReservationList)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.Resource)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the DeprecatedCreateReservationListInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DeprecatedCreateReservationListInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DeprecatedCreateReservationListInstruction: %w", err)
	}
	return nil
}

// UnmarshalDeprecatedCreateReservationListInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDeprecatedCreateReservationListInstruction(buf []byte) (*DeprecatedCreateReservationListInstruction, error) {
	obj := new(DeprecatedCreateReservationListInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SignMetadataInstruction struct {

	// Accounts:
	Metadata         solanago.PublicKey `json:"metadata"`
	MetadataWritable bool               `json:"metadata_writable"`
	Creator          solanago.PublicKey `json:"creator"`
	CreatorSigner    bool               `json:"creator_signer"`
}

func (obj *SignMetadataInstruction) GetDiscriminator() []byte {
	return Instruction_SignMetadata[:]
}

// UnmarshalWithDecoder unmarshals the SignMetadataInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SignMetadataInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SignMetadataInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_SignMetadata[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "SignMetadataInstruction", Instruction_SignMetadata[:], discriminator)
	}
	return nil
}

func (obj *SignMetadataInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from creator account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "creator", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SignMetadataInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set creator account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "creator", len(accountKeys)-1)
	}
	obj.Creator = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SignMetadataInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Creator)
	return keys
}

// Unmarshal unmarshals the SignMetadataInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SignMetadataInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SignMetadataInstruction: %w", err)
	}
	return nil
}

// UnmarshalSignMetadataInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSignMetadataInstruction(buf []byte) (*SignMetadataInstruction, error) {
	obj := new(SignMetadataInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DeprecatedMintPrintingTokensViaTokenInstruction struct {

	// Accounts:
	Destination                              solanago.PublicKey `json:"destination"`
	DestinationWritable                      bool               `json:"destination_writable"`
	Token                                    solanago.PublicKey `json:"token"`
	TokenWritable                            bool               `json:"token_writable"`
	OneTimePrintingAuthorizationMint         solanago.PublicKey `json:"one_time_printing_authorization_mint"`
	OneTimePrintingAuthorizationMintWritable bool               `json:"one_time_printing_authorization_mint_writable"`
	PrintingMint                             solanago.PublicKey `json:"printing_mint"`
	PrintingMintWritable                     bool               `json:"printing_mint_writable"`
	BurnAuthority                            solanago.PublicKey `json:"burn_authority"`
	BurnAuthoritySigner                      bool               `json:"burn_authority_signer"`
	Metadata                                 solanago.PublicKey `json:"metadata"`
	MasterEdition                            solanago.PublicKey `json:"master_edition"`
	TokenProgram                             solanago.PublicKey `json:"token_program"`
	Rent                                     solanago.PublicKey `json:"rent"`
}

func (obj *DeprecatedMintPrintingTokensViaTokenInstruction) GetDiscriminator() []byte {
	return Instruction_DeprecatedMintPrintingTokensViaToken[:]
}

// UnmarshalWithDecoder unmarshals the DeprecatedMintPrintingTokensViaTokenInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DeprecatedMintPrintingTokensViaTokenInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DeprecatedMintPrintingTokensViaTokenInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DeprecatedMintPrintingTokensViaToken[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DeprecatedMintPrintingTokensViaTokenInstruction", Instruction_DeprecatedMintPrintingTokensViaToken[:], discriminator)
	}
	return nil
}

func (obj *DeprecatedMintPrintingTokensViaTokenInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from destination account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "destination", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from one_time_printing_authorization_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "one_time_printing_authorization_mint", err)
	}
	indices = append(indices, index)
	// Decode from printing_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "printing_mint", err)
	}
	indices = append(indices, index)
	// Decode from burn_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "burn_authority", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DeprecatedMintPrintingTokensViaTokenInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 9 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 9, len(indices))
	}
	indexOffset := 0
	// Set destination account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "destination", len(accountKeys)-1)
	}
	obj.Destination = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set one_time_printing_authorization_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "one_time_printing_authorization_mint", len(accountKeys)-1)
	}
	obj.OneTimePrintingAuthorizationMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set printing_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "printing_mint", len(accountKeys)-1)
	}
	obj.PrintingMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set burn_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "burn_authority", len(accountKeys)-1)
	}
	obj.BurnAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DeprecatedMintPrintingTokensViaTokenInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Destination)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.OneTimePrintingAuthorizationMint)
	keys = append(keys, obj.PrintingMint)
	keys = append(keys, obj.BurnAuthority)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the DeprecatedMintPrintingTokensViaTokenInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DeprecatedMintPrintingTokensViaTokenInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DeprecatedMintPrintingTokensViaTokenInstruction: %w", err)
	}
	return nil
}

// UnmarshalDeprecatedMintPrintingTokensViaTokenInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDeprecatedMintPrintingTokensViaTokenInstruction(buf []byte) (*DeprecatedMintPrintingTokensViaTokenInstruction, error) {
	obj := new(DeprecatedMintPrintingTokensViaTokenInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DeprecatedMintPrintingTokensInstruction struct {

	// Accounts:
	Destination           solanago.PublicKey `json:"destination"`
	DestinationWritable   bool               `json:"destination_writable"`
	PrintingMint          solanago.PublicKey `json:"printing_mint"`
	PrintingMintWritable  bool               `json:"printing_mint_writable"`
	UpdateAuthority       solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner bool               `json:"update_authority_signer"`
	Metadata              solanago.PublicKey `json:"metadata"`
	MasterEdition         solanago.PublicKey `json:"master_edition"`
	TokenProgram          solanago.PublicKey `json:"token_program"`
	Rent                  solanago.PublicKey `json:"rent"`
}

func (obj *DeprecatedMintPrintingTokensInstruction) GetDiscriminator() []byte {
	return Instruction_DeprecatedMintPrintingTokens[:]
}

// UnmarshalWithDecoder unmarshals the DeprecatedMintPrintingTokensInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DeprecatedMintPrintingTokensInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DeprecatedMintPrintingTokensInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_DeprecatedMintPrintingTokens[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DeprecatedMintPrintingTokensInstruction", Instruction_DeprecatedMintPrintingTokens[:], discriminator)
	}
	return nil
}

func (obj *DeprecatedMintPrintingTokensInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from destination account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "destination", err)
	}
	indices = append(indices, index)
	// Decode from printing_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "printing_mint", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DeprecatedMintPrintingTokensInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set destination account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "destination", len(accountKeys)-1)
	}
	obj.Destination = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set printing_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "printing_mint", len(accountKeys)-1)
	}
	obj.PrintingMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DeprecatedMintPrintingTokensInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Destination)
	keys = append(keys, obj.PrintingMint)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the DeprecatedMintPrintingTokensInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DeprecatedMintPrintingTokensInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DeprecatedMintPrintingTokensInstruction: %w", err)
	}
	return nil
}

// UnmarshalDeprecatedMintPrintingTokensInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDeprecatedMintPrintingTokensInstruction(buf []byte) (*DeprecatedMintPrintingTokensInstruction, error) {
	obj := new(DeprecatedMintPrintingTokensInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateMasterEditionInstruction struct {

	// Accounts:
	Edition               solanago.PublicKey `json:"edition"`
	EditionWritable       bool               `json:"edition_writable"`
	Mint                  solanago.PublicKey `json:"mint"`
	MintWritable          bool               `json:"mint_writable"`
	UpdateAuthority       solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner bool               `json:"update_authority_signer"`
	MintAuthority         solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner   bool               `json:"mint_authority_signer"`
	Payer                 solanago.PublicKey `json:"payer"`
	PayerWritable         bool               `json:"payer_writable"`
	PayerSigner           bool               `json:"payer_signer"`
	Metadata              solanago.PublicKey `json:"metadata"`
	TokenProgram          solanago.PublicKey `json:"token_program"`
	SystemProgram         solanago.PublicKey `json:"system_program"`
	Rent                  solanago.PublicKey `json:"rent"`
}

func (obj *CreateMasterEditionInstruction) GetDiscriminator() []byte {
	return Instruction_CreateMasterEdition[:]
}

// UnmarshalWithDecoder unmarshals the CreateMasterEditionInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateMasterEditionInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateMasterEditionInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateMasterEdition[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateMasterEditionInstruction", Instruction_CreateMasterEdition[:], discriminator)
	}
	return nil
}

func (obj *CreateMasterEditionInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateMasterEditionInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 9 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 9, len(indices))
	}
	indexOffset := 0
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateMasterEditionInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the CreateMasterEditionInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateMasterEditionInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateMasterEditionInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateMasterEditionInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateMasterEditionInstruction(buf []byte) (*CreateMasterEditionInstruction, error) {
	obj := new(CreateMasterEditionInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type MintNewEditionFromMasterEditionViaTokenInstruction struct {
	MintNewEditionFromMasterEditionViaTokenArgs MintNewEditionFromMasterEditionViaTokenArgs `json:"mint_new_edition_from_master_edition_via_token_args"`

	// Accounts:
	NewMetadata                solanago.PublicKey `json:"new_metadata"`
	NewMetadataWritable        bool               `json:"new_metadata_writable"`
	NewEdition                 solanago.PublicKey `json:"new_edition"`
	NewEditionWritable         bool               `json:"new_edition_writable"`
	MasterEdition              solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable      bool               `json:"master_edition_writable"`
	NewMint                    solanago.PublicKey `json:"new_mint"`
	NewMintWritable            bool               `json:"new_mint_writable"`
	EditionMarkPda             solanago.PublicKey `json:"edition_mark_pda"`
	EditionMarkPdaWritable     bool               `json:"edition_mark_pda_writable"`
	NewMintAuthority           solanago.PublicKey `json:"new_mint_authority"`
	NewMintAuthoritySigner     bool               `json:"new_mint_authority_signer"`
	Payer                      solanago.PublicKey `json:"payer"`
	PayerWritable              bool               `json:"payer_writable"`
	PayerSigner                bool               `json:"payer_signer"`
	TokenAccountOwner          solanago.PublicKey `json:"token_account_owner"`
	TokenAccountOwnerSigner    bool               `json:"token_account_owner_signer"`
	TokenAccount               solanago.PublicKey `json:"token_account"`
	NewMetadataUpdateAuthority solanago.PublicKey `json:"new_metadata_update_authority"`
	Metadata                   solanago.PublicKey `json:"metadata"`
	TokenProgram               solanago.PublicKey `json:"token_program"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	Rent                       solanago.PublicKey `json:"rent"`
	RentOptional               bool               `json:"rent_optional"`
}

func (obj *MintNewEditionFromMasterEditionViaTokenInstruction) GetDiscriminator() []byte {
	return Instruction_MintNewEditionFromMasterEditionViaToken[:]
}

// UnmarshalWithDecoder unmarshals the MintNewEditionFromMasterEditionViaTokenInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *MintNewEditionFromMasterEditionViaTokenInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "MintNewEditionFromMasterEditionViaTokenInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_MintNewEditionFromMasterEditionViaToken[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "MintNewEditionFromMasterEditionViaTokenInstruction", Instruction_MintNewEditionFromMasterEditionViaToken[:], discriminator)
	}
	// Deserialize `MintNewEditionFromMasterEditionViaTokenArgs`:
	err = decoder.Decode(&obj.MintNewEditionFromMasterEditionViaTokenArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *MintNewEditionFromMasterEditionViaTokenInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from new_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_metadata", err)
	}
	indices = append(indices, index)
	// Decode from new_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_edition", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from new_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_mint", err)
	}
	indices = append(indices, index)
	// Decode from edition_mark_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_mark_pda", err)
	}
	indices = append(indices, index)
	// Decode from new_mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from token_account_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account_owner", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from new_metadata_update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_metadata_update_authority", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *MintNewEditionFromMasterEditionViaTokenInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 14 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 14, len(indices))
	}
	indexOffset := 0
	// Set new_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_metadata", len(accountKeys)-1)
	}
	obj.NewMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_edition", len(accountKeys)-1)
	}
	obj.NewEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_mint", len(accountKeys)-1)
	}
	obj.NewMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_mark_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_mark_pda", len(accountKeys)-1)
	}
	obj.EditionMarkPda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_mint_authority", len(accountKeys)-1)
	}
	obj.NewMintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account_owner", len(accountKeys)-1)
	}
	obj.TokenAccountOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_metadata_update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_metadata_update_authority", len(accountKeys)-1)
	}
	obj.NewMetadataUpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *MintNewEditionFromMasterEditionViaTokenInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.NewMetadata)
	keys = append(keys, obj.NewEdition)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.NewMint)
	keys = append(keys, obj.EditionMarkPda)
	keys = append(keys, obj.NewMintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.TokenAccountOwner)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.NewMetadataUpdateAuthority)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the MintNewEditionFromMasterEditionViaTokenInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *MintNewEditionFromMasterEditionViaTokenInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling MintNewEditionFromMasterEditionViaTokenInstruction: %w", err)
	}
	return nil
}

// UnmarshalMintNewEditionFromMasterEditionViaTokenInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalMintNewEditionFromMasterEditionViaTokenInstruction(buf []byte) (*MintNewEditionFromMasterEditionViaTokenInstruction, error) {
	obj := new(MintNewEditionFromMasterEditionViaTokenInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ConvertMasterEditionV1ToV2Instruction struct {

	// Accounts:
	MasterEdition         solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable bool               `json:"master_edition_writable"`
	OneTimeAuth           solanago.PublicKey `json:"one_time_auth"`
	OneTimeAuthWritable   bool               `json:"one_time_auth_writable"`
	PrintingMint          solanago.PublicKey `json:"printing_mint"`
	PrintingMintWritable  bool               `json:"printing_mint_writable"`
}

func (obj *ConvertMasterEditionV1ToV2Instruction) GetDiscriminator() []byte {
	return Instruction_ConvertMasterEditionV1ToV2[:]
}

// UnmarshalWithDecoder unmarshals the ConvertMasterEditionV1ToV2Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ConvertMasterEditionV1ToV2Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ConvertMasterEditionV1ToV2Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ConvertMasterEditionV1ToV2[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ConvertMasterEditionV1ToV2Instruction", Instruction_ConvertMasterEditionV1ToV2[:], discriminator)
	}
	return nil
}

func (obj *ConvertMasterEditionV1ToV2Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from one_time_auth account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "one_time_auth", err)
	}
	indices = append(indices, index)
	// Decode from printing_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "printing_mint", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ConvertMasterEditionV1ToV2Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 3 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 3, len(indices))
	}
	indexOffset := 0
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set one_time_auth account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "one_time_auth", len(accountKeys)-1)
	}
	obj.OneTimeAuth = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set printing_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "printing_mint", len(accountKeys)-1)
	}
	obj.PrintingMint = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ConvertMasterEditionV1ToV2Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.OneTimeAuth)
	keys = append(keys, obj.PrintingMint)
	return keys
}

// Unmarshal unmarshals the ConvertMasterEditionV1ToV2Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ConvertMasterEditionV1ToV2Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ConvertMasterEditionV1ToV2Instruction: %w", err)
	}
	return nil
}

// UnmarshalConvertMasterEditionV1ToV2Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalConvertMasterEditionV1ToV2Instruction(buf []byte) (*ConvertMasterEditionV1ToV2Instruction, error) {
	obj := new(ConvertMasterEditionV1ToV2Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type MintNewEditionFromMasterEditionViaVaultProxyInstruction struct {
	MintNewEditionFromMasterEditionViaTokenArgs MintNewEditionFromMasterEditionViaTokenArgs `json:"mint_new_edition_from_master_edition_via_token_args"`

	// Accounts:
	NewMetadata                solanago.PublicKey `json:"new_metadata"`
	NewMetadataWritable        bool               `json:"new_metadata_writable"`
	NewEdition                 solanago.PublicKey `json:"new_edition"`
	NewEditionWritable         bool               `json:"new_edition_writable"`
	MasterEdition              solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable      bool               `json:"master_edition_writable"`
	NewMint                    solanago.PublicKey `json:"new_mint"`
	NewMintWritable            bool               `json:"new_mint_writable"`
	EditionMarkPda             solanago.PublicKey `json:"edition_mark_pda"`
	EditionMarkPdaWritable     bool               `json:"edition_mark_pda_writable"`
	NewMintAuthority           solanago.PublicKey `json:"new_mint_authority"`
	NewMintAuthoritySigner     bool               `json:"new_mint_authority_signer"`
	Payer                      solanago.PublicKey `json:"payer"`
	PayerWritable              bool               `json:"payer_writable"`
	PayerSigner                bool               `json:"payer_signer"`
	VaultAuthority             solanago.PublicKey `json:"vault_authority"`
	VaultAuthoritySigner       bool               `json:"vault_authority_signer"`
	SafetyDepositStore         solanago.PublicKey `json:"safety_deposit_store"`
	SafetyDepositBox           solanago.PublicKey `json:"safety_deposit_box"`
	Vault                      solanago.PublicKey `json:"vault"`
	NewMetadataUpdateAuthority solanago.PublicKey `json:"new_metadata_update_authority"`
	Metadata                   solanago.PublicKey `json:"metadata"`
	TokenProgram               solanago.PublicKey `json:"token_program"`
	TokenVaultProgram          solanago.PublicKey `json:"token_vault_program"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	Rent                       solanago.PublicKey `json:"rent"`
	RentOptional               bool               `json:"rent_optional"`
}

func (obj *MintNewEditionFromMasterEditionViaVaultProxyInstruction) GetDiscriminator() []byte {
	return Instruction_MintNewEditionFromMasterEditionViaVaultProxy[:]
}

// UnmarshalWithDecoder unmarshals the MintNewEditionFromMasterEditionViaVaultProxyInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *MintNewEditionFromMasterEditionViaVaultProxyInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "MintNewEditionFromMasterEditionViaVaultProxyInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_MintNewEditionFromMasterEditionViaVaultProxy[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "MintNewEditionFromMasterEditionViaVaultProxyInstruction", Instruction_MintNewEditionFromMasterEditionViaVaultProxy[:], discriminator)
	}
	// Deserialize `MintNewEditionFromMasterEditionViaTokenArgs`:
	err = decoder.Decode(&obj.MintNewEditionFromMasterEditionViaTokenArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *MintNewEditionFromMasterEditionViaVaultProxyInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from new_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_metadata", err)
	}
	indices = append(indices, index)
	// Decode from new_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_edition", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from new_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_mint", err)
	}
	indices = append(indices, index)
	// Decode from edition_mark_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_mark_pda", err)
	}
	indices = append(indices, index)
	// Decode from new_mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from vault_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "vault_authority", err)
	}
	indices = append(indices, index)
	// Decode from safety_deposit_store account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "safety_deposit_store", err)
	}
	indices = append(indices, index)
	// Decode from safety_deposit_box account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "safety_deposit_box", err)
	}
	indices = append(indices, index)
	// Decode from vault account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "vault", err)
	}
	indices = append(indices, index)
	// Decode from new_metadata_update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_metadata_update_authority", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from token_vault_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_vault_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *MintNewEditionFromMasterEditionViaVaultProxyInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 17 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 17, len(indices))
	}
	indexOffset := 0
	// Set new_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_metadata", len(accountKeys)-1)
	}
	obj.NewMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_edition", len(accountKeys)-1)
	}
	obj.NewEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_mint", len(accountKeys)-1)
	}
	obj.NewMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_mark_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_mark_pda", len(accountKeys)-1)
	}
	obj.EditionMarkPda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_mint_authority", len(accountKeys)-1)
	}
	obj.NewMintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set vault_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "vault_authority", len(accountKeys)-1)
	}
	obj.VaultAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set safety_deposit_store account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "safety_deposit_store", len(accountKeys)-1)
	}
	obj.SafetyDepositStore = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set safety_deposit_box account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "safety_deposit_box", len(accountKeys)-1)
	}
	obj.SafetyDepositBox = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set vault account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "vault", len(accountKeys)-1)
	}
	obj.Vault = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_metadata_update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_metadata_update_authority", len(accountKeys)-1)
	}
	obj.NewMetadataUpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_vault_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_vault_program", len(accountKeys)-1)
	}
	obj.TokenVaultProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *MintNewEditionFromMasterEditionViaVaultProxyInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.NewMetadata)
	keys = append(keys, obj.NewEdition)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.NewMint)
	keys = append(keys, obj.EditionMarkPda)
	keys = append(keys, obj.NewMintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.VaultAuthority)
	keys = append(keys, obj.SafetyDepositStore)
	keys = append(keys, obj.SafetyDepositBox)
	keys = append(keys, obj.Vault)
	keys = append(keys, obj.NewMetadataUpdateAuthority)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.TokenVaultProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the MintNewEditionFromMasterEditionViaVaultProxyInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *MintNewEditionFromMasterEditionViaVaultProxyInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling MintNewEditionFromMasterEditionViaVaultProxyInstruction: %w", err)
	}
	return nil
}

// UnmarshalMintNewEditionFromMasterEditionViaVaultProxyInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalMintNewEditionFromMasterEditionViaVaultProxyInstruction(buf []byte) (*MintNewEditionFromMasterEditionViaVaultProxyInstruction, error) {
	obj := new(MintNewEditionFromMasterEditionViaVaultProxyInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type PuffMetadataInstruction struct {

	// Accounts:
	Metadata         solanago.PublicKey `json:"metadata"`
	MetadataWritable bool               `json:"metadata_writable"`
}

func (obj *PuffMetadataInstruction) GetDiscriminator() []byte {
	return Instruction_PuffMetadata[:]
}

// UnmarshalWithDecoder unmarshals the PuffMetadataInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *PuffMetadataInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "PuffMetadataInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_PuffMetadata[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "PuffMetadataInstruction", Instruction_PuffMetadata[:], discriminator)
	}
	return nil
}

func (obj *PuffMetadataInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *PuffMetadataInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 1 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 1, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *PuffMetadataInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	return keys
}

// Unmarshal unmarshals the PuffMetadataInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *PuffMetadataInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling PuffMetadataInstruction: %w", err)
	}
	return nil
}

// UnmarshalPuffMetadataInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalPuffMetadataInstruction(buf []byte) (*PuffMetadataInstruction, error) {
	obj := new(PuffMetadataInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateMetadataAccountV2Instruction struct {
	UpdateMetadataAccountArgsV2 UpdateMetadataAccountArgsV2 `json:"update_metadata_account_args_v2"`

	// Accounts:
	Metadata              solanago.PublicKey `json:"metadata"`
	MetadataWritable      bool               `json:"metadata_writable"`
	UpdateAuthority       solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner bool               `json:"update_authority_signer"`
}

func (obj *UpdateMetadataAccountV2Instruction) GetDiscriminator() []byte {
	return Instruction_UpdateMetadataAccountV2[:]
}

// UnmarshalWithDecoder unmarshals the UpdateMetadataAccountV2Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateMetadataAccountV2Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateMetadataAccountV2Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UpdateMetadataAccountV2[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateMetadataAccountV2Instruction", Instruction_UpdateMetadataAccountV2[:], discriminator)
	}
	// Deserialize `UpdateMetadataAccountArgsV2`:
	err = decoder.Decode(&obj.UpdateMetadataAccountArgsV2)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateMetadataAccountV2Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateMetadataAccountV2Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateMetadataAccountV2Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.UpdateAuthority)
	return keys
}

// Unmarshal unmarshals the UpdateMetadataAccountV2Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateMetadataAccountV2Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateMetadataAccountV2Instruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateMetadataAccountV2Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateMetadataAccountV2Instruction(buf []byte) (*UpdateMetadataAccountV2Instruction, error) {
	obj := new(UpdateMetadataAccountV2Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateMetadataAccountV2Instruction struct {

	// Accounts:
	Metadata            solanago.PublicKey `json:"metadata"`
	MetadataWritable    bool               `json:"metadata_writable"`
	Mint                solanago.PublicKey `json:"mint"`
	MintAuthority       solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner bool               `json:"mint_authority_signer"`
	Payer               solanago.PublicKey `json:"payer"`
	PayerWritable       bool               `json:"payer_writable"`
	PayerSigner         bool               `json:"payer_signer"`
	UpdateAuthority     solanago.PublicKey `json:"update_authority"`
	SystemProgram       solanago.PublicKey `json:"system_program"`
	Rent                solanago.PublicKey `json:"rent"`
	RentOptional        bool               `json:"rent_optional"`
}

func (obj *CreateMetadataAccountV2Instruction) GetDiscriminator() []byte {
	return Instruction_CreateMetadataAccountV2[:]
}

// UnmarshalWithDecoder unmarshals the CreateMetadataAccountV2Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateMetadataAccountV2Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateMetadataAccountV2Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateMetadataAccountV2[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateMetadataAccountV2Instruction", Instruction_CreateMetadataAccountV2[:], discriminator)
	}
	return nil
}

func (obj *CreateMetadataAccountV2Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateMetadataAccountV2Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateMetadataAccountV2Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the CreateMetadataAccountV2Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateMetadataAccountV2Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateMetadataAccountV2Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateMetadataAccountV2Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateMetadataAccountV2Instruction(buf []byte) (*CreateMetadataAccountV2Instruction, error) {
	obj := new(CreateMetadataAccountV2Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateMasterEditionV3Instruction struct {
	CreateMasterEditionArgs CreateMasterEditionArgs `json:"create_master_edition_args"`

	// Accounts:
	Edition               solanago.PublicKey `json:"edition"`
	EditionWritable       bool               `json:"edition_writable"`
	Mint                  solanago.PublicKey `json:"mint"`
	MintWritable          bool               `json:"mint_writable"`
	UpdateAuthority       solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner bool               `json:"update_authority_signer"`
	MintAuthority         solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner   bool               `json:"mint_authority_signer"`
	Payer                 solanago.PublicKey `json:"payer"`
	PayerWritable         bool               `json:"payer_writable"`
	PayerSigner           bool               `json:"payer_signer"`
	Metadata              solanago.PublicKey `json:"metadata"`
	MetadataWritable      bool               `json:"metadata_writable"`
	TokenProgram          solanago.PublicKey `json:"token_program"`
	SystemProgram         solanago.PublicKey `json:"system_program"`
	Rent                  solanago.PublicKey `json:"rent"`
	RentOptional          bool               `json:"rent_optional"`
}

func (obj *CreateMasterEditionV3Instruction) GetDiscriminator() []byte {
	return Instruction_CreateMasterEditionV3[:]
}

// UnmarshalWithDecoder unmarshals the CreateMasterEditionV3Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateMasterEditionV3Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateMasterEditionV3Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateMasterEditionV3[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateMasterEditionV3Instruction", Instruction_CreateMasterEditionV3[:], discriminator)
	}
	// Deserialize `CreateMasterEditionArgs`:
	err = decoder.Decode(&obj.CreateMasterEditionArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateMasterEditionV3Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateMasterEditionV3Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 9 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 9, len(indices))
	}
	indexOffset := 0
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateMasterEditionV3Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the CreateMasterEditionV3Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateMasterEditionV3Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateMasterEditionV3Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateMasterEditionV3Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateMasterEditionV3Instruction(buf []byte) (*CreateMasterEditionV3Instruction, error) {
	obj := new(CreateMasterEditionV3Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type VerifyCollectionInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthorityWritable       bool               `json:"collection_authority_writable"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	Collection                        solanago.PublicKey `json:"collection"`
	CollectionMasterEditionAccount    solanago.PublicKey `json:"collection_master_edition_account"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *VerifyCollectionInstruction) GetDiscriminator() []byte {
	return Instruction_VerifyCollection[:]
}

// UnmarshalWithDecoder unmarshals the VerifyCollectionInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *VerifyCollectionInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "VerifyCollectionInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_VerifyCollection[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "VerifyCollectionInstruction", Instruction_VerifyCollection[:], discriminator)
	}
	return nil
}

func (obj *VerifyCollectionInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *VerifyCollectionInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition_account", len(accountKeys)-1)
	}
	obj.CollectionMasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *VerifyCollectionInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.CollectionMasterEditionAccount)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the VerifyCollectionInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *VerifyCollectionInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling VerifyCollectionInstruction: %w", err)
	}
	return nil
}

// UnmarshalVerifyCollectionInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalVerifyCollectionInstruction(buf []byte) (*VerifyCollectionInstruction, error) {
	obj := new(VerifyCollectionInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UtilizeInstruction struct {
	UtilizeArgs UtilizeArgs `json:"utilize_args"`

	// Accounts:
	Metadata                   solanago.PublicKey `json:"metadata"`
	MetadataWritable           bool               `json:"metadata_writable"`
	TokenAccount               solanago.PublicKey `json:"token_account"`
	TokenAccountWritable       bool               `json:"token_account_writable"`
	Mint                       solanago.PublicKey `json:"mint"`
	MintWritable               bool               `json:"mint_writable"`
	UseAuthority               solanago.PublicKey `json:"use_authority"`
	UseAuthorityWritable       bool               `json:"use_authority_writable"`
	UseAuthoritySigner         bool               `json:"use_authority_signer"`
	Owner                      solanago.PublicKey `json:"owner"`
	TokenProgram               solanago.PublicKey `json:"token_program"`
	AtaProgram                 solanago.PublicKey `json:"ata_program"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	Rent                       solanago.PublicKey `json:"rent"`
	UseAuthorityRecord         solanago.PublicKey `json:"use_authority_record"`
	UseAuthorityRecordWritable bool               `json:"use_authority_record_writable"`
	UseAuthorityRecordOptional bool               `json:"use_authority_record_optional"`
	Burner                     solanago.PublicKey `json:"burner"`
	BurnerOptional             bool               `json:"burner_optional"`
}

func (obj *UtilizeInstruction) GetDiscriminator() []byte {
	return Instruction_Utilize[:]
}

// UnmarshalWithDecoder unmarshals the UtilizeInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UtilizeInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UtilizeInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Utilize[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UtilizeInstruction", Instruction_Utilize[:], discriminator)
	}
	// Deserialize `UtilizeArgs`:
	err = decoder.Decode(&obj.UtilizeArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UtilizeInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from use_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "use_authority", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from ata_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "ata_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	// Decode from use_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "use_authority_record", err)
	}
	indices = append(indices, index)
	// Decode from burner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "burner", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UtilizeInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 11 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 11, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set use_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "use_authority", len(accountKeys)-1)
	}
	obj.UseAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set ata_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "ata_program", len(accountKeys)-1)
	}
	obj.AtaProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set use_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "use_authority_record", len(accountKeys)-1)
	}
	obj.UseAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set burner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "burner", len(accountKeys)-1)
	}
	obj.Burner = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UtilizeInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.UseAuthority)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.AtaProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	keys = append(keys, obj.UseAuthorityRecord)
	keys = append(keys, obj.Burner)
	return keys
}

// Unmarshal unmarshals the UtilizeInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UtilizeInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UtilizeInstruction: %w", err)
	}
	return nil
}

// UnmarshalUtilizeInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUtilizeInstruction(buf []byte) (*UtilizeInstruction, error) {
	obj := new(UtilizeInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ApproveUseAuthorityInstruction struct {
	ApproveUseAuthorityArgs ApproveUseAuthorityArgs `json:"approve_use_authority_args"`

	// Accounts:
	UseAuthorityRecord         solanago.PublicKey `json:"use_authority_record"`
	UseAuthorityRecordWritable bool               `json:"use_authority_record_writable"`
	Owner                      solanago.PublicKey `json:"owner"`
	OwnerWritable              bool               `json:"owner_writable"`
	OwnerSigner                bool               `json:"owner_signer"`
	Payer                      solanago.PublicKey `json:"payer"`
	PayerWritable              bool               `json:"payer_writable"`
	PayerSigner                bool               `json:"payer_signer"`
	User                       solanago.PublicKey `json:"user"`
	OwnerTokenAccount          solanago.PublicKey `json:"owner_token_account"`
	OwnerTokenAccountWritable  bool               `json:"owner_token_account_writable"`
	Metadata                   solanago.PublicKey `json:"metadata"`
	Mint                       solanago.PublicKey `json:"mint"`
	Burner                     solanago.PublicKey `json:"burner"`
	TokenProgram               solanago.PublicKey `json:"token_program"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	Rent                       solanago.PublicKey `json:"rent"`
	RentOptional               bool               `json:"rent_optional"`
}

func (obj *ApproveUseAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_ApproveUseAuthority[:]
}

// UnmarshalWithDecoder unmarshals the ApproveUseAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ApproveUseAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ApproveUseAuthorityInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ApproveUseAuthority[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ApproveUseAuthorityInstruction", Instruction_ApproveUseAuthority[:], discriminator)
	}
	// Deserialize `ApproveUseAuthorityArgs`:
	err = decoder.Decode(&obj.ApproveUseAuthorityArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *ApproveUseAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from use_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "use_authority_record", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from user account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "user", err)
	}
	indices = append(indices, index)
	// Decode from owner_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner_token_account", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from burner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "burner", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ApproveUseAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 11 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 11, len(indices))
	}
	indexOffset := 0
	// Set use_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "use_authority_record", len(accountKeys)-1)
	}
	obj.UseAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set user account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "user", len(accountKeys)-1)
	}
	obj.User = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner_token_account", len(accountKeys)-1)
	}
	obj.OwnerTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set burner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "burner", len(accountKeys)-1)
	}
	obj.Burner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ApproveUseAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.UseAuthorityRecord)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.User)
	keys = append(keys, obj.OwnerTokenAccount)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Burner)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the ApproveUseAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ApproveUseAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ApproveUseAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalApproveUseAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalApproveUseAuthorityInstruction(buf []byte) (*ApproveUseAuthorityInstruction, error) {
	obj := new(ApproveUseAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RevokeUseAuthorityInstruction struct {

	// Accounts:
	UseAuthorityRecord         solanago.PublicKey `json:"use_authority_record"`
	UseAuthorityRecordWritable bool               `json:"use_authority_record_writable"`
	Owner                      solanago.PublicKey `json:"owner"`
	OwnerWritable              bool               `json:"owner_writable"`
	OwnerSigner                bool               `json:"owner_signer"`
	User                       solanago.PublicKey `json:"user"`
	OwnerTokenAccount          solanago.PublicKey `json:"owner_token_account"`
	OwnerTokenAccountWritable  bool               `json:"owner_token_account_writable"`
	Mint                       solanago.PublicKey `json:"mint"`
	Metadata                   solanago.PublicKey `json:"metadata"`
	TokenProgram               solanago.PublicKey `json:"token_program"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	Rent                       solanago.PublicKey `json:"rent"`
	RentOptional               bool               `json:"rent_optional"`
}

func (obj *RevokeUseAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_RevokeUseAuthority[:]
}

// UnmarshalWithDecoder unmarshals the RevokeUseAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RevokeUseAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RevokeUseAuthorityInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RevokeUseAuthority[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RevokeUseAuthorityInstruction", Instruction_RevokeUseAuthority[:], discriminator)
	}
	return nil
}

func (obj *RevokeUseAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from use_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "use_authority_record", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from user account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "user", err)
	}
	indices = append(indices, index)
	// Decode from owner_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner_token_account", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RevokeUseAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 9 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 9, len(indices))
	}
	indexOffset := 0
	// Set use_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "use_authority_record", len(accountKeys)-1)
	}
	obj.UseAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set user account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "user", len(accountKeys)-1)
	}
	obj.User = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner_token_account", len(accountKeys)-1)
	}
	obj.OwnerTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RevokeUseAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.UseAuthorityRecord)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.User)
	keys = append(keys, obj.OwnerTokenAccount)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the RevokeUseAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RevokeUseAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RevokeUseAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalRevokeUseAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRevokeUseAuthorityInstruction(buf []byte) (*RevokeUseAuthorityInstruction, error) {
	obj := new(RevokeUseAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UnverifyCollectionInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthorityWritable       bool               `json:"collection_authority_writable"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	Collection                        solanago.PublicKey `json:"collection"`
	CollectionMasterEditionAccount    solanago.PublicKey `json:"collection_master_edition_account"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *UnverifyCollectionInstruction) GetDiscriminator() []byte {
	return Instruction_UnverifyCollection[:]
}

// UnmarshalWithDecoder unmarshals the UnverifyCollectionInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UnverifyCollectionInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UnverifyCollectionInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UnverifyCollection[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UnverifyCollectionInstruction", Instruction_UnverifyCollection[:], discriminator)
	}
	return nil
}

func (obj *UnverifyCollectionInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UnverifyCollectionInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 6 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 6, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition_account", len(accountKeys)-1)
	}
	obj.CollectionMasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UnverifyCollectionInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.CollectionMasterEditionAccount)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the UnverifyCollectionInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UnverifyCollectionInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UnverifyCollectionInstruction: %w", err)
	}
	return nil
}

// UnmarshalUnverifyCollectionInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUnverifyCollectionInstruction(buf []byte) (*UnverifyCollectionInstruction, error) {
	obj := new(UnverifyCollectionInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ApproveCollectionAuthorityInstruction struct {

	// Accounts:
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordWritable bool               `json:"collection_authority_record_writable"`
	NewCollectionAuthority            solanago.PublicKey `json:"new_collection_authority"`
	UpdateAuthority                   solanago.PublicKey `json:"update_authority"`
	UpdateAuthorityWritable           bool               `json:"update_authority_writable"`
	UpdateAuthoritySigner             bool               `json:"update_authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	Mint                              solanago.PublicKey `json:"mint"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	Rent                              solanago.PublicKey `json:"rent"`
	RentOptional                      bool               `json:"rent_optional"`
}

func (obj *ApproveCollectionAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_ApproveCollectionAuthority[:]
}

// UnmarshalWithDecoder unmarshals the ApproveCollectionAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ApproveCollectionAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ApproveCollectionAuthorityInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ApproveCollectionAuthority[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ApproveCollectionAuthorityInstruction", Instruction_ApproveCollectionAuthority[:], discriminator)
	}
	return nil
}

func (obj *ApproveCollectionAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	// Decode from new_collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "new_collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ApproveCollectionAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set new_collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "new_collection_authority", len(accountKeys)-1)
	}
	obj.NewCollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ApproveCollectionAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.CollectionAuthorityRecord)
	keys = append(keys, obj.NewCollectionAuthority)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the ApproveCollectionAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ApproveCollectionAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ApproveCollectionAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalApproveCollectionAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalApproveCollectionAuthorityInstruction(buf []byte) (*ApproveCollectionAuthorityInstruction, error) {
	obj := new(ApproveCollectionAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RevokeCollectionAuthorityInstruction struct {

	// Accounts:
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordWritable bool               `json:"collection_authority_record_writable"`
	DelegateAuthority                 solanago.PublicKey `json:"delegate_authority"`
	DelegateAuthorityWritable         bool               `json:"delegate_authority_writable"`
	RevokeAuthority                   solanago.PublicKey `json:"revoke_authority"`
	RevokeAuthorityWritable           bool               `json:"revoke_authority_writable"`
	RevokeAuthoritySigner             bool               `json:"revoke_authority_signer"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	Mint                              solanago.PublicKey `json:"mint"`
}

func (obj *RevokeCollectionAuthorityInstruction) GetDiscriminator() []byte {
	return Instruction_RevokeCollectionAuthority[:]
}

// UnmarshalWithDecoder unmarshals the RevokeCollectionAuthorityInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RevokeCollectionAuthorityInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RevokeCollectionAuthorityInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RevokeCollectionAuthority[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RevokeCollectionAuthorityInstruction", Instruction_RevokeCollectionAuthority[:], discriminator)
	}
	return nil
}

func (obj *RevokeCollectionAuthorityInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	// Decode from delegate_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_authority", err)
	}
	indices = append(indices, index)
	// Decode from revoke_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "revoke_authority", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RevokeCollectionAuthorityInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_authority", len(accountKeys)-1)
	}
	obj.DelegateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set revoke_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "revoke_authority", len(accountKeys)-1)
	}
	obj.RevokeAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RevokeCollectionAuthorityInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.CollectionAuthorityRecord)
	keys = append(keys, obj.DelegateAuthority)
	keys = append(keys, obj.RevokeAuthority)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	return keys
}

// Unmarshal unmarshals the RevokeCollectionAuthorityInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RevokeCollectionAuthorityInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RevokeCollectionAuthorityInstruction: %w", err)
	}
	return nil
}

// UnmarshalRevokeCollectionAuthorityInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRevokeCollectionAuthorityInstruction(buf []byte) (*RevokeCollectionAuthorityInstruction, error) {
	obj := new(RevokeCollectionAuthorityInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetAndVerifyCollectionInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthorityWritable       bool               `json:"collection_authority_writable"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	UpdateAuthority                   solanago.PublicKey `json:"update_authority"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	Collection                        solanago.PublicKey `json:"collection"`
	CollectionMasterEditionAccount    solanago.PublicKey `json:"collection_master_edition_account"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *SetAndVerifyCollectionInstruction) GetDiscriminator() []byte {
	return Instruction_SetAndVerifyCollection[:]
}

// UnmarshalWithDecoder unmarshals the SetAndVerifyCollectionInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetAndVerifyCollectionInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetAndVerifyCollectionInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_SetAndVerifyCollection[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "SetAndVerifyCollectionInstruction", Instruction_SetAndVerifyCollection[:], discriminator)
	}
	return nil
}

func (obj *SetAndVerifyCollectionInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetAndVerifyCollectionInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition_account", len(accountKeys)-1)
	}
	obj.CollectionMasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetAndVerifyCollectionInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.CollectionMasterEditionAccount)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the SetAndVerifyCollectionInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetAndVerifyCollectionInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetAndVerifyCollectionInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetAndVerifyCollectionInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetAndVerifyCollectionInstruction(buf []byte) (*SetAndVerifyCollectionInstruction, error) {
	obj := new(SetAndVerifyCollectionInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type FreezeDelegatedAccountInstruction struct {

	// Accounts:
	Delegate             solanago.PublicKey `json:"delegate"`
	DelegateWritable     bool               `json:"delegate_writable"`
	DelegateSigner       bool               `json:"delegate_signer"`
	TokenAccount         solanago.PublicKey `json:"token_account"`
	TokenAccountWritable bool               `json:"token_account_writable"`
	Edition              solanago.PublicKey `json:"edition"`
	Mint                 solanago.PublicKey `json:"mint"`
	TokenProgram         solanago.PublicKey `json:"token_program"`
}

func (obj *FreezeDelegatedAccountInstruction) GetDiscriminator() []byte {
	return Instruction_FreezeDelegatedAccount[:]
}

// UnmarshalWithDecoder unmarshals the FreezeDelegatedAccountInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *FreezeDelegatedAccountInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "FreezeDelegatedAccountInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_FreezeDelegatedAccount[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "FreezeDelegatedAccountInstruction", Instruction_FreezeDelegatedAccount[:], discriminator)
	}
	return nil
}

func (obj *FreezeDelegatedAccountInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from delegate account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *FreezeDelegatedAccountInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set delegate account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate", len(accountKeys)-1)
	}
	obj.Delegate = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *FreezeDelegatedAccountInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Delegate)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.TokenProgram)
	return keys
}

// Unmarshal unmarshals the FreezeDelegatedAccountInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *FreezeDelegatedAccountInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling FreezeDelegatedAccountInstruction: %w", err)
	}
	return nil
}

// UnmarshalFreezeDelegatedAccountInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalFreezeDelegatedAccountInstruction(buf []byte) (*FreezeDelegatedAccountInstruction, error) {
	obj := new(FreezeDelegatedAccountInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ThawDelegatedAccountInstruction struct {

	// Accounts:
	Delegate             solanago.PublicKey `json:"delegate"`
	DelegateWritable     bool               `json:"delegate_writable"`
	DelegateSigner       bool               `json:"delegate_signer"`
	TokenAccount         solanago.PublicKey `json:"token_account"`
	TokenAccountWritable bool               `json:"token_account_writable"`
	Edition              solanago.PublicKey `json:"edition"`
	Mint                 solanago.PublicKey `json:"mint"`
	TokenProgram         solanago.PublicKey `json:"token_program"`
}

func (obj *ThawDelegatedAccountInstruction) GetDiscriminator() []byte {
	return Instruction_ThawDelegatedAccount[:]
}

// UnmarshalWithDecoder unmarshals the ThawDelegatedAccountInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ThawDelegatedAccountInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ThawDelegatedAccountInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_ThawDelegatedAccount[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ThawDelegatedAccountInstruction", Instruction_ThawDelegatedAccount[:], discriminator)
	}
	return nil
}

func (obj *ThawDelegatedAccountInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from delegate account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ThawDelegatedAccountInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set delegate account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate", len(accountKeys)-1)
	}
	obj.Delegate = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ThawDelegatedAccountInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Delegate)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.TokenProgram)
	return keys
}

// Unmarshal unmarshals the ThawDelegatedAccountInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ThawDelegatedAccountInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ThawDelegatedAccountInstruction: %w", err)
	}
	return nil
}

// UnmarshalThawDelegatedAccountInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalThawDelegatedAccountInstruction(buf []byte) (*ThawDelegatedAccountInstruction, error) {
	obj := new(ThawDelegatedAccountInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RemoveCreatorVerificationInstruction struct {

	// Accounts:
	Metadata         solanago.PublicKey `json:"metadata"`
	MetadataWritable bool               `json:"metadata_writable"`
	Creator          solanago.PublicKey `json:"creator"`
	CreatorSigner    bool               `json:"creator_signer"`
}

func (obj *RemoveCreatorVerificationInstruction) GetDiscriminator() []byte {
	return Instruction_RemoveCreatorVerification[:]
}

// UnmarshalWithDecoder unmarshals the RemoveCreatorVerificationInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RemoveCreatorVerificationInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RemoveCreatorVerificationInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_RemoveCreatorVerification[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RemoveCreatorVerificationInstruction", Instruction_RemoveCreatorVerification[:], discriminator)
	}
	return nil
}

func (obj *RemoveCreatorVerificationInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from creator account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "creator", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RemoveCreatorVerificationInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set creator account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "creator", len(accountKeys)-1)
	}
	obj.Creator = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RemoveCreatorVerificationInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Creator)
	return keys
}

// Unmarshal unmarshals the RemoveCreatorVerificationInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RemoveCreatorVerificationInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RemoveCreatorVerificationInstruction: %w", err)
	}
	return nil
}

// UnmarshalRemoveCreatorVerificationInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRemoveCreatorVerificationInstruction(buf []byte) (*RemoveCreatorVerificationInstruction, error) {
	obj := new(RemoveCreatorVerificationInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type BurnNftInstruction struct {

	// Accounts:
	Metadata                     solanago.PublicKey `json:"metadata"`
	MetadataWritable             bool               `json:"metadata_writable"`
	Owner                        solanago.PublicKey `json:"owner"`
	OwnerWritable                bool               `json:"owner_writable"`
	OwnerSigner                  bool               `json:"owner_signer"`
	Mint                         solanago.PublicKey `json:"mint"`
	MintWritable                 bool               `json:"mint_writable"`
	TokenAccount                 solanago.PublicKey `json:"token_account"`
	TokenAccountWritable         bool               `json:"token_account_writable"`
	MasterEditionAccount         solanago.PublicKey `json:"master_edition_account"`
	MasterEditionAccountWritable bool               `json:"master_edition_account_writable"`
	SplTokenProgram              solanago.PublicKey `json:"spl_token_program"`
	CollectionMetadata           solanago.PublicKey `json:"collection_metadata"`
	CollectionMetadataWritable   bool               `json:"collection_metadata_writable"`
	CollectionMetadataOptional   bool               `json:"collection_metadata_optional"`
}

func (obj *BurnNftInstruction) GetDiscriminator() []byte {
	return Instruction_BurnNft[:]
}

// UnmarshalWithDecoder unmarshals the BurnNftInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *BurnNftInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "BurnNftInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_BurnNft[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "BurnNftInstruction", Instruction_BurnNft[:], discriminator)
	}
	return nil
}

func (obj *BurnNftInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *BurnNftInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition_account", len(accountKeys)-1)
	}
	obj.MasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *BurnNftInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.MasterEditionAccount)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.CollectionMetadata)
	return keys
}

// Unmarshal unmarshals the BurnNftInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *BurnNftInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling BurnNftInstruction: %w", err)
	}
	return nil
}

// UnmarshalBurnNftInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalBurnNftInstruction(buf []byte) (*BurnNftInstruction, error) {
	obj := new(BurnNftInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type VerifySizedCollectionItemInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	Collection                        solanago.PublicKey `json:"collection"`
	CollectionWritable                bool               `json:"collection_writable"`
	CollectionMasterEditionAccount    solanago.PublicKey `json:"collection_master_edition_account"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *VerifySizedCollectionItemInstruction) GetDiscriminator() []byte {
	return Instruction_VerifySizedCollectionItem[:]
}

// UnmarshalWithDecoder unmarshals the VerifySizedCollectionItemInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *VerifySizedCollectionItemInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "VerifySizedCollectionItemInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_VerifySizedCollectionItem[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "VerifySizedCollectionItemInstruction", Instruction_VerifySizedCollectionItem[:], discriminator)
	}
	return nil
}

func (obj *VerifySizedCollectionItemInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *VerifySizedCollectionItemInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition_account", len(accountKeys)-1)
	}
	obj.CollectionMasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *VerifySizedCollectionItemInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.CollectionMasterEditionAccount)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the VerifySizedCollectionItemInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *VerifySizedCollectionItemInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling VerifySizedCollectionItemInstruction: %w", err)
	}
	return nil
}

// UnmarshalVerifySizedCollectionItemInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalVerifySizedCollectionItemInstruction(buf []byte) (*VerifySizedCollectionItemInstruction, error) {
	obj := new(VerifySizedCollectionItemInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UnverifySizedCollectionItemInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	Collection                        solanago.PublicKey `json:"collection"`
	CollectionWritable                bool               `json:"collection_writable"`
	CollectionMasterEditionAccount    solanago.PublicKey `json:"collection_master_edition_account"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *UnverifySizedCollectionItemInstruction) GetDiscriminator() []byte {
	return Instruction_UnverifySizedCollectionItem[:]
}

// UnmarshalWithDecoder unmarshals the UnverifySizedCollectionItemInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UnverifySizedCollectionItemInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UnverifySizedCollectionItemInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_UnverifySizedCollectionItem[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UnverifySizedCollectionItemInstruction", Instruction_UnverifySizedCollectionItem[:], discriminator)
	}
	return nil
}

func (obj *UnverifySizedCollectionItemInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UnverifySizedCollectionItemInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition_account", len(accountKeys)-1)
	}
	obj.CollectionMasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UnverifySizedCollectionItemInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.CollectionMasterEditionAccount)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the UnverifySizedCollectionItemInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UnverifySizedCollectionItemInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UnverifySizedCollectionItemInstruction: %w", err)
	}
	return nil
}

// UnmarshalUnverifySizedCollectionItemInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUnverifySizedCollectionItemInstruction(buf []byte) (*UnverifySizedCollectionItemInstruction, error) {
	obj := new(UnverifySizedCollectionItemInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetAndVerifySizedCollectionItemInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	UpdateAuthority                   solanago.PublicKey `json:"update_authority"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	Collection                        solanago.PublicKey `json:"collection"`
	CollectionWritable                bool               `json:"collection_writable"`
	CollectionMasterEditionAccount    solanago.PublicKey `json:"collection_master_edition_account"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *SetAndVerifySizedCollectionItemInstruction) GetDiscriminator() []byte {
	return Instruction_SetAndVerifySizedCollectionItem[:]
}

// UnmarshalWithDecoder unmarshals the SetAndVerifySizedCollectionItemInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetAndVerifySizedCollectionItemInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetAndVerifySizedCollectionItemInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_SetAndVerifySizedCollectionItem[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "SetAndVerifySizedCollectionItemInstruction", Instruction_SetAndVerifySizedCollectionItem[:], discriminator)
	}
	return nil
}

func (obj *SetAndVerifySizedCollectionItemInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetAndVerifySizedCollectionItemInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection", len(accountKeys)-1)
	}
	obj.Collection = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition_account", len(accountKeys)-1)
	}
	obj.CollectionMasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetAndVerifySizedCollectionItemInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.Collection)
	keys = append(keys, obj.CollectionMasterEditionAccount)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the SetAndVerifySizedCollectionItemInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetAndVerifySizedCollectionItemInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetAndVerifySizedCollectionItemInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetAndVerifySizedCollectionItemInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetAndVerifySizedCollectionItemInstruction(buf []byte) (*SetAndVerifySizedCollectionItemInstruction, error) {
	obj := new(SetAndVerifySizedCollectionItemInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateMetadataAccountV3Instruction struct {
	CreateMetadataAccountArgsV3 CreateMetadataAccountArgsV3 `json:"create_metadata_account_args_v3"`

	// Accounts:
	Metadata            solanago.PublicKey `json:"metadata"`
	MetadataWritable    bool               `json:"metadata_writable"`
	Mint                solanago.PublicKey `json:"mint"`
	MintAuthority       solanago.PublicKey `json:"mint_authority"`
	MintAuthoritySigner bool               `json:"mint_authority_signer"`
	Payer               solanago.PublicKey `json:"payer"`
	PayerWritable       bool               `json:"payer_writable"`
	PayerSigner         bool               `json:"payer_signer"`
	UpdateAuthority     solanago.PublicKey `json:"update_authority"`
	SystemProgram       solanago.PublicKey `json:"system_program"`
	Rent                solanago.PublicKey `json:"rent"`
	RentOptional        bool               `json:"rent_optional"`
}

func (obj *CreateMetadataAccountV3Instruction) GetDiscriminator() []byte {
	return Instruction_CreateMetadataAccountV3[:]
}

// UnmarshalWithDecoder unmarshals the CreateMetadataAccountV3Instruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateMetadataAccountV3Instruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateMetadataAccountV3Instruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateMetadataAccountV3[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateMetadataAccountV3Instruction", Instruction_CreateMetadataAccountV3[:], discriminator)
	}
	// Deserialize `CreateMetadataAccountArgsV3`:
	err = decoder.Decode(&obj.CreateMetadataAccountArgsV3)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateMetadataAccountV3Instruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from rent account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "rent", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateMetadataAccountV3Instruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint_authority", len(accountKeys)-1)
	}
	obj.MintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set rent account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "rent", len(accountKeys)-1)
	}
	obj.Rent = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateMetadataAccountV3Instruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.MintAuthority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.Rent)
	return keys
}

// Unmarshal unmarshals the CreateMetadataAccountV3Instruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateMetadataAccountV3Instruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateMetadataAccountV3Instruction: %w", err)
	}
	return nil
}

// UnmarshalCreateMetadataAccountV3Instruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateMetadataAccountV3Instruction(buf []byte) (*CreateMetadataAccountV3Instruction, error) {
	obj := new(CreateMetadataAccountV3Instruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetCollectionSizeInstruction struct {
	SetCollectionSizeArgs SetCollectionSizeArgs `json:"set_collection_size_args"`

	// Accounts:
	CollectionMetadata                solanago.PublicKey `json:"collection_metadata"`
	CollectionMetadataWritable        bool               `json:"collection_metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthorityWritable       bool               `json:"collection_authority_writable"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *SetCollectionSizeInstruction) GetDiscriminator() []byte {
	return Instruction_SetCollectionSize[:]
}

// UnmarshalWithDecoder unmarshals the SetCollectionSizeInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetCollectionSizeInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetCollectionSizeInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_SetCollectionSize[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "SetCollectionSizeInstruction", Instruction_SetCollectionSize[:], discriminator)
	}
	// Deserialize `SetCollectionSizeArgs`:
	err = decoder.Decode(&obj.SetCollectionSizeArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *SetCollectionSizeInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetCollectionSizeInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetCollectionSizeInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.CollectionMetadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the SetCollectionSizeInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetCollectionSizeInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetCollectionSizeInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetCollectionSizeInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetCollectionSizeInstruction(buf []byte) (*SetCollectionSizeInstruction, error) {
	obj := new(SetCollectionSizeInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type SetTokenStandardInstruction struct {

	// Accounts:
	Metadata              solanago.PublicKey `json:"metadata"`
	MetadataWritable      bool               `json:"metadata_writable"`
	UpdateAuthority       solanago.PublicKey `json:"update_authority"`
	UpdateAuthoritySigner bool               `json:"update_authority_signer"`
	Mint                  solanago.PublicKey `json:"mint"`
	Edition               solanago.PublicKey `json:"edition"`
	EditionOptional       bool               `json:"edition_optional"`
}

func (obj *SetTokenStandardInstruction) GetDiscriminator() []byte {
	return Instruction_SetTokenStandard[:]
}

// UnmarshalWithDecoder unmarshals the SetTokenStandardInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *SetTokenStandardInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "SetTokenStandardInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_SetTokenStandard[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "SetTokenStandardInstruction", Instruction_SetTokenStandard[:], discriminator)
	}
	return nil
}

func (obj *SetTokenStandardInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *SetTokenStandardInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 4 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 4, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *SetTokenStandardInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Edition)
	return keys
}

// Unmarshal unmarshals the SetTokenStandardInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *SetTokenStandardInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling SetTokenStandardInstruction: %w", err)
	}
	return nil
}

// UnmarshalSetTokenStandardInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalSetTokenStandardInstruction(buf []byte) (*SetTokenStandardInstruction, error) {
	obj := new(SetTokenStandardInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type BubblegumSetCollectionSizeInstruction struct {
	SetCollectionSizeArgs SetCollectionSizeArgs `json:"set_collection_size_args"`

	// Accounts:
	CollectionMetadata                solanago.PublicKey `json:"collection_metadata"`
	CollectionMetadataWritable        bool               `json:"collection_metadata_writable"`
	CollectionAuthority               solanago.PublicKey `json:"collection_authority"`
	CollectionAuthoritySigner         bool               `json:"collection_authority_signer"`
	CollectionMint                    solanago.PublicKey `json:"collection_mint"`
	BubblegumSigner                   solanago.PublicKey `json:"bubblegum_signer"`
	BubblegumSignerSigner             bool               `json:"bubblegum_signer_signer"`
	CollectionAuthorityRecord         solanago.PublicKey `json:"collection_authority_record"`
	CollectionAuthorityRecordOptional bool               `json:"collection_authority_record_optional"`
}

func (obj *BubblegumSetCollectionSizeInstruction) GetDiscriminator() []byte {
	return Instruction_BubblegumSetCollectionSize[:]
}

// UnmarshalWithDecoder unmarshals the BubblegumSetCollectionSizeInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *BubblegumSetCollectionSizeInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "BubblegumSetCollectionSizeInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_BubblegumSetCollectionSize[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "BubblegumSetCollectionSizeInstruction", Instruction_BubblegumSetCollectionSize[:], discriminator)
	}
	// Deserialize `SetCollectionSizeArgs`:
	err = decoder.Decode(&obj.SetCollectionSizeArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *BubblegumSetCollectionSizeInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from bubblegum_signer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "bubblegum_signer", err)
	}
	indices = append(indices, index)
	// Decode from collection_authority_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_authority_record", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *BubblegumSetCollectionSizeInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority", len(accountKeys)-1)
	}
	obj.CollectionAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set bubblegum_signer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "bubblegum_signer", len(accountKeys)-1)
	}
	obj.BubblegumSigner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_authority_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_authority_record", len(accountKeys)-1)
	}
	obj.CollectionAuthorityRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *BubblegumSetCollectionSizeInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.CollectionMetadata)
	keys = append(keys, obj.CollectionAuthority)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.BubblegumSigner)
	keys = append(keys, obj.CollectionAuthorityRecord)
	return keys
}

// Unmarshal unmarshals the BubblegumSetCollectionSizeInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *BubblegumSetCollectionSizeInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling BubblegumSetCollectionSizeInstruction: %w", err)
	}
	return nil
}

// UnmarshalBubblegumSetCollectionSizeInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalBubblegumSetCollectionSizeInstruction(buf []byte) (*BubblegumSetCollectionSizeInstruction, error) {
	obj := new(BubblegumSetCollectionSizeInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type BurnEditionNftInstruction struct {

	// Accounts:
	Metadata                         solanago.PublicKey `json:"metadata"`
	MetadataWritable                 bool               `json:"metadata_writable"`
	Owner                            solanago.PublicKey `json:"owner"`
	OwnerWritable                    bool               `json:"owner_writable"`
	OwnerSigner                      bool               `json:"owner_signer"`
	PrintEditionMint                 solanago.PublicKey `json:"print_edition_mint"`
	PrintEditionMintWritable         bool               `json:"print_edition_mint_writable"`
	MasterEditionMint                solanago.PublicKey `json:"master_edition_mint"`
	PrintEditionTokenAccount         solanago.PublicKey `json:"print_edition_token_account"`
	PrintEditionTokenAccountWritable bool               `json:"print_edition_token_account_writable"`
	MasterEditionTokenAccount        solanago.PublicKey `json:"master_edition_token_account"`
	MasterEditionAccount             solanago.PublicKey `json:"master_edition_account"`
	MasterEditionAccountWritable     bool               `json:"master_edition_account_writable"`
	PrintEditionAccount              solanago.PublicKey `json:"print_edition_account"`
	PrintEditionAccountWritable      bool               `json:"print_edition_account_writable"`
	EditionMarkerAccount             solanago.PublicKey `json:"edition_marker_account"`
	EditionMarkerAccountWritable     bool               `json:"edition_marker_account_writable"`
	SplTokenProgram                  solanago.PublicKey `json:"spl_token_program"`
}

func (obj *BurnEditionNftInstruction) GetDiscriminator() []byte {
	return Instruction_BurnEditionNft[:]
}

// UnmarshalWithDecoder unmarshals the BurnEditionNftInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *BurnEditionNftInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "BurnEditionNftInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_BurnEditionNft[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "BurnEditionNftInstruction", Instruction_BurnEditionNft[:], discriminator)
	}
	return nil
}

func (obj *BurnEditionNftInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner", err)
	}
	indices = append(indices, index)
	// Decode from print_edition_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "print_edition_mint", err)
	}
	indices = append(indices, index)
	// Decode from master_edition_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition_mint", err)
	}
	indices = append(indices, index)
	// Decode from print_edition_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "print_edition_token_account", err)
	}
	indices = append(indices, index)
	// Decode from master_edition_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition_token_account", err)
	}
	indices = append(indices, index)
	// Decode from master_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from print_edition_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "print_edition_account", err)
	}
	indices = append(indices, index)
	// Decode from edition_marker_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_marker_account", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *BurnEditionNftInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 10 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 10, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner", len(accountKeys)-1)
	}
	obj.Owner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set print_edition_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "print_edition_mint", len(accountKeys)-1)
	}
	obj.PrintEditionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition_mint", len(accountKeys)-1)
	}
	obj.MasterEditionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set print_edition_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "print_edition_token_account", len(accountKeys)-1)
	}
	obj.PrintEditionTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition_token_account", len(accountKeys)-1)
	}
	obj.MasterEditionTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition_account", len(accountKeys)-1)
	}
	obj.MasterEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set print_edition_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "print_edition_account", len(accountKeys)-1)
	}
	obj.PrintEditionAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_marker_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_marker_account", len(accountKeys)-1)
	}
	obj.EditionMarkerAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *BurnEditionNftInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Owner)
	keys = append(keys, obj.PrintEditionMint)
	keys = append(keys, obj.MasterEditionMint)
	keys = append(keys, obj.PrintEditionTokenAccount)
	keys = append(keys, obj.MasterEditionTokenAccount)
	keys = append(keys, obj.MasterEditionAccount)
	keys = append(keys, obj.PrintEditionAccount)
	keys = append(keys, obj.EditionMarkerAccount)
	keys = append(keys, obj.SplTokenProgram)
	return keys
}

// Unmarshal unmarshals the BurnEditionNftInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *BurnEditionNftInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling BurnEditionNftInstruction: %w", err)
	}
	return nil
}

// UnmarshalBurnEditionNftInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalBurnEditionNftInstruction(buf []byte) (*BurnEditionNftInstruction, error) {
	obj := new(BurnEditionNftInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateEscrowAccountInstruction struct {

	// Accounts:
	Escrow             solanago.PublicKey `json:"escrow"`
	EscrowWritable     bool               `json:"escrow_writable"`
	Metadata           solanago.PublicKey `json:"metadata"`
	MetadataWritable   bool               `json:"metadata_writable"`
	Mint               solanago.PublicKey `json:"mint"`
	TokenAccount       solanago.PublicKey `json:"token_account"`
	Edition            solanago.PublicKey `json:"edition"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	SysvarInstructions solanago.PublicKey `json:"sysvar_instructions"`
	Authority          solanago.PublicKey `json:"authority"`
	AuthoritySigner    bool               `json:"authority_signer"`
	AuthorityOptional  bool               `json:"authority_optional"`
}

func (obj *CreateEscrowAccountInstruction) GetDiscriminator() []byte {
	return Instruction_CreateEscrowAccount[:]
}

// UnmarshalWithDecoder unmarshals the CreateEscrowAccountInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateEscrowAccountInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateEscrowAccountInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CreateEscrowAccount[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateEscrowAccountInstruction", Instruction_CreateEscrowAccount[:], discriminator)
	}
	return nil
}

func (obj *CreateEscrowAccountInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from escrow account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "escrow", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateEscrowAccountInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 9 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 9, len(indices))
	}
	indexOffset := 0
	// Set escrow account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "escrow", len(accountKeys)-1)
	}
	obj.Escrow = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateEscrowAccountInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Escrow)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.Authority)
	return keys
}

// Unmarshal unmarshals the CreateEscrowAccountInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateEscrowAccountInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateEscrowAccountInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateEscrowAccountInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateEscrowAccountInstruction(buf []byte) (*CreateEscrowAccountInstruction, error) {
	obj := new(CreateEscrowAccountInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CloseEscrowAccountInstruction struct {

	// Accounts:
	Escrow             solanago.PublicKey `json:"escrow"`
	EscrowWritable     bool               `json:"escrow_writable"`
	Metadata           solanago.PublicKey `json:"metadata"`
	MetadataWritable   bool               `json:"metadata_writable"`
	Mint               solanago.PublicKey `json:"mint"`
	TokenAccount       solanago.PublicKey `json:"token_account"`
	Edition            solanago.PublicKey `json:"edition"`
	Payer              solanago.PublicKey `json:"payer"`
	PayerWritable      bool               `json:"payer_writable"`
	PayerSigner        bool               `json:"payer_signer"`
	SystemProgram      solanago.PublicKey `json:"system_program"`
	SysvarInstructions solanago.PublicKey `json:"sysvar_instructions"`
}

func (obj *CloseEscrowAccountInstruction) GetDiscriminator() []byte {
	return Instruction_CloseEscrowAccount[:]
}

// UnmarshalWithDecoder unmarshals the CloseEscrowAccountInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CloseEscrowAccountInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CloseEscrowAccountInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CloseEscrowAccount[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CloseEscrowAccountInstruction", Instruction_CloseEscrowAccount[:], discriminator)
	}
	return nil
}

func (obj *CloseEscrowAccountInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from escrow account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "escrow", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_account", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CloseEscrowAccountInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set escrow account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "escrow", len(accountKeys)-1)
	}
	obj.Escrow = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_account", len(accountKeys)-1)
	}
	obj.TokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CloseEscrowAccountInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Escrow)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.TokenAccount)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	return keys
}

// Unmarshal unmarshals the CloseEscrowAccountInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CloseEscrowAccountInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CloseEscrowAccountInstruction: %w", err)
	}
	return nil
}

// UnmarshalCloseEscrowAccountInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCloseEscrowAccountInstruction(buf []byte) (*CloseEscrowAccountInstruction, error) {
	obj := new(CloseEscrowAccountInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type TransferOutOfEscrowInstruction struct {
	TransferOutOfEscrowArgs TransferOutOfEscrowArgs `json:"transfer_out_of_escrow_args"`

	// Accounts:
	Escrow               solanago.PublicKey `json:"escrow"`
	Metadata             solanago.PublicKey `json:"metadata"`
	MetadataWritable     bool               `json:"metadata_writable"`
	Payer                solanago.PublicKey `json:"payer"`
	PayerWritable        bool               `json:"payer_writable"`
	PayerSigner          bool               `json:"payer_signer"`
	AttributeMint        solanago.PublicKey `json:"attribute_mint"`
	AttributeSrc         solanago.PublicKey `json:"attribute_src"`
	AttributeSrcWritable bool               `json:"attribute_src_writable"`
	AttributeDst         solanago.PublicKey `json:"attribute_dst"`
	AttributeDstWritable bool               `json:"attribute_dst_writable"`
	EscrowMint           solanago.PublicKey `json:"escrow_mint"`
	EscrowAccount        solanago.PublicKey `json:"escrow_account"`
	SystemProgram        solanago.PublicKey `json:"system_program"`
	AtaProgram           solanago.PublicKey `json:"ata_program"`
	TokenProgram         solanago.PublicKey `json:"token_program"`
	SysvarInstructions   solanago.PublicKey `json:"sysvar_instructions"`
	Authority            solanago.PublicKey `json:"authority"`
	AuthoritySigner      bool               `json:"authority_signer"`
	AuthorityOptional    bool               `json:"authority_optional"`
}

func (obj *TransferOutOfEscrowInstruction) GetDiscriminator() []byte {
	return Instruction_TransferOutOfEscrow[:]
}

// UnmarshalWithDecoder unmarshals the TransferOutOfEscrowInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *TransferOutOfEscrowInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "TransferOutOfEscrowInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_TransferOutOfEscrow[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "TransferOutOfEscrowInstruction", Instruction_TransferOutOfEscrow[:], discriminator)
	}
	// Deserialize `TransferOutOfEscrowArgs`:
	err = decoder.Decode(&obj.TransferOutOfEscrowArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *TransferOutOfEscrowInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from escrow account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "escrow", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from attribute_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "attribute_mint", err)
	}
	indices = append(indices, index)
	// Decode from attribute_src account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "attribute_src", err)
	}
	indices = append(indices, index)
	// Decode from attribute_dst account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "attribute_dst", err)
	}
	indices = append(indices, index)
	// Decode from escrow_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "escrow_mint", err)
	}
	indices = append(indices, index)
	// Decode from escrow_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "escrow_account", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from ata_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "ata_program", err)
	}
	indices = append(indices, index)
	// Decode from token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *TransferOutOfEscrowInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 13 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 13, len(indices))
	}
	indexOffset := 0
	// Set escrow account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "escrow", len(accountKeys)-1)
	}
	obj.Escrow = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set attribute_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "attribute_mint", len(accountKeys)-1)
	}
	obj.AttributeMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set attribute_src account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "attribute_src", len(accountKeys)-1)
	}
	obj.AttributeSrc = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set attribute_dst account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "attribute_dst", len(accountKeys)-1)
	}
	obj.AttributeDst = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set escrow_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "escrow_mint", len(accountKeys)-1)
	}
	obj.EscrowMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set escrow_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "escrow_account", len(accountKeys)-1)
	}
	obj.EscrowAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set ata_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "ata_program", len(accountKeys)-1)
	}
	obj.AtaProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_program", len(accountKeys)-1)
	}
	obj.TokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *TransferOutOfEscrowInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Escrow)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.AttributeMint)
	keys = append(keys, obj.AttributeSrc)
	keys = append(keys, obj.AttributeDst)
	keys = append(keys, obj.EscrowMint)
	keys = append(keys, obj.EscrowAccount)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.AtaProgram)
	keys = append(keys, obj.TokenProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.Authority)
	return keys
}

// Unmarshal unmarshals the TransferOutOfEscrowInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *TransferOutOfEscrowInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling TransferOutOfEscrowInstruction: %w", err)
	}
	return nil
}

// UnmarshalTransferOutOfEscrowInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalTransferOutOfEscrowInstruction(buf []byte) (*TransferOutOfEscrowInstruction, error) {
	obj := new(TransferOutOfEscrowInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type BurnInstruction struct {
	BurnArgs BurnArgs `json:"burn_args"`

	// Accounts:
	Authority                  solanago.PublicKey `json:"authority"`
	AuthorityWritable          bool               `json:"authority_writable"`
	AuthoritySigner            bool               `json:"authority_signer"`
	CollectionMetadata         solanago.PublicKey `json:"collection_metadata"`
	CollectionMetadataWritable bool               `json:"collection_metadata_writable"`
	CollectionMetadataOptional bool               `json:"collection_metadata_optional"`
	Metadata                   solanago.PublicKey `json:"metadata"`
	MetadataWritable           bool               `json:"metadata_writable"`
	Edition                    solanago.PublicKey `json:"edition"`
	EditionWritable            bool               `json:"edition_writable"`
	EditionOptional            bool               `json:"edition_optional"`
	Mint                       solanago.PublicKey `json:"mint"`
	MintWritable               bool               `json:"mint_writable"`
	Token                      solanago.PublicKey `json:"token"`
	TokenWritable              bool               `json:"token_writable"`
	MasterEdition              solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable      bool               `json:"master_edition_writable"`
	MasterEditionOptional      bool               `json:"master_edition_optional"`
	MasterEditionMint          solanago.PublicKey `json:"master_edition_mint"`
	MasterEditionMintOptional  bool               `json:"master_edition_mint_optional"`
	MasterEditionToken         solanago.PublicKey `json:"master_edition_token"`
	MasterEditionTokenOptional bool               `json:"master_edition_token_optional"`
	EditionMarker              solanago.PublicKey `json:"edition_marker"`
	EditionMarkerWritable      bool               `json:"edition_marker_writable"`
	EditionMarkerOptional      bool               `json:"edition_marker_optional"`
	TokenRecord                solanago.PublicKey `json:"token_record"`
	TokenRecordWritable        bool               `json:"token_record_writable"`
	TokenRecordOptional        bool               `json:"token_record_optional"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	SysvarInstructions         solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram            solanago.PublicKey `json:"spl_token_program"`
}

func (obj *BurnInstruction) GetDiscriminator() []byte {
	return Instruction_Burn[:]
}

// UnmarshalWithDecoder unmarshals the BurnInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *BurnInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "BurnInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Burn[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "BurnInstruction", Instruction_Burn[:], discriminator)
	}
	// Deserialize `BurnArgs`:
	err = decoder.Decode(&obj.BurnArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *BurnInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from master_edition_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition_mint", err)
	}
	indices = append(indices, index)
	// Decode from master_edition_token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition_token", err)
	}
	indices = append(indices, index)
	// Decode from edition_marker account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_marker", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *BurnInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 14 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 14, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition_mint", len(accountKeys)-1)
	}
	obj.MasterEditionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition_token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition_token", len(accountKeys)-1)
	}
	obj.MasterEditionToken = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_marker account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_marker", len(accountKeys)-1)
	}
	obj.EditionMarker = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *BurnInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.CollectionMetadata)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.MasterEditionMint)
	keys = append(keys, obj.MasterEditionToken)
	keys = append(keys, obj.EditionMarker)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	return keys
}

// Unmarshal unmarshals the BurnInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *BurnInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling BurnInstruction: %w", err)
	}
	return nil
}

// UnmarshalBurnInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalBurnInstruction(buf []byte) (*BurnInstruction, error) {
	obj := new(BurnInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CreateInstruction struct {
	CreateArgs CreateArgs `json:"create_args"`

	// Accounts:
	Metadata                solanago.PublicKey `json:"metadata"`
	MetadataWritable        bool               `json:"metadata_writable"`
	MasterEdition           solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable   bool               `json:"master_edition_writable"`
	MasterEditionOptional   bool               `json:"master_edition_optional"`
	Mint                    solanago.PublicKey `json:"mint"`
	MintWritable            bool               `json:"mint_writable"`
	Authority               solanago.PublicKey `json:"authority"`
	AuthoritySigner         bool               `json:"authority_signer"`
	Payer                   solanago.PublicKey `json:"payer"`
	PayerWritable           bool               `json:"payer_writable"`
	PayerSigner             bool               `json:"payer_signer"`
	UpdateAuthority         solanago.PublicKey `json:"update_authority"`
	SystemProgram           solanago.PublicKey `json:"system_program"`
	SysvarInstructions      solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram         solanago.PublicKey `json:"spl_token_program"`
	SplTokenProgramOptional bool               `json:"spl_token_program_optional"`
}

func (obj *CreateInstruction) GetDiscriminator() []byte {
	return Instruction_Create[:]
}

// UnmarshalWithDecoder unmarshals the CreateInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CreateInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CreateInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Create[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CreateInstruction", Instruction_Create[:], discriminator)
	}
	// Deserialize `CreateArgs`:
	err = decoder.Decode(&obj.CreateArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *CreateInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CreateInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 9 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 9, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CreateInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	return keys
}

// Unmarshal unmarshals the CreateInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CreateInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CreateInstruction: %w", err)
	}
	return nil
}

// UnmarshalCreateInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCreateInstruction(buf []byte) (*CreateInstruction, error) {
	obj := new(CreateInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type MintInstruction struct {
	MintArgs MintArgs `json:"mint_args"`

	// Accounts:
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	TokenOwner                        solanago.PublicKey `json:"token_owner"`
	TokenOwnerOptional                bool               `json:"token_owner_optional"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MasterEdition                     solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable             bool               `json:"master_edition_writable"`
	MasterEditionOptional             bool               `json:"master_edition_optional"`
	TokenRecord                       solanago.PublicKey `json:"token_record"`
	TokenRecordWritable               bool               `json:"token_record_writable"`
	TokenRecordOptional               bool               `json:"token_record_optional"`
	Mint                              solanago.PublicKey `json:"mint"`
	MintWritable                      bool               `json:"mint_writable"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	DelegateRecord                    solanago.PublicKey `json:"delegate_record"`
	DelegateRecordOptional            bool               `json:"delegate_record_optional"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplAtaProgram                     solanago.PublicKey `json:"spl_ata_program"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *MintInstruction) GetDiscriminator() []byte {
	return Instruction_Mint[:]
}

// UnmarshalWithDecoder unmarshals the MintInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *MintInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "MintInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Mint[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "MintInstruction", Instruction_Mint[:], discriminator)
	}
	// Deserialize `MintArgs`:
	err = decoder.Decode(&obj.MintArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *MintInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from token_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_owner", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from spl_ata_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_ata_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *MintInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 15 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 15, len(indices))
	}
	indexOffset := 0
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_owner", len(accountKeys)-1)
	}
	obj.TokenOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_ata_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_ata_program", len(accountKeys)-1)
	}
	obj.SplAtaProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *MintInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.TokenOwner)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.SplAtaProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the MintInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *MintInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling MintInstruction: %w", err)
	}
	return nil
}

// UnmarshalMintInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalMintInstruction(buf []byte) (*MintInstruction, error) {
	obj := new(MintInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type DelegateInstruction struct {
	DelegateArgs DelegateArgs `json:"delegate_args"`

	// Accounts:
	DelegateRecord                    solanago.PublicKey `json:"delegate_record"`
	DelegateRecordWritable            bool               `json:"delegate_record_writable"`
	DelegateRecordOptional            bool               `json:"delegate_record_optional"`
	Delegate                          solanago.PublicKey `json:"delegate"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	MasterEdition                     solanago.PublicKey `json:"master_edition"`
	MasterEditionOptional             bool               `json:"master_edition_optional"`
	TokenRecord                       solanago.PublicKey `json:"token_record"`
	TokenRecordWritable               bool               `json:"token_record_writable"`
	TokenRecordOptional               bool               `json:"token_record_optional"`
	Mint                              solanago.PublicKey `json:"mint"`
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	TokenOptional                     bool               `json:"token_optional"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplTokenProgramOptional           bool               `json:"spl_token_program_optional"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *DelegateInstruction) GetDiscriminator() []byte {
	return Instruction_Delegate[:]
}

// UnmarshalWithDecoder unmarshals the DelegateInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *DelegateInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "DelegateInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Delegate[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "DelegateInstruction", Instruction_Delegate[:], discriminator)
	}
	// Deserialize `DelegateArgs`:
	err = decoder.Decode(&obj.DelegateArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *DelegateInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from delegate account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *DelegateInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 14 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 14, len(indices))
	}
	indexOffset := 0
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate", len(accountKeys)-1)
	}
	obj.Delegate = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *DelegateInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Delegate)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the DelegateInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *DelegateInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling DelegateInstruction: %w", err)
	}
	return nil
}

// UnmarshalDelegateInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalDelegateInstruction(buf []byte) (*DelegateInstruction, error) {
	obj := new(DelegateInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type RevokeInstruction struct {
	RevokeArgs RevokeArgs `json:"revoke_args"`

	// Accounts:
	DelegateRecord                    solanago.PublicKey `json:"delegate_record"`
	DelegateRecordWritable            bool               `json:"delegate_record_writable"`
	DelegateRecordOptional            bool               `json:"delegate_record_optional"`
	Delegate                          solanago.PublicKey `json:"delegate"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	MasterEdition                     solanago.PublicKey `json:"master_edition"`
	MasterEditionOptional             bool               `json:"master_edition_optional"`
	TokenRecord                       solanago.PublicKey `json:"token_record"`
	TokenRecordWritable               bool               `json:"token_record_writable"`
	TokenRecordOptional               bool               `json:"token_record_optional"`
	Mint                              solanago.PublicKey `json:"mint"`
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	TokenOptional                     bool               `json:"token_optional"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplTokenProgramOptional           bool               `json:"spl_token_program_optional"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *RevokeInstruction) GetDiscriminator() []byte {
	return Instruction_Revoke[:]
}

// UnmarshalWithDecoder unmarshals the RevokeInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *RevokeInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "RevokeInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Revoke[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "RevokeInstruction", Instruction_Revoke[:], discriminator)
	}
	// Deserialize `RevokeArgs`:
	err = decoder.Decode(&obj.RevokeArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *RevokeInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from delegate account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *RevokeInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 14 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 14, len(indices))
	}
	indexOffset := 0
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate", len(accountKeys)-1)
	}
	obj.Delegate = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *RevokeInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Delegate)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the RevokeInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *RevokeInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling RevokeInstruction: %w", err)
	}
	return nil
}

// UnmarshalRevokeInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalRevokeInstruction(buf []byte) (*RevokeInstruction, error) {
	obj := new(RevokeInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type LockInstruction struct {
	LockArgs LockArgs `json:"lock_args"`

	// Accounts:
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	TokenOwner                        solanago.PublicKey `json:"token_owner"`
	TokenOwnerOptional                bool               `json:"token_owner_optional"`
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	Mint                              solanago.PublicKey `json:"mint"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	Edition                           solanago.PublicKey `json:"edition"`
	EditionOptional                   bool               `json:"edition_optional"`
	TokenRecord                       solanago.PublicKey `json:"token_record"`
	TokenRecordWritable               bool               `json:"token_record_writable"`
	TokenRecordOptional               bool               `json:"token_record_optional"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplTokenProgramOptional           bool               `json:"spl_token_program_optional"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *LockInstruction) GetDiscriminator() []byte {
	return Instruction_Lock[:]
}

// UnmarshalWithDecoder unmarshals the LockInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *LockInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "LockInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Lock[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "LockInstruction", Instruction_Lock[:], discriminator)
	}
	// Deserialize `LockArgs`:
	err = decoder.Decode(&obj.LockArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *LockInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from token_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_owner", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *LockInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 13 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 13, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_owner", len(accountKeys)-1)
	}
	obj.TokenOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *LockInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.TokenOwner)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the LockInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *LockInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling LockInstruction: %w", err)
	}
	return nil
}

// UnmarshalLockInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalLockInstruction(buf []byte) (*LockInstruction, error) {
	obj := new(LockInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UnlockInstruction struct {
	UnlockArgs UnlockArgs `json:"unlock_args"`

	// Accounts:
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	TokenOwner                        solanago.PublicKey `json:"token_owner"`
	TokenOwnerOptional                bool               `json:"token_owner_optional"`
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	Mint                              solanago.PublicKey `json:"mint"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	Edition                           solanago.PublicKey `json:"edition"`
	EditionOptional                   bool               `json:"edition_optional"`
	TokenRecord                       solanago.PublicKey `json:"token_record"`
	TokenRecordWritable               bool               `json:"token_record_writable"`
	TokenRecordOptional               bool               `json:"token_record_optional"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplTokenProgramOptional           bool               `json:"spl_token_program_optional"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *UnlockInstruction) GetDiscriminator() []byte {
	return Instruction_Unlock[:]
}

// UnmarshalWithDecoder unmarshals the UnlockInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UnlockInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UnlockInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Unlock[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UnlockInstruction", Instruction_Unlock[:], discriminator)
	}
	// Deserialize `UnlockArgs`:
	err = decoder.Decode(&obj.UnlockArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UnlockInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from token_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_owner", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UnlockInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 13 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 13, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_owner", len(accountKeys)-1)
	}
	obj.TokenOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UnlockInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.TokenOwner)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the UnlockInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UnlockInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UnlockInstruction: %w", err)
	}
	return nil
}

// UnmarshalUnlockInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUnlockInstruction(buf []byte) (*UnlockInstruction, error) {
	obj := new(UnlockInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type MigrateInstruction struct {

	// Accounts:
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	Edition                           solanago.PublicKey `json:"edition"`
	EditionWritable                   bool               `json:"edition_writable"`
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	TokenOwner                        solanago.PublicKey `json:"token_owner"`
	Mint                              solanago.PublicKey `json:"mint"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	CollectionMetadata                solanago.PublicKey `json:"collection_metadata"`
	DelegateRecord                    solanago.PublicKey `json:"delegate_record"`
	TokenRecord                       solanago.PublicKey `json:"token_record"`
	TokenRecordWritable               bool               `json:"token_record_writable"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *MigrateInstruction) GetDiscriminator() []byte {
	return Instruction_Migrate[:]
}

// UnmarshalWithDecoder unmarshals the MigrateInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *MigrateInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "MigrateInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Migrate[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "MigrateInstruction", Instruction_Migrate[:], discriminator)
	}
	return nil
}

func (obj *MigrateInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from token_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_owner", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_record", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *MigrateInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 15 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 15, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_owner", len(accountKeys)-1)
	}
	obj.TokenOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_record", len(accountKeys)-1)
	}
	obj.TokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *MigrateInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.TokenOwner)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.CollectionMetadata)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.TokenRecord)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the MigrateInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *MigrateInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling MigrateInstruction: %w", err)
	}
	return nil
}

// UnmarshalMigrateInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalMigrateInstruction(buf []byte) (*MigrateInstruction, error) {
	obj := new(MigrateInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type TransferInstruction struct {
	TransferArgs TransferArgs `json:"transfer_args"`

	// Accounts:
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	TokenOwner                        solanago.PublicKey `json:"token_owner"`
	Destination                       solanago.PublicKey `json:"destination"`
	DestinationWritable               bool               `json:"destination_writable"`
	DestinationOwner                  solanago.PublicKey `json:"destination_owner"`
	Mint                              solanago.PublicKey `json:"mint"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	Edition                           solanago.PublicKey `json:"edition"`
	EditionOptional                   bool               `json:"edition_optional"`
	OwnerTokenRecord                  solanago.PublicKey `json:"owner_token_record"`
	OwnerTokenRecordWritable          bool               `json:"owner_token_record_writable"`
	OwnerTokenRecordOptional          bool               `json:"owner_token_record_optional"`
	DestinationTokenRecord            solanago.PublicKey `json:"destination_token_record"`
	DestinationTokenRecordWritable    bool               `json:"destination_token_record_writable"`
	DestinationTokenRecordOptional    bool               `json:"destination_token_record_optional"`
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplAtaProgram                     solanago.PublicKey `json:"spl_ata_program"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *TransferInstruction) GetDiscriminator() []byte {
	return Instruction_Transfer[:]
}

// UnmarshalWithDecoder unmarshals the TransferInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *TransferInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "TransferInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Transfer[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "TransferInstruction", Instruction_Transfer[:], discriminator)
	}
	// Deserialize `TransferArgs`:
	err = decoder.Decode(&obj.TransferArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *TransferInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from token_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token_owner", err)
	}
	indices = append(indices, index)
	// Decode from destination account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "destination", err)
	}
	indices = append(indices, index)
	// Decode from destination_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "destination_owner", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from owner_token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "owner_token_record", err)
	}
	indices = append(indices, index)
	// Decode from destination_token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "destination_token_record", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from spl_ata_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_ata_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *TransferInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 17 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 17, len(indices))
	}
	indexOffset := 0
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token_owner", len(accountKeys)-1)
	}
	obj.TokenOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set destination account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "destination", len(accountKeys)-1)
	}
	obj.Destination = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set destination_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "destination_owner", len(accountKeys)-1)
	}
	obj.DestinationOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set owner_token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "owner_token_record", len(accountKeys)-1)
	}
	obj.OwnerTokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set destination_token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "destination_token_record", len(accountKeys)-1)
	}
	obj.DestinationTokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_ata_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_ata_program", len(accountKeys)-1)
	}
	obj.SplAtaProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *TransferInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.TokenOwner)
	keys = append(keys, obj.Destination)
	keys = append(keys, obj.DestinationOwner)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.OwnerTokenRecord)
	keys = append(keys, obj.DestinationTokenRecord)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.SplAtaProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the TransferInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *TransferInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling TransferInstruction: %w", err)
	}
	return nil
}

// UnmarshalTransferInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalTransferInstruction(buf []byte) (*TransferInstruction, error) {
	obj := new(TransferInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UpdateInstruction struct {
	UpdateArgs UpdateArgs `json:"update_args"`

	// Accounts:
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	DelegateRecord                    solanago.PublicKey `json:"delegate_record"`
	DelegateRecordOptional            bool               `json:"delegate_record_optional"`
	Token                             solanago.PublicKey `json:"token"`
	TokenOptional                     bool               `json:"token_optional"`
	Mint                              solanago.PublicKey `json:"mint"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	Edition                           solanago.PublicKey `json:"edition"`
	EditionOptional                   bool               `json:"edition_optional"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerWritable                     bool               `json:"payer_writable"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *UpdateInstruction) GetDiscriminator() []byte {
	return Instruction_Update[:]
}

// UnmarshalWithDecoder unmarshals the UpdateInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UpdateInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UpdateInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Update[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UpdateInstruction", Instruction_Update[:], discriminator)
	}
	// Deserialize `UpdateArgs`:
	err = decoder.Decode(&obj.UpdateArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UpdateInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UpdateInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 11 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 11, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UpdateInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the UpdateInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UpdateInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UpdateInstruction: %w", err)
	}
	return nil
}

// UnmarshalUpdateInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUpdateInstruction(buf []byte) (*UpdateInstruction, error) {
	obj := new(UpdateInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UseInstruction struct {
	UseArgs UseArgs `json:"use_args"`

	// Accounts:
	Authority                         solanago.PublicKey `json:"authority"`
	AuthoritySigner                   bool               `json:"authority_signer"`
	DelegateRecord                    solanago.PublicKey `json:"delegate_record"`
	DelegateRecordWritable            bool               `json:"delegate_record_writable"`
	DelegateRecordOptional            bool               `json:"delegate_record_optional"`
	Token                             solanago.PublicKey `json:"token"`
	TokenWritable                     bool               `json:"token_writable"`
	TokenOptional                     bool               `json:"token_optional"`
	Mint                              solanago.PublicKey `json:"mint"`
	Metadata                          solanago.PublicKey `json:"metadata"`
	MetadataWritable                  bool               `json:"metadata_writable"`
	Edition                           solanago.PublicKey `json:"edition"`
	EditionWritable                   bool               `json:"edition_writable"`
	EditionOptional                   bool               `json:"edition_optional"`
	Payer                             solanago.PublicKey `json:"payer"`
	PayerSigner                       bool               `json:"payer_signer"`
	SystemProgram                     solanago.PublicKey `json:"system_program"`
	SysvarInstructions                solanago.PublicKey `json:"sysvar_instructions"`
	SplTokenProgram                   solanago.PublicKey `json:"spl_token_program"`
	SplTokenProgramOptional           bool               `json:"spl_token_program_optional"`
	AuthorizationRulesProgram         solanago.PublicKey `json:"authorization_rules_program"`
	AuthorizationRulesProgramOptional bool               `json:"authorization_rules_program_optional"`
	AuthorizationRules                solanago.PublicKey `json:"authorization_rules"`
	AuthorizationRulesOptional        bool               `json:"authorization_rules_optional"`
}

func (obj *UseInstruction) GetDiscriminator() []byte {
	return Instruction_Use[:]
}

// UnmarshalWithDecoder unmarshals the UseInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UseInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UseInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Use[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UseInstruction", Instruction_Use[:], discriminator)
	}
	// Deserialize `UseArgs`:
	err = decoder.Decode(&obj.UseArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UseInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules_program", err)
	}
	indices = append(indices, index)
	// Decode from authorization_rules account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authorization_rules", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UseInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 12 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 12, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules_program", len(accountKeys)-1)
	}
	obj.AuthorizationRulesProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authorization_rules account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authorization_rules", len(accountKeys)-1)
	}
	obj.AuthorizationRules = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UseInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.AuthorizationRulesProgram)
	keys = append(keys, obj.AuthorizationRules)
	return keys
}

// Unmarshal unmarshals the UseInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UseInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UseInstruction: %w", err)
	}
	return nil
}

// UnmarshalUseInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUseInstruction(buf []byte) (*UseInstruction, error) {
	obj := new(UseInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type VerifyInstruction struct {
	VerificationArgs VerificationArgs `json:"verification_args"`

	// Accounts:
	Authority                       solanago.PublicKey `json:"authority"`
	AuthoritySigner                 bool               `json:"authority_signer"`
	DelegateRecord                  solanago.PublicKey `json:"delegate_record"`
	DelegateRecordOptional          bool               `json:"delegate_record_optional"`
	Metadata                        solanago.PublicKey `json:"metadata"`
	MetadataWritable                bool               `json:"metadata_writable"`
	CollectionMint                  solanago.PublicKey `json:"collection_mint"`
	CollectionMintOptional          bool               `json:"collection_mint_optional"`
	CollectionMetadata              solanago.PublicKey `json:"collection_metadata"`
	CollectionMetadataWritable      bool               `json:"collection_metadata_writable"`
	CollectionMetadataOptional      bool               `json:"collection_metadata_optional"`
	CollectionMasterEdition         solanago.PublicKey `json:"collection_master_edition"`
	CollectionMasterEditionOptional bool               `json:"collection_master_edition_optional"`
	SystemProgram                   solanago.PublicKey `json:"system_program"`
	SysvarInstructions              solanago.PublicKey `json:"sysvar_instructions"`
}

func (obj *VerifyInstruction) GetDiscriminator() []byte {
	return Instruction_Verify[:]
}

// UnmarshalWithDecoder unmarshals the VerifyInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *VerifyInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "VerifyInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Verify[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "VerifyInstruction", Instruction_Verify[:], discriminator)
	}
	// Deserialize `VerificationArgs`:
	err = decoder.Decode(&obj.VerificationArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *VerifyInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_master_edition", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *VerifyInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 8 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 8, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_master_edition", len(accountKeys)-1)
	}
	obj.CollectionMasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *VerifyInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.CollectionMetadata)
	keys = append(keys, obj.CollectionMasterEdition)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	return keys
}

// Unmarshal unmarshals the VerifyInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *VerifyInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling VerifyInstruction: %w", err)
	}
	return nil
}

// UnmarshalVerifyInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalVerifyInstruction(buf []byte) (*VerifyInstruction, error) {
	obj := new(VerifyInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type UnverifyInstruction struct {
	VerificationArgs VerificationArgs `json:"verification_args"`

	// Accounts:
	Authority                  solanago.PublicKey `json:"authority"`
	AuthoritySigner            bool               `json:"authority_signer"`
	DelegateRecord             solanago.PublicKey `json:"delegate_record"`
	DelegateRecordOptional     bool               `json:"delegate_record_optional"`
	Metadata                   solanago.PublicKey `json:"metadata"`
	MetadataWritable           bool               `json:"metadata_writable"`
	CollectionMint             solanago.PublicKey `json:"collection_mint"`
	CollectionMintOptional     bool               `json:"collection_mint_optional"`
	CollectionMetadata         solanago.PublicKey `json:"collection_metadata"`
	CollectionMetadataWritable bool               `json:"collection_metadata_writable"`
	CollectionMetadataOptional bool               `json:"collection_metadata_optional"`
	SystemProgram              solanago.PublicKey `json:"system_program"`
	SysvarInstructions         solanago.PublicKey `json:"sysvar_instructions"`
}

func (obj *UnverifyInstruction) GetDiscriminator() []byte {
	return Instruction_Unverify[:]
}

// UnmarshalWithDecoder unmarshals the UnverifyInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *UnverifyInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "UnverifyInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Unverify[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "UnverifyInstruction", Instruction_Unverify[:], discriminator)
	}
	// Deserialize `VerificationArgs`:
	err = decoder.Decode(&obj.VerificationArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *UnverifyInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from delegate_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "delegate_record", err)
	}
	indices = append(indices, index)
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from collection_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_mint", err)
	}
	indices = append(indices, index)
	// Decode from collection_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "collection_metadata", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *UnverifyInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set delegate_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "delegate_record", len(accountKeys)-1)
	}
	obj.DelegateRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_mint", len(accountKeys)-1)
	}
	obj.CollectionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set collection_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "collection_metadata", len(accountKeys)-1)
	}
	obj.CollectionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *UnverifyInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.DelegateRecord)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.CollectionMint)
	keys = append(keys, obj.CollectionMetadata)
	keys = append(keys, obj.SystemProgram)
	keys = append(keys, obj.SysvarInstructions)
	return keys
}

// Unmarshal unmarshals the UnverifyInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *UnverifyInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling UnverifyInstruction: %w", err)
	}
	return nil
}

// UnmarshalUnverifyInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalUnverifyInstruction(buf []byte) (*UnverifyInstruction, error) {
	obj := new(UnverifyInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CollectInstruction struct {

	// Accounts:
	Authority       solanago.PublicKey `json:"authority"`
	AuthoritySigner bool               `json:"authority_signer"`
	Recipient       solanago.PublicKey `json:"recipient"`
}

func (obj *CollectInstruction) GetDiscriminator() []byte {
	return Instruction_Collect[:]
}

// UnmarshalWithDecoder unmarshals the CollectInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CollectInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CollectInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Collect[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CollectInstruction", Instruction_Collect[:], discriminator)
	}
	return nil
}

func (obj *CollectInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from recipient account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "recipient", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CollectInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 2 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 2, len(indices))
	}
	indexOffset := 0
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set recipient account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "recipient", len(accountKeys)-1)
	}
	obj.Recipient = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CollectInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Recipient)
	return keys
}

// Unmarshal unmarshals the CollectInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CollectInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CollectInstruction: %w", err)
	}
	return nil
}

// UnmarshalCollectInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCollectInstruction(buf []byte) (*CollectInstruction, error) {
	obj := new(CollectInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type PrintInstruction struct {
	PrintArgs PrintArgs `json:"print_args"`

	// Accounts:
	EditionMetadata             solanago.PublicKey `json:"edition_metadata"`
	EditionMetadataWritable     bool               `json:"edition_metadata_writable"`
	Edition                     solanago.PublicKey `json:"edition"`
	EditionWritable             bool               `json:"edition_writable"`
	EditionMint                 solanago.PublicKey `json:"edition_mint"`
	EditionMintWritable         bool               `json:"edition_mint_writable"`
	EditionTokenAccountOwner    solanago.PublicKey `json:"edition_token_account_owner"`
	EditionTokenAccount         solanago.PublicKey `json:"edition_token_account"`
	EditionTokenAccountWritable bool               `json:"edition_token_account_writable"`
	EditionMintAuthority        solanago.PublicKey `json:"edition_mint_authority"`
	EditionMintAuthoritySigner  bool               `json:"edition_mint_authority_signer"`
	EditionTokenRecord          solanago.PublicKey `json:"edition_token_record"`
	EditionTokenRecordWritable  bool               `json:"edition_token_record_writable"`
	EditionTokenRecordOptional  bool               `json:"edition_token_record_optional"`
	MasterEdition               solanago.PublicKey `json:"master_edition"`
	MasterEditionWritable       bool               `json:"master_edition_writable"`
	EditionMarkerPda            solanago.PublicKey `json:"edition_marker_pda"`
	EditionMarkerPdaWritable    bool               `json:"edition_marker_pda_writable"`
	Payer                       solanago.PublicKey `json:"payer"`
	PayerWritable               bool               `json:"payer_writable"`
	PayerSigner                 bool               `json:"payer_signer"`
	MasterTokenAccountOwner     solanago.PublicKey `json:"master_token_account_owner"`
	MasterTokenAccount          solanago.PublicKey `json:"master_token_account"`
	MasterMetadata              solanago.PublicKey `json:"master_metadata"`
	UpdateAuthority             solanago.PublicKey `json:"update_authority"`
	SplTokenProgram             solanago.PublicKey `json:"spl_token_program"`
	SplAtaProgram               solanago.PublicKey `json:"spl_ata_program"`
	SysvarInstructions          solanago.PublicKey `json:"sysvar_instructions"`
	SystemProgram               solanago.PublicKey `json:"system_program"`
}

func (obj *PrintInstruction) GetDiscriminator() []byte {
	return Instruction_Print[:]
}

// UnmarshalWithDecoder unmarshals the PrintInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *PrintInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	var err error
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "PrintInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Print[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "PrintInstruction", Instruction_Print[:], discriminator)
	}
	// Deserialize `PrintArgs`:
	err = decoder.Decode(&obj.PrintArgs)
	if err != nil {
		return err
	}
	return nil
}

func (obj *PrintInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from edition_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from edition_mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_mint", err)
	}
	indices = append(indices, index)
	// Decode from edition_token_account_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_token_account_owner", err)
	}
	indices = append(indices, index)
	// Decode from edition_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_token_account", err)
	}
	indices = append(indices, index)
	// Decode from edition_mint_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_mint_authority", err)
	}
	indices = append(indices, index)
	// Decode from edition_token_record account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_token_record", err)
	}
	indices = append(indices, index)
	// Decode from master_edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_edition", err)
	}
	indices = append(indices, index)
	// Decode from edition_marker_pda account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition_marker_pda", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from master_token_account_owner account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_token_account_owner", err)
	}
	indices = append(indices, index)
	// Decode from master_token_account account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_token_account", err)
	}
	indices = append(indices, index)
	// Decode from master_metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "master_metadata", err)
	}
	indices = append(indices, index)
	// Decode from update_authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "update_authority", err)
	}
	indices = append(indices, index)
	// Decode from spl_token_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_token_program", err)
	}
	indices = append(indices, index)
	// Decode from spl_ata_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "spl_ata_program", err)
	}
	indices = append(indices, index)
	// Decode from sysvar_instructions account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "sysvar_instructions", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *PrintInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 18 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 18, len(indices))
	}
	indexOffset := 0
	// Set edition_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_metadata", len(accountKeys)-1)
	}
	obj.EditionMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_mint", len(accountKeys)-1)
	}
	obj.EditionMint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_token_account_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_token_account_owner", len(accountKeys)-1)
	}
	obj.EditionTokenAccountOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_token_account", len(accountKeys)-1)
	}
	obj.EditionTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_mint_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_mint_authority", len(accountKeys)-1)
	}
	obj.EditionMintAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_token_record account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_token_record", len(accountKeys)-1)
	}
	obj.EditionTokenRecord = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_edition", len(accountKeys)-1)
	}
	obj.MasterEdition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition_marker_pda account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition_marker_pda", len(accountKeys)-1)
	}
	obj.EditionMarkerPda = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_token_account_owner account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_token_account_owner", len(accountKeys)-1)
	}
	obj.MasterTokenAccountOwner = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_token_account account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_token_account", len(accountKeys)-1)
	}
	obj.MasterTokenAccount = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set master_metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "master_metadata", len(accountKeys)-1)
	}
	obj.MasterMetadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set update_authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "update_authority", len(accountKeys)-1)
	}
	obj.UpdateAuthority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_token_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_token_program", len(accountKeys)-1)
	}
	obj.SplTokenProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set spl_ata_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "spl_ata_program", len(accountKeys)-1)
	}
	obj.SplAtaProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set sysvar_instructions account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "sysvar_instructions", len(accountKeys)-1)
	}
	obj.SysvarInstructions = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *PrintInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.EditionMetadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.EditionMint)
	keys = append(keys, obj.EditionTokenAccountOwner)
	keys = append(keys, obj.EditionTokenAccount)
	keys = append(keys, obj.EditionMintAuthority)
	keys = append(keys, obj.EditionTokenRecord)
	keys = append(keys, obj.MasterEdition)
	keys = append(keys, obj.EditionMarkerPda)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.MasterTokenAccountOwner)
	keys = append(keys, obj.MasterTokenAccount)
	keys = append(keys, obj.MasterMetadata)
	keys = append(keys, obj.UpdateAuthority)
	keys = append(keys, obj.SplTokenProgram)
	keys = append(keys, obj.SplAtaProgram)
	keys = append(keys, obj.SysvarInstructions)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the PrintInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *PrintInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling PrintInstruction: %w", err)
	}
	return nil
}

// UnmarshalPrintInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalPrintInstruction(buf []byte) (*PrintInstruction, error) {
	obj := new(PrintInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type ResizeInstruction struct {

	// Accounts:
	Metadata          solanago.PublicKey `json:"metadata"`
	MetadataWritable  bool               `json:"metadata_writable"`
	Edition           solanago.PublicKey `json:"edition"`
	EditionWritable   bool               `json:"edition_writable"`
	Mint              solanago.PublicKey `json:"mint"`
	Payer             solanago.PublicKey `json:"payer"`
	PayerWritable     bool               `json:"payer_writable"`
	Authority         solanago.PublicKey `json:"authority"`
	AuthoritySigner   bool               `json:"authority_signer"`
	AuthorityOptional bool               `json:"authority_optional"`
	Token             solanago.PublicKey `json:"token"`
	TokenOptional     bool               `json:"token_optional"`
	SystemProgram     solanago.PublicKey `json:"system_program"`
}

func (obj *ResizeInstruction) GetDiscriminator() []byte {
	return Instruction_Resize[:]
}

// UnmarshalWithDecoder unmarshals the ResizeInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *ResizeInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "ResizeInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_Resize[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "ResizeInstruction", Instruction_Resize[:], discriminator)
	}
	return nil
}

func (obj *ResizeInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from payer account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "payer", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from token account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "token", err)
	}
	indices = append(indices, index)
	// Decode from system_program account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "system_program", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *ResizeInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 7 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 7, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set payer account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "payer", len(accountKeys)-1)
	}
	obj.Payer = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set token account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "token", len(accountKeys)-1)
	}
	obj.Token = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set system_program account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "system_program", len(accountKeys)-1)
	}
	obj.SystemProgram = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *ResizeInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Payer)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Token)
	keys = append(keys, obj.SystemProgram)
	return keys
}

// Unmarshal unmarshals the ResizeInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *ResizeInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling ResizeInstruction: %w", err)
	}
	return nil
}

// UnmarshalResizeInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalResizeInstruction(buf []byte) (*ResizeInstruction, error) {
	obj := new(ResizeInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

type CloseAccountsInstruction struct {

	// Accounts:
	Metadata            solanago.PublicKey `json:"metadata"`
	MetadataWritable    bool               `json:"metadata_writable"`
	Edition             solanago.PublicKey `json:"edition"`
	EditionWritable     bool               `json:"edition_writable"`
	Mint                solanago.PublicKey `json:"mint"`
	MintWritable        bool               `json:"mint_writable"`
	Authority           solanago.PublicKey `json:"authority"`
	AuthoritySigner     bool               `json:"authority_signer"`
	Destination         solanago.PublicKey `json:"destination"`
	DestinationWritable bool               `json:"destination_writable"`
}

func (obj *CloseAccountsInstruction) GetDiscriminator() []byte {
	return Instruction_CloseAccounts[:]
}

// UnmarshalWithDecoder unmarshals the CloseAccountsInstruction from Borsh-encoded bytes prefixed with its discriminator.
func (obj *CloseAccountsInstruction) UnmarshalWithDecoder(decoder *binary.Decoder) error {
	// Read the discriminator and check it against the expected value:
	discriminator, err := decoder.ReadNBytes(1)
	if err != nil {
		return fmt.Errorf("failed to read instruction discriminator for %s: %w", "CloseAccountsInstruction", err)
	}
	if !bytes.Equal(discriminator, Instruction_CloseAccounts[:]) {
		return fmt.Errorf("instruction discriminator mismatch for %s: expected %v, got %v", "CloseAccountsInstruction", Instruction_CloseAccounts[:], discriminator)
	}
	return nil
}

func (obj *CloseAccountsInstruction) UnmarshalAccountIndices(buf []byte) ([]uint8, error) {
	// UnmarshalAccountIndices decodes account indices from Borsh-encoded bytes
	decoder := binary.NewBorshDecoder(buf)
	indices := make([]uint8, 0)
	index := uint8(0)
	var err error
	// Decode from metadata account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "metadata", err)
	}
	indices = append(indices, index)
	// Decode from edition account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "edition", err)
	}
	indices = append(indices, index)
	// Decode from mint account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "mint", err)
	}
	indices = append(indices, index)
	// Decode from authority account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "authority", err)
	}
	indices = append(indices, index)
	// Decode from destination account index
	index = uint8(0)
	err = decoder.Decode(&index)
	if err != nil {
		return nil, fmt.Errorf("failed to decode %s account index: %w", "destination", err)
	}
	indices = append(indices, index)
	return indices, nil
}

func (obj *CloseAccountsInstruction) PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error {
	// PopulateFromAccountIndices sets account public keys from indices and account keys array
	if len(indices) != 5 {
		return fmt.Errorf("mismatch between expected accounts (%d) and provided indices (%d)", 5, len(indices))
	}
	indexOffset := 0
	// Set metadata account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "metadata", len(accountKeys)-1)
	}
	obj.Metadata = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set edition account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "edition", len(accountKeys)-1)
	}
	obj.Edition = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set mint account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "mint", len(accountKeys)-1)
	}
	obj.Mint = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set authority account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "authority", len(accountKeys)-1)
	}
	obj.Authority = accountKeys[indices[indexOffset]]
	indexOffset++
	// Set destination account from index
	if indices[indexOffset] >= uint8(len(accountKeys)) {
		return fmt.Errorf("account index %d for %s is out of bounds (max: %d)", indices[indexOffset], "destination", len(accountKeys)-1)
	}
	obj.Destination = accountKeys[indices[indexOffset]]
	indexOffset++
	return nil
}

func (obj *CloseAccountsInstruction) GetAccountKeys() []solanago.PublicKey {
	keys := make([]solanago.PublicKey, 0)
	keys = append(keys, obj.Metadata)
	keys = append(keys, obj.Edition)
	keys = append(keys, obj.Mint)
	keys = append(keys, obj.Authority)
	keys = append(keys, obj.Destination)
	return keys
}

// Unmarshal unmarshals the CloseAccountsInstruction from Borsh-encoded bytes prefixed with the discriminator.
func (obj *CloseAccountsInstruction) Unmarshal(buf []byte) error {
	var err error
	err = obj.UnmarshalWithDecoder(binary.NewBorshDecoder(buf))
	if err != nil {
		return fmt.Errorf("error while unmarshaling CloseAccountsInstruction: %w", err)
	}
	return nil
}

// UnmarshalCloseAccountsInstruction unmarshals the instruction from Borsh-encoded bytes prefixed with the discriminator.
func UnmarshalCloseAccountsInstruction(buf []byte) (*CloseAccountsInstruction, error) {
	obj := new(CloseAccountsInstruction)
	var err error
	err = obj.Unmarshal(buf)
	if err != nil {
		return nil, err
	}
	return obj, nil
}

// Instruction interface defines common methods for all instruction types
type Instruction interface {
	GetDiscriminator() []byte

	UnmarshalWithDecoder(decoder *binary.Decoder) error

	UnmarshalAccountIndices(buf []byte) ([]uint8, error)

	PopulateFromAccountIndices(indices []uint8, accountKeys []solanago.PublicKey) error

	GetAccountKeys() []solanago.PublicKey
}

// ParseInstruction parses instruction data and optionally populates accounts
// If accountIndicesData is nil or empty, accounts will not be populated
func ParseInstruction(instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (Instruction, error) {
	// Validate inputs
	if len(instructionData) < 1 {
		return nil, fmt.Errorf("instruction data too short: expected at least 1 bytes, got %d", len(instructionData))
	}
	// Extract discriminator
	discriminator := [1]byte{}
	copy(discriminator[:], instructionData[0:1])
	// Parse based on discriminator
	switch discriminator {
	case Instruction_CreateMetadataAccount:
		instruction := new(CreateMetadataAccountInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateMetadataAccountInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateMetadataAccount:
		instruction := new(UpdateMetadataAccountInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateMetadataAccountInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DeprecatedCreateMasterEdition:
		instruction := new(DeprecatedCreateMasterEditionInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DeprecatedCreateMasterEditionInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DeprecatedMintNewEditionFromMasterEditionViaPrintingToken:
		instruction := new(DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DeprecatedMintNewEditionFromMasterEditionViaPrintingTokenInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdatePrimarySaleHappenedViaToken:
		instruction := new(UpdatePrimarySaleHappenedViaTokenInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdatePrimarySaleHappenedViaTokenInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DeprecatedSetReservationList:
		instruction := new(DeprecatedSetReservationListInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DeprecatedSetReservationListInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DeprecatedCreateReservationList:
		instruction := new(DeprecatedCreateReservationListInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DeprecatedCreateReservationListInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SignMetadata:
		instruction := new(SignMetadataInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SignMetadataInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DeprecatedMintPrintingTokensViaToken:
		instruction := new(DeprecatedMintPrintingTokensViaTokenInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DeprecatedMintPrintingTokensViaTokenInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_DeprecatedMintPrintingTokens:
		instruction := new(DeprecatedMintPrintingTokensInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DeprecatedMintPrintingTokensInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateMasterEdition:
		instruction := new(CreateMasterEditionInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateMasterEditionInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_MintNewEditionFromMasterEditionViaToken:
		instruction := new(MintNewEditionFromMasterEditionViaTokenInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as MintNewEditionFromMasterEditionViaTokenInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ConvertMasterEditionV1ToV2:
		instruction := new(ConvertMasterEditionV1ToV2Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ConvertMasterEditionV1ToV2Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_MintNewEditionFromMasterEditionViaVaultProxy:
		instruction := new(MintNewEditionFromMasterEditionViaVaultProxyInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as MintNewEditionFromMasterEditionViaVaultProxyInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_PuffMetadata:
		instruction := new(PuffMetadataInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as PuffMetadataInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UpdateMetadataAccountV2:
		instruction := new(UpdateMetadataAccountV2Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateMetadataAccountV2Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateMetadataAccountV2:
		instruction := new(CreateMetadataAccountV2Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateMetadataAccountV2Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateMasterEditionV3:
		instruction := new(CreateMasterEditionV3Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateMasterEditionV3Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_VerifyCollection:
		instruction := new(VerifyCollectionInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as VerifyCollectionInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Utilize:
		instruction := new(UtilizeInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UtilizeInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ApproveUseAuthority:
		instruction := new(ApproveUseAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ApproveUseAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RevokeUseAuthority:
		instruction := new(RevokeUseAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RevokeUseAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UnverifyCollection:
		instruction := new(UnverifyCollectionInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UnverifyCollectionInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ApproveCollectionAuthority:
		instruction := new(ApproveCollectionAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ApproveCollectionAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RevokeCollectionAuthority:
		instruction := new(RevokeCollectionAuthorityInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RevokeCollectionAuthorityInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetAndVerifyCollection:
		instruction := new(SetAndVerifyCollectionInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetAndVerifyCollectionInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_FreezeDelegatedAccount:
		instruction := new(FreezeDelegatedAccountInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as FreezeDelegatedAccountInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_ThawDelegatedAccount:
		instruction := new(ThawDelegatedAccountInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ThawDelegatedAccountInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_RemoveCreatorVerification:
		instruction := new(RemoveCreatorVerificationInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RemoveCreatorVerificationInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_BurnNft:
		instruction := new(BurnNftInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as BurnNftInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_VerifySizedCollectionItem:
		instruction := new(VerifySizedCollectionItemInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as VerifySizedCollectionItemInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_UnverifySizedCollectionItem:
		instruction := new(UnverifySizedCollectionItemInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UnverifySizedCollectionItemInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetAndVerifySizedCollectionItem:
		instruction := new(SetAndVerifySizedCollectionItemInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetAndVerifySizedCollectionItemInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateMetadataAccountV3:
		instruction := new(CreateMetadataAccountV3Instruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateMetadataAccountV3Instruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetCollectionSize:
		instruction := new(SetCollectionSizeInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetCollectionSizeInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_SetTokenStandard:
		instruction := new(SetTokenStandardInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as SetTokenStandardInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_BubblegumSetCollectionSize:
		instruction := new(BubblegumSetCollectionSizeInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as BubblegumSetCollectionSizeInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_BurnEditionNft:
		instruction := new(BurnEditionNftInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as BurnEditionNftInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CreateEscrowAccount:
		instruction := new(CreateEscrowAccountInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateEscrowAccountInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CloseEscrowAccount:
		instruction := new(CloseEscrowAccountInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CloseEscrowAccountInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_TransferOutOfEscrow:
		instruction := new(TransferOutOfEscrowInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as TransferOutOfEscrowInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Burn:
		instruction := new(BurnInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as BurnInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Create:
		instruction := new(CreateInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CreateInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Mint:
		instruction := new(MintInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as MintInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Delegate:
		instruction := new(DelegateInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as DelegateInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Revoke:
		instruction := new(RevokeInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as RevokeInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Lock:
		instruction := new(LockInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as LockInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Unlock:
		instruction := new(UnlockInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UnlockInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Migrate:
		instruction := new(MigrateInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as MigrateInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Transfer:
		instruction := new(TransferInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as TransferInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Update:
		instruction := new(UpdateInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UpdateInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Use:
		instruction := new(UseInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UseInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Verify:
		instruction := new(VerifyInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as VerifyInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Unverify:
		instruction := new(UnverifyInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as UnverifyInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Collect:
		instruction := new(CollectInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CollectInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Print:
		instruction := new(PrintInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as PrintInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_Resize:
		instruction := new(ResizeInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as ResizeInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	case Instruction_CloseAccounts:
		instruction := new(CloseAccountsInstruction)
		decoder := binary.NewBorshDecoder(instructionData)
		err := instruction.UnmarshalWithDecoder(decoder)
		if err != nil {
			return nil, fmt.Errorf("failed to unmarshal instruction as CloseAccountsInstruction: %w", err)
		}
		if accountIndicesData != nil && len(accountIndicesData) > 0 {
			indices, err := instruction.UnmarshalAccountIndices(accountIndicesData)
			if err != nil {
				return nil, fmt.Errorf("failed to unmarshal account indices: %w", err)
			}
			err = instruction.PopulateFromAccountIndices(indices, accountKeys)
			if err != nil {
				return nil, fmt.Errorf("failed to populate accounts: %w", err)
			}
		}
		return instruction, nil
	default:
		return nil, fmt.Errorf("unknown instruction discriminator: %v", discriminator[:])
	}
}

// ParseInstructionTyped parses instruction data and returns a specific instruction type // T must implement the Instruction interface
func ParseInstructionTyped[T Instruction](instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (T, error) {
	instruction, err := ParseInstruction(instructionData, accountIndicesData, accountKeys)
	if err != nil {
		return *new(T), err
	}
	typed, ok := instruction.(T)
	if !ok {
		return *new(T), fmt.Errorf("instruction is not of expected type")
	}
	return typed, nil
}

// ParseInstructionWithoutAccounts parses instruction data without account information
func ParseInstructionWithoutAccounts(instructionData []byte) (Instruction, error) {
	return ParseInstruction(instructionData, nil, []solanago.PublicKey{})
}

// ParseInstructionWithAccounts parses instruction data with account information
func ParseInstructionWithAccounts(instructionData []byte, accountIndicesData []byte, accountKeys []solanago.PublicKey) (Instruction, error) {
	return ParseInstruction(instructionData, accountIndicesData, accountKeys)
}
